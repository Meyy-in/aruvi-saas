# App shell (page.jsx + layout.jsx + GuidedTour + ProfilePortal + MeyyMark + shared-setup)

> ★ **RECONCILED 2026-09-18.** Drawn 2026-09-15 and never re-read while 5d, 5e, 6a–6d and 8b landed on top of it, so it had been saying MISSING about a gear, a bottom nav and an Ask Meyy panel that all ship. **43 rows moved**, each with a dated `★ 2026-09-18 reconcile:` clause naming the file that closed it. Of the 95 feature rows: **85 DONE · 5 WEB-ONLY-BY-NATURE · 5 NOT-PORTED-BY-DECISION · nothing MISSING.** (Rows 96–118 are the tour-step record; 8b shipped against them.)
> **What is still owed from this family, all small:** the brand mark is 22px on the phone where the web is 19px at ≤600px (row 26); `originRoute` in `lib/portal.js` is written and never read (row 78 — `leavePortal`, its only reader, was deleted today). **Web owed:** the set-up check fires only from `finishTour` on the web, where the phone raises it for everyone who finishes first run (row 74), and the web still fetches `/readiness` itself instead of reading the shared store (row 11).
> ⚠️ **The open questions at the foot are all answered** — Q6/Q7/Q8 on 2026-09-16 as noted, the gate location (item 3) by putting it in the layout, and the name on the bar (item 4) by `shared/account.js`.

Snapshot note: `web/app/lib/` in the snapshot holds only `budget.js`, `shared-setup.js`, `useSupportedGrades.js`. The thin wrappers page.jsx imports (`./lib/format`, `./lib/verify`, `./lib/sectionState`, `./lib/sectionHistory`, `./lib/auth`, `./ask-aruvi/bank`) are absent from the upload; per CLAUDE.md §5 they are one-line re-exports of `@aruvi/shared/*` after `import "./shared-setup"`. Line refs below for those helpers point at the shared package, which is the real logic on both surfaces.

## Web source map

- `web/app/page.jsx` (1493 lines) — the ONE shell component `Home`, plus everything the phone's routes have "nothing above them" for:
  - 1–22 imports (format helpers, verify, sectionState, sectionHistory, auth, `clearTeacherCaches`, all view components, `GuidedTour`, `ProfilePortal` + its five queue exports, `AskAruvi`, bank `primeBank/clearBank/refreshBank`, `MeyyMark`).
  - 24–47 header comments: the login gate, two-tab nav history, activation gate = `ready` (server-derived, no local flag).
  - 49–74 core state: `user` (null = not yet read from storage), `subjects/subject/grades/grade`, `tab` ("myplans"|"generate"), `ready`, `readiness`, `readinessLoaded`, `editFlow` (null|"lessonplans"|"profile"|"settings"), `profileAutoAdd` (always null), `profilePortal` (one-shot intent), `pendingOpen`, `prepareReturn`, `pendingAttach`, `generateEntry`, `askOpen`.
  - 76–114 tour state (`tour`, `tourInfo`, `tourDismissed` session-only) + bank refresh effect on every signed-in load (`refreshBank()`).
  - 116–131 `tourEligible` effect: GET `/section-state` → eligible iff ≤1 bound section AND no progress; null on failure.
  - 136–167 `entLapsed`, `entTrial`, `tourSpent`, `tourOfferedThisSession`, `tourOnOffer`, POST `/account/tour-offered` once per offer.
  - 197–208 `portalWin` descriptor (three moods) + `finishTour` (close Ask, clear tour, session-dismiss, `goClasses()`, open check window `{mode:"check", reason:"tour"}`).
  - 210–234 setup-check diff effect: subject·class keys of `readiness` diffed against the previous set per user → `queueSetupCheck(added)` + `pruneSetupCheck(keys)`; first resolved profile only seeds.
  - 236–260 `onLessonsScope`: `takeSetupCheck` → 1 s timer (`SETUP_CHECK_DELAY_MS`) → `{mode:"check", reason:"added", subject, grade}`.
  - 265 `startTour` (goClasses then step 1).
  - 267–279 `sectionFailed` + `setSectionMismatchHandler` (re-pull then one sentence).
  - 282 restore user from storage on mount.
  - 289–293 `html.app-shell` class toggle (document scroll lock; `.bodycontent` is the scroller).
  - 306–333 CSS-var publisher `--nav-h` / `--hdr-h` / `--bnav-h` via ResizeObserver + resize.
  - 339–355 readiness rehydrate on user change: GET `/readiness`; 401 → `onSignOut()`.
  - 370–390 `saveFailed` + `verifyReadiness` (read-after-write: POST `/readiness` then GET, fingerprint compare; on mismatch re-sync `readiness`/`ready` and show caption).
  - 399–439 `firstGenNeeded` heuristic (GET `/plans-prepared` + `/section-state` + `/academic-year`; latch refs `everGeneratedRef`/`latchUserRef`).
  - 441–480 `onFirstRunComplete(payload, preparing)` (merge into base profile, `goLessons()`, seed `preparingCard`, `verifyReadiness`).
  - 482–495 GET `/subjects`, GET `/subjects/{s}/grades` scoping effects.
  - 500–504 `onReadyComplete` (legacy Readiness path).
  - 509–546 `onAllocateScoped`, `subjectSlugify`, `onEnterGenerate` (the single route into Generate; picker vs scoped; `prepareReturn`).
  - 554–601 `onPrepared`, `preparingCard` state, `onPreparing` (returns true only when the card is taken), `onPrepareError` (card stays, `failed:true`), `onDismissPrepareError`.
  - 609–619 paywall (`paywall` string, `onPaywall`), `subscribeOpen`, `entSyncTick`.
  - 624–630 `onOpenSection` deep-link (My Lessons → My Classes section open).
  - 632–638 `onEnter(id)`: `setUser`, `primeBank()`.
  - 650–656 `onErased` (device sweep, keep `user` so the farewell renders).
  - 658–675 `onSignOut` (idempotent: clearUser, `signOutAuth`, `clearTeacherCaches([...4 web prefixes])`, reset all shell state).
  - 685–688 `lessonsPaneIntentRef`, `goClasses`, `goLessons`.
  - 700–724 `tourNext` / `tourBack` shell-level navigation per step.
  - 725 `goProfile` (bare profile, ordinary tab row).
  - 744–759 `displayName` effect: GET `/account` → first name capitalised (numeric → ""), plus `tourSpent` from `tour_offered_at`; re-run on `entSyncTick`.
  - 768–835 academic-year cutover: `yearInfo` (GET `/academic-year`, re-read on focus/visibilitychange), `cutoverBusy/Result/Dismissed` (per-user reset), `runCutover` (POST `/academic-year/cutover` → clear section + history caches, latch, re-pull).
  - 845–886 entitlement sync: `fetchEntitlement()` on mount + visibilitychange + focus + 20 s interval → `entLapsed`, `entTrial`, `paidScopes`.
  - 892–895 lapsed → forced to My Lessons.
  - 897–907 Settings context: `settingsOriginRef`, `settingsView`, `profileViaSettings`, `legalDoc`, `goSettings`.
  - 919–935 privacy-notice bar: GET `/legal/privacy/status` once per sign-in; `stampPrivacySeen` POST `/legal/privacy/seen`; `readPrivacyNote`.
  - 936–939 `openProfileFromSettings`; 948–970 `settingsClose` (✕ semantics, erased → sign out); 971–980 `inSettingsBar`, `settingsBarLabel`.
  - 998–1062 portal plumbing: `portalOriginRef`, `profilePortalScope`, `onProfilePortal(kind)`, `onEditYearBudget`, `goPortalHome`, `openFullProfile`.
  - 1065–1066 `activeNav`.
  - 1069–1082 render gates: null while `user === null`; `<Login>`; null until `readinessLoaded`; `<FirstRun>` when `!ready || firstGenNeeded`.
  - 1087–1116 `setupCheckSub` (check-window sub-line copy); 1139–1177 `setupCheckValues` (class/section/ppw/budget current values).
  - 1179–1248 top bar JSX: `.topbar` › `.hdr` (brand + gear + name/Log out) › optional `.set-bar`; `.topbar-spacer`.
  - 1250–1344 `.bodycontent` › `main`: `saveFailed`, `sectionFailed`, `privacyNote` bars; the four view branches (MyLessonPlans / TeachingProfile / Settings / "Connecting…" / GenerateTab / MyPlans) with every prop.
  - 1371–1422 `.bnav` bottom nav (4 items, SVG glyphs, lapsed hides two, `data-tour` anchors).
  - 1426–1428 `<GuidedTour>`; 1438–1443 `<ProfilePortal>`; 1448 `<AskAruvi>`; 1449–1467 SubscribeFlow overlay; 1468–1489 paywall modal.
- `web/app/layout.jsx` (74 lines) — root HTML: `metadata` (title "Meyy"), `viewport` (`viewportFit: "cover"`, `themeColor` pine per scheme), `THEME_SCRIPT` (no-flash theme resolver: `history.scrollRestoration = 'manual'`, reads `aruvi-theme` from localStorage, stamps `data-theme` / `data-theme-effective` on `<html>`, re-syncs `<meta name="theme-color">`, exposes `window.__aruviTheme.get/set`, listens to `prefers-color-scheme` changes).
- `web/app/components/GuidedTour.jsx` (295 lines) — 43 `fallback` info; 46 `Plus` glyph; 50–56 outline `Hand` SVG; 58–146 `STEPS` (20 entries); 149–157 DOM query helpers; 159–214 measurement poll (200 ms interval + resize + capture-phase scroll; instant scroll-into-view with retry caps; `scrollTop` pins `.bodycontent`); 216–262 placement maths (`center` / `over`+`lift` / `above` / `below` clamped; hand at corner or centre); 264–294 render: `.gt-root.gt-block` › `.gt-scrim` (hit-blocking) › `.gt-ring` (box-shadow spotlight) › `.gt-hand` › `.gt-tip` (title, body, foot with "N of 20", "← Back", "Skip", "Next →"/"Done ✓").
- `web/app/components/ProfilePortal.jsx` (184 lines) — 56–61 `ROWS` (class · section · ppw · budget); 76–119 component (two moods, values in check mood only, footer "Want to see your full teaching profile?"); 135–144 localStorage queue under `userKey("setup_check_pending")` capped at 24; 146 `setupKey`; 152 `SETUP_CHECK_DELAY_MS = 1000`; 155–159 `queueSetupCheck`; 162–167 `takeSetupCheck`; 179–184 `pruneSetupCheck`.
- `web/app/components/MeyyMark.jsx` (53 lines) — inline SVG wordmark, viewBox `12.5 0 413 110`, `stroke="currentColor"`, two `.brand-mark-dot` circles, circled ™; sizing in globals.css `.brand-mark`.
- `web/app/lib/shared-setup.js` (33 lines) — installs `webStorage()`, `configure({ apiBase: NEXT_PUBLIC_API_URL || http://{hostname}:8000, accessToken })`, creates the supabase-js client (persistSession, autoRefreshToken) when env set.

## Mobile counterpart map

- `mobile/app/_layout.jsx` (38) — imports `../lib/boot` first; `useFonts(FONT_MAP)` + splash hold; `SafeAreaProvider` › `ThemeProvider` › `Shell` (`StatusBar style="light" backgroundColor={t.bar_fill}`; `Stack` headerShown:false, paper background, fade animation).
- `mobile/app/index.jsx` (8) — `<Redirect href={getUser() ? "/(app)" : "/login"} />` (synchronous; no login flash).
- `mobile/app/(app)/_layout.jsx` (49) — signed-in shell: `Redirect` to `/login` when no user; `Stack`; `<BottomNav active={pathname.startsWith("/lessons") ? "lessons" : "classes"} onClasses=navigate("/") onLessons=navigate("/lessons") onAdd={() => {}} onAsk={() => {}} />` (Add and Ask Meyy INERT). No `showClasses`/`showAdd` wiring (no lapsed state).
- `mobile/lib/boot.js` (76) — phone twin of shared-setup: kv = `expo-sqlite/kv-store` sync adapter (web target → localStorage), `configure({ apiBase: EXPO_PUBLIC_API_URL || localhost:8000, accessToken })`, supabase client with the same kv storage + `detectSessionInUrl:false`, AppState → `startAutoRefresh/stopAutoRefresh`, exports `IS_WEB`, `kv`, `API_URL`, `supabase`, `authConfigured`.
- `mobile/lib/session.js` (25) — `endSession(router)`: `signOutAuth()`, `clearTeacherCaches(EXTRA)` (same four web prefixes), `router.replace("/login")`. The ONE sign-out/401 door.
- `mobile/lib/preparing.js` (91) — module-level `{ descriptor, paywall }` store with `subscribePreparing` (fires immediately), `getPreparing`, `startPreparing`, `clearPreparing`, `failPreparing`, `paywallPreparing`, `clearPaywall`. The phone's stand-in for page.jsx's `preparingCard`/`paywall` state.
- `mobile/components/BottomNav.jsx` (111) — 1:1 port of `.bnav`: four `Item`s (icon + label + clay rule), `showClasses`/`showAdd` props exist but unused by the layout, safe-area bottom padding inside the bar, `accessibilityRole="tablist"`.
- `mobile/components/Bar.jsx` (79) — the top bar: pine fill, `insets.top + 14` / 15 padding, `BAR_CONTENT_H = 64`; `MeyyMark height 22` + "lesson studio"; when `user` (defaults to `getUser()`): ⚙ gear (`disabled={!onSettings}` — no screen passes one, so it is INERT everywhere), raw id (`numberOfLines=1`) over "Log out" → `endSession`.
- `mobile/components/MeyyMark.jsx` (21) — same path data in react-native-svg; `height`, `color`, `dot`, `label` props; width = height×413/110.
- `mobile/theme/ThemeContext.jsx` (22) — `pref` (system|light|dark) stored under `aruvi-theme` via the shared storage shim (survives sign-out), `scheme` = pref or OS, `t` = light/dark tokens.
- `mobile/app/(app)/index.jsx` (456) shell-ish parts — `<Bar user={user} />` (239); `DashHead` greeting (306–335): "Good morning/afternoon/evening, {firstName}!" where firstName = raw id unless numeric (NO display_name fetch); sub-line "Continue where you left off with every class." only when a section is bound; welcome block "Your classes are ready" + one of two lines; 401 → `endSession` (127); foot card (269–286): entitlement status line (`status · N of M trial chapters used` when `enforced`) + Auto/Light/Dark segmented control (`setPref`) — both flagged "moves to Settings in step 6"; empty state copy "No classes yet — set up your teaching profile (first run comes in a later step)." (255).
- `mobile/app/(app)/lessons.jsx` (775) shell-ish parts — ~~`<Bar user={getUser()} />` in all three branches (487, 500)~~ **no bar of its own since Q8 (2026-09-16); the layout draws it**; `subscribePreparing` (143–145) drives the proposed card; PAYWALL `Sheet` (651–660) title "Your free chapters are used up", kicker `{subject} · Class {n}`, sub = server sentence, ONE "Close" button (no Subscribe); toast (664–673) positioned `56.85 + insets.bottom + 16` above the bar; 401 → `endSession` (190).
- `packages/shared/src/readiness.js` (167) — `cachedReadiness()` (sync), `cachedReady()`, `fetchReadiness({force})` (rethrows 401; falls back to stored copy otherwise), `invalidateReadiness()`, `clearReadiness()`; key `aruvi_readiness_{user}`. Phone-only today (the web still fetches `/readiness` directly in page.jsx).
- `packages/shared/src/plans.js` (199) — per subject·class listing store with ETag, `cachedPlans/fetchPlans/readPlans/invalidatePlans/notePlansYear/clearPlans`; key `aruvi_plans_{key}_{user}` (+ `_etag`).
- `packages/shared/src/signout.js` (49) — `TEACHER_CACHE_PREFIXES` + `clearTeacherCaches(extra)` (section cache, history cache, bank, plans memory, readiness memory, `aruvi_user`, prefix sweep).
- `packages/shared/src/config.js` (28) — `API` live binding, `configure`, `getAccessToken`.
- `packages/shared/src/auth.js` (96) — `configureAuth`, `authEnabled`, `accessToken` (sync; falls back to `sb-*-auth-token` in storage), `sendOtp`, `verifyOtp`, `signOutAuth`, `authHeaders`.
- `packages/shared/src/storage.js` (73) — sync `{getItem,setItem,removeItem,keys}` seam, `setStorage`, `webStorage`, `removeByPrefix`.
- `packages/shared/src/ask-aruvi/bank.js` — `loadBank`, `refreshBank` (ETag / If-None-Match), `primeBank`, `clearBank`; keys `aruvi_ask_bank`, `aruvi_ask_bank_etag`.

## Inventory table

Status vocabulary: DONE · PARTIAL · MISSING · DEFERRED · NOT-PORTED-BY-DECISION · WEB-ONLY-BY-NATURE.

### A. Boot, identity gate, theme (layout.jsx · shared-setup.js · page.jsx 49–74, 282, 1069–1082)

| # | Feature / UI element | Web ref (file:lines) | Mobile status | Mobile ref | Depends on | Notes / native answer |
|---|---|---|---|---|---|---|
| 1 | Shared-package boot: storage install, API base, Supabase client | shared-setup.js:19–33 | DONE | mobile/lib/boot.js:18–65 | @aruvi/shared config/storage/auth, expo-sqlite/kv-store, react-native-url-polyfill | Phone adds AppState-driven token auto-refresh (boot.js:69–74) — a phone capability, named in header. Web target uses `webStorage()`. |
| 2 | `<html lang="en">`, `metadata.title` "Meyy", description | layout.jsx:3–6, 65–73 | WEB-ONLY-BY-NATURE | app.json (not in scope) | — | App name/icon are Expo config. |
| 3 | `viewport-fit=cover` + `themeColor` pine per scheme (`#164436` light / `#14332a` dark) | layout.jsx:8–24 | DONE | mobile/app/_layout.jsx:21 | `t.bar_fill` token | `StatusBar style="light" backgroundColor={t.bar_fill}`; bar padding uses `insets.top` (Bar.jsx:50). |
| 4 | No-flash theme resolver: `aruvi-theme` pref (system/light/dark) → `data-theme`/`data-theme-effective`; `window.__aruviTheme` API; OS-change listener | layout.jsx:30–63 | DONE | mobile/theme/ThemeContext.jsx:11–22 | shared `storage`, `theme/tokens.js` | Same key `aruvi-theme`, same three-way; survives sign-out on both. `useColorScheme()` replaces the media query. |
| 5 | `history.scrollRestoration = 'manual'` (open at top on relaunch) | layout.jsx:35 | WEB-ONLY-BY-NATURE | — | — | Native screens have no restored document scroll. |
| 6 | `user === null` → render nothing (no login flash); `!user` → `<Login>` | page.jsx:51, 282, 1069–1071 | DONE | mobile/app/index.jsx:6–8; (app)/_layout.jsx:19 | shared `getUser()` (sync) | Redirect instead of conditional render; `(app)/_layout` re-checks so a deep link cannot land inside. |
| 7 | `onEnter(id)`: `setUser`, `primeBank()` | page.jsx:632–638 | DONE | mobile/app/login.jsx:73–78 | shared format.setUser, bank.primeBank | Phone also writes `SEEN_KEY` and `router.replace("/(app)")`. |
| 8 | Fonts (Google Fonts `@import` in globals.css) | globals.css (web) | DONE | mobile/app/_layout.jsx:28–30, theme/fonts.js | expo-font, splash hold | Bundled static cuts; the variable Fraunces reads slightly heavier on phone (known, CLAUDE.md §0). |
| 9 | `html.app-shell` scroll lock: document never scrolls, `.bodycontent` is the one scroller, top bar static flow | page.jsx:284–293; globals.css:199–204 | WEB-ONLY-BY-NATURE | each route's `ScrollView` | — | Native: every screen is `View{flex:1}` › `Bar` › `ScrollView`; the equivalent already holds. |
| 10 | CSS var publisher `--nav-h`, `--hdr-h`, `--bnav-h` (ResizeObserver + resize; `--bnav-h` = 0 when the bar is absent) | page.jsx:295–333 | WEB-ONLY-BY-NATURE — **native answer DONE 2026-09-16** | Bar.jsx `BAR_CONTENT_H = 64`; **BottomNav.jsx `BNAV_H = 56.85`**, read by the My Lessons toast | theme/web.js `bnav*` | Native layout needs no measured vars. The literal `56.85` in lessons.jsx is gone: both numbers are now published from the component that owns them, so Ask Meyy's panel (row 78) gets the same one and a re-measure lands in one place. |

### B. Readiness rehydrate, activation gate, first-run gate (page.jsx 335–480, 482–504, 1072–1082)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes / native answer |
|---|---|---|---|---|---|---|
| 11 | Rehydrate on user change: clear `ready/readiness/readinessLoaded`, GET `/readiness` → `projectReadiness(d.readiness)`, `ready = true` | page.jsx:339–355 | **DONE on the phone** — web owed | shared readiness.js:113–142 (`fetchReadiness`); index.jsx:109–137; lessons.jsx:180–194 | `GET /readiness`, `aruvi_readiness_{user}` key | Phone fetches per ROUTE via the shared store (memory + device copy, one revalidation per session). No `projectReadiness` projection on the phone (screens read `subjects[]` directly — fine, the projection is legacy). Web should adopt the shared store too (CLAUDE.md notes it is phone-only today). ★ **2026-09-18 reconcile:** the phone reads the shared store everywhere — `(app)/_layout.jsx` calls `fetchReadiness()` once and subscribes (`subscribeReadiness`), and every screen re-renders from it. ⚠️ The WEB still GETs `/readiness` itself (page.jsx:426, 1548); adopting the store there is web-side tidying, not a phone gap. |
| 12 | 401 on `/readiness` → `onSignOut()` (server refused the session; never first-run) | page.jsx:347–353 | DONE | index.jsx:127; lessons.jsx:190; readiness.js:131–135 rethrows 401 | lib/session.js `endSession` | Only a 401 ends the session; unreachable → message ("Couldn't reach Meyy right now."). Same rule on both. |
| 13 | `readinessLoaded` gate → render nothing until GET resolves (no first-run flash for a set-up teacher) | page.jsx:59, 354, 1072–1074 | **DONE** (2026-09-18) | index.jsx:250 "Loading your classes…" spinner only when nothing cached | `cachedReadiness()` | Phone paints from the device copy first; a first-ever load shows a spinner. Equivalent intent; no FirstRun to flash yet. ★ **2026-09-18 reconcile:** `(app)/_layout.jsx` holds a bare paper screen (`!resolved && !ready`) until `/readiness` answers when nothing is cached — no shell flash for a teacher about to be sent to first run. |
| 14 | Activation gate: `!ready || firstGenNeeded` → `<FirstRun user onComplete onPrepared onPrepareError onSignOut />` (shell-less) | page.jsx:1075–1082 | **DONE** (2026-09-18) | index.jsx:255 prints "No classes yet — set up your teaching profile (first run comes in a later step)." | FirstRun port (separate family), `cachedReady()`, `firstGenNeeded` heuristic (row 15) | The phone has no gate at all: a brand-new teacher lands in an empty My Classes. Needs a route-level decision in `(app)/_layout.jsx` or `app/index.jsx` (`ready` from `cachedReady()`/`fetchReadiness`) → `/first-run` route. ★ **2026-09-18 reconcile:** the gate is in `(app)/_layout.jsx` (`<Redirect href="/first-run"/>` on `(resolved && !ready) || needFirstGen`), re-asked whenever her profile changes; first run is `app/first-run.jsx`. |
| 15 | `firstGenNeeded` heuristic: ready AND nothing prepared AND nothing bound AND no prior year → first run; latched per user by `everGeneratedRef`/`latchUserRef`; unknown → never force | page.jsx:392–439 | **DONE** (2026-09-18) | — | GET `/plans-prepared`, GET `/section-state`, GET `/academic-year` | Direct subscribers (checkout-created profile) need this on the phone too. Module-level latch keyed by user (the `preparing.js` pattern). ★ **2026-09-18 reconcile:** `lib/firstRun.js` `firstGenNeeded()` + the per-session `hasActivated()` latch that stops the bounce back to the welcome screen while the first serve is in flight. |
| 16 | `onFirstRunComplete(payload, preparing)`: latch, MERGE subject records with checkout-created base, `setReady`, scope `subject/grade`, `goLessons()`, seed `preparingCard`, `verifyReadiness(merged)` | page.jsx:441–480 | **DONE** (2026-09-18) | — | FirstRun port, `startPreparing` (preparing.js), `verifiedWrite` + `readinessFingerprint` (shared verify.js), `invalidateReadiness()` | The merge rule (same-name replaced, others kept) must be reproduced verbatim. ★ **2026-09-18 reconcile:** `app/first-run.jsx`:295-315 — merges with the checkout-created base (same-name replaced), `markGenerated()`, `adoptReadiness` optimistically then `saveReadiness`, `startPreparing(descriptor)`, lands on My Lessons. |
| 17 | `verifyReadiness(subs)`: read-after-write POST `/readiness` then GET; fingerprint mismatch → re-sync `readiness`/`ready = real.length > 0`, `saveFailed = true` | page.jsx:364–390 | **DONE** (2026-09-18) | shared verify.js:35, 102 exist | `POST /readiness`, `GET /readiness` | Only needed once FirstRun / legacy Readiness write from the phone. TeachingProfile family has its own copy of this doctrine. ★ **2026-09-18 reconcile:** shared `saveReadiness` (`@aruvi/shared/readiness`) is the verified write — POST, read back, `readinessFingerprint` — and adopts the server copy on mismatch. First run and `ProfileEditor` both call it. |
| 18 | `saveFailed` caption bar: "Your teaching set-up didn’t save — this is what Meyy has for you." + "Dismiss" (`.tp-savefail`, role=alert) | page.jsx:370, 1257–1262; globals.css:4991–5004 | **DONE — as a NAMED DIVERGENCE** | `ProfileEditor.jsx:658` (in the window the write happened in); `web.js` `tp_savefail*` | row 17 | ★ **The phone says it WHERE SHE IS, and keeps her there.** The web puts this in the shell because its profile edit and its shell share a scroller; the phone's edit is a WINDOW, and `saveReadiness` has already adopted the server's copy, so the editor re-seeds its field from it and holds her on the number the sentence is about. Its own comment: *"sending her back with a banner would put the message on one screen and the wrong number on another"*. A second bar in the notices slot would be the same sentence twice — deliberately NOT built (2026-09-16). |
| 19 | `onReadyComplete(payload)` (legacy upfront `Readiness` path via MyPlans `onReady`) | page.jsx:497–504 | NOT-PORTED-BY-DECISION | — | Readiness.jsx is a dead entry (CLAUDE.md §0) | Still wired on web only because MyPlans keeps the prop. |
| 20 | GET `/subjects` → `subjects`, default `subject` ("science" if present); GET `/subjects/{s}/grades` → sorted `grades`, keep valid `grade` else "vii"/first | page.jsx:482–495 | **DONE** — named structural difference | prepare.jsx (route params carry subject/grade); lessons.jsx wheels from readiness | `/subjects`, `/subjects/{s}/grades` | On the phone scope travels as route params, not shell state — a named structural difference, no user-visible gap. "Connecting to the Meyy engine…" (`.empty`, page.jsx:1326) has no phone equivalent; not needed. ★ **2026-09-18 reconcile:** nothing to build: scope travels as route params on the phone, and the "Connecting…" empty state belongs to the dead Generate tab. |

### C. Sign-out, erase, caches (page.jsx 639–675)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes / native answer |
|---|---|---|---|---|---|---|
| 21 | `onSignOut`: `clearUser`, `signOutAuth`, `clearTeacherCaches(["setup_check_pending_","mylessons_subject_","mylessons_class_","allocations_"])`, reset shell state, back to Login | page.jsx:658–675 | DONE | mobile/lib/session.js:21–25 | shared signout.js, auth.signOutAuth | Same four extra prefixes. Phone `router.replace("/login")`. |
| 22 | "Log out" button on the bar (`.hdr-user-logout`) | page.jsx:1217; globals.css:253–254 | DONE | Bar.jsx:69–72 | theme/web.js `hdr_user_logout` | — |
| 23 | `onErased`: sweep device immediately, keep `user` so the farewell card renders, arm `erased` so ✕ also signs out | page.jsx:639–656, 948–951 | **DONE** (2026-09-18) | — | Settings family (Your data & export), `endSession` | Phone Settings does not exist yet; when built, the farewell + both exits must sign out. ★ **2026-09-18 reconcile:** `settings/index.jsx` `erase()` — the device is swept (`clearSession`) the moment the receipt arrives and the farewell stays on screen; with nothing left signed in there is no ✕ to re-arm. Named divergence, same outcome. |
| 24 | Ask Meyy bank: `refreshBank()` on every signed-in app load (ETag 304 path) | page.jsx:105–114 | **DONE 2026-09-16** | `(app)/_layout.jsx` on mount | shared bank.refreshBank, GET `/ask-aruvi` | Built as recommended, mount only — an ETag request, so an unchanged bank costs a 304. The optional AppState pass was left out: a bank changes on a deploy, not on a foregrounding. Sign-out clears via `clearTeacherCaches`. |

### D. Top bar / header (page.jsx 1179–1248; MeyyMark.jsx)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes / native answer |
|---|---|---|---|---|---|---|
| 25 | `.topbar` pine bar, fixed pre-shell / static in shell, safe-area top padding; `.topbar-spacer` | page.jsx:1197, 1246–1248; globals.css:139–186 | DONE | Bar.jsx:50 (`paddingTop: insets.top + 14`, `paddingBottom: 15`); **drawn ONCE, in `(app)/_layout.jsx`** | react-native-safe-area-context; theme/web.js `hdr` | ★ **Q8 answered 2026-09-16 — one bar, in the shell.** Was eleven per-route copies of a strip that never changes; five of them passed no `user` and shed the bar's right-hand half (the 2026-09-14 report). Shell-less screens (login · privacy · first run) are outside `(app)` and keep their own. |
| 26 | Brand lockup `.brand`: `<MeyyMark/>` over `.hdr-brand-tag` "lesson studio" | page.jsx:1203–1206; MeyyMark.jsx:21–52; globals.css:225–244, 2307 | DONE | Bar.jsx:53–56; MeyyMark.jsx | react-native-svg; `hdr_brand`, `hdr_brand_tag` | Stacked column, mark 22px, dot `#e0705f` on the bar, ™ ring included. Web sizes mark 22px / 19px ≤600px — phone uses 22 (phone width is the ≤600 case → should be 19; verify on the parity page). ★ **2026-09-18 reconcile:** ⚠️ **still 22px on the phone**; the web is 19px at ≤600px (globals.css:225). Owed: `Bar.jsx:88` → 19, then re-check `BAR_CONTENT_H` on the parity page. Not changed blind because the bar height feeds Ask Meyy's top and the tour's measurements. |
| 27 | Settings gear `⚙` (`.hdr-gear`, aria "Settings", `data-tour="settings-gear"`) → `goSettings` | page.jsx:1212–1213; globals.css:537–538 | **DONE** (2026-09-18) | Bar.jsx:63–66 (`disabled={!onSettings}`; NO caller passes `onSettings`) | Settings family (step 6); `hdr_gear` | Rendered inert on purpose ("the bar must not change shape"). Wire `onSettings` → `router.push("/settings")` once Settings exists; Bar's default should call it itself (like `user` defaults to `getUser()`), so the five call sites need no edit. ★ **2026-09-18 reconcile:** `(app)/_layout.jsx` passes `onSettings={() => router.push("/settings")}` — live since 6b·A. |
| 28 | Identity block `.hdr-user-id`: `displayName || user` (`.hdr-user-name`) over Log out | page.jsx:1215–1218; globals.css:251–254 | **DONE** (2026-09-18) | Bar.jsx:67–73 shows raw `user` id only | row 29 | Name never shown on the phone. ★ **2026-09-18 reconcile:** `Bar.jsx`:23, 79 reads `accountFirstName(fetchAccount())`, the id only as fallback. |
| 29 | `displayName` effect: GET `/account` → first word of `display_name`, capitalised, "" when numeric; re-fetched on `entSyncTick` (subscribe / profile save) | page.jsx:737–759 | **DONE** (2026-09-18) | index.jsx:310–311 derives `firstName` from the raw id (non-numeric id only) | `GET /account`; a module-level account store (readiness.js pattern) shared by Bar and DashHead | Also feeds the greeting ("Good morning, Priya!") and `tourSpent`. Suggest `packages/shared/src/account.js` with `cachedAccount/fetchAccount/invalidateAccount`. ★ **2026-09-18 reconcile:** `@aruvi/shared/account.js` (`cachedAccount` / `fetchAccount` / `accountFirstName` / `invalidateAccount`); Bar and the My Classes greeting (`index.jsx`:709-720) both read it. |
| 30 | Frozen Settings bar `.set-bar` (`nav.tabs.main-tabs.set-bar`): `⚙` + `settingsBarLabel` left, `✕` right (aria "Close {label}"); shown when `editFlow==="settings"` or profile-via-settings | page.jsx:971–980, 1227–1245; globals.css:4362–4373 | **DONE** (2026-09-18) | — | Settings family; `set_bar_title` (display 19px 600), `set_bar_gear` (20px ink-soft), `set_bar_x` (15px) in theme/web.js | Labels: "Settings" · "Personal profile" · "Subscription & billing" · "Your data & export" · "Support" · "About Meyy" · "Legal" · "Teaching profile". No hairline under the bar. ★ **2026-09-18 reconcile:** `components/SettingsBar.jsx` + `settingsLabel(pathname)`, drawn once in `(app)/_layout.jsx` under the brand bar, no hairline. |
| 31 | `settingsClose` semantics: erased → sign out; profile-via-settings → back to list; subview → list (`settingsView="home"`, `legalDoc="agreement"`); list → origin tab (`settingsOriginRef`: lessonplans / profile / classes) | page.jsx:940–970 | **DONE** (2026-09-18) | — | Settings family; expo-router stack | Native answer: a `/settings` stack whose header ✕ pops one level; origin restored by `router.back()` from the entry point (the stack remembers it). ★ **2026-09-18 reconcile:** ✕ is `router.back()` on every level — the stack remembers the origin that `settingsOriginRef` holds on the web. Named divergence. |
| 32 | `goSettings` captures origin (`editFlow`) unless already inside Settings; resets `settingsView`/`legalDoc` | page.jsx:903–907 | **DONE** (2026-09-18) | — | Settings family | `router.push("/settings")` from the gear. ★ **2026-09-18 reconcile:** the gear `push`es `/settings`; the stack is the origin. |
| 33 | `openProfileFromSettings` / `openFullProfile` (profile under the Settings bar, `profileViaSettings=true`) | page.jsx:936–939, 1059–1062 | **DONE** (2026-09-18) | — | TeachingProfile family, Settings family | — ★ **2026-09-18 reconcile:** Settings home card → `/settings/profile`; the portal footer (row 69) → `router.navigate("/settings/profile")`. |
| 34 | `inSettingsBar` hides the ordinary tab row — SUPERSEDED: since 2026-09-13 the top bar has only the brand row; the bottom nav is up on every screen incl. Settings | page.jsx:971, 1222–1226 | DONE (as designed) | (app)/_layout.jsx:29–39 | — | Phone's BottomNav already sits at the layout level under every route. |

### E. Bottom nav `.bnav` (page.jsx 1346–1422; globals.css 4868–4881)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes / native answer |
|---|---|---|---|---|---|---|
| 35 | Bar skin: sunk paper, 1px `--edge` top rule, contents capped to `--shell-w`, safe-area bottom padding INSIDE the bar | page.jsx:1371–1372; globals.css:4868–4872 | DONE | BottomNav.jsx:36–38, theme/web.js `bnav`, `bnav_in` | — | Measured 390×844: 56.85 tall. |
| 36 | Item "My Classes" (calendar-page glyph), `data-tour="nav-classes"`, hidden when `entLapsed` | page.jsx:1375–1382 | **DONE** (2026-09-18) | BottomNav.jsx:30–36, 49–51 (`showClasses` prop, never passed) | row 60 (lapsed) | Glyph, label, active colour DONE; lapsed hide MISSING. ★ **2026-09-18 reconcile:** `showClasses={!ent.lapsed}` is passed (`(app)/_layout.jsx`). |
| 37 | Item "My Lessons" (document glyph), `data-tour="nav-lessons"` | page.jsx:1383–1388 | DONE | BottomNav.jsx:38–44, 52 | — | — |
| 38 | Item "Add" (ringed plus + four dots), aria "Add or change subjects, classes, or sections", title "Add or change what you teach", `data-tour="grow-add"`; shown when `ready && !entLapsed`; opens `portalWin {mode:"change"}` (and closes Ask) | page.jsx:1395–1409 | **DONE** (2026-09-18) | BottomNav.jsx:46–57, 55–58 rendered; (app)/_layout.jsx:37 `onAdd={() => {}}` | ProfilePortal port (rows 65–75), lapsed | Glyph/label DONE, action MISSING. ★ **2026-09-18 reconcile:** `onAdd` raises `{mode:"change"}` and closes Ask; `showAdd={ready && !ent.lapsed}`. |
| 39 | Item "Ask Meyy" (stream-a mark, red dot `#c0392b`), aria/title "Ask Meyy", `data-tour="ask-aruvi"`, TOGGLES `askOpen`; active (clay) while the panel is open, and the two destinations lose the clay then | page.jsx:1410–1420, 1376, 1383 | **DONE** (2026-09-18) | BottomNav.jsx:59–67, 59; `onAsk={() => {}}` | AskAruvi port (separate family) | Glyph DONE, action MISSING; the "active while open, destinations un-lit" rule needs the layout to own `askOpen`. ★ **2026-09-18 reconcile:** `onAsk={toggleAsk}`; `active === "ask"` lights the clay while the panel is open, and the destinations close it. |
| 40 | `activeNav`: "lessons" when repository open; "none" in profile/settings; else "classes" (Generate, a lesson opened from cards) | page.jsx:1063–1066 | DONE | (app)/_layout.jsx:24 (`pathname.startsWith("/lessons")`) | — | `/lesson` (singular) does not start with `/lessons`, so a lesson opened from My Lessons lights My Classes — SAME as the web's rule ("a lesson opened from them reads as My Classes"). Settings must map to `null`. |
| 41 | Nav actions close Ask Meyy and clear Generate entry / pane intent (`goClasses`, `goLessons`) | page.jsx:686–688, 1377, 1384 | DONE (navigate, not push) | (app)/_layout.jsx:35–36 | — | Phone uses `router.navigate` so a crossing returns to the existing instance (founder, 2026-09-14). |
| 42 | Tour anchors on all four items (`data-tour`, `place:"above"`) | page.jsx:1378, 1385, 1396, 1413 | **DONE** (2026-09-18) | — | GuidedTour (deferred) | — ★ **2026-09-18 reconcile:** `BottomNav.jsx` registers `nav-classes` / `nav-lessons` / `grow-add` / `ask-aruvi` through `useTourAnchor` (8b). |

### F. Notices and toasts inside `main` (page.jsx 1250–1281)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes / native answer |
|---|---|---|---|---|---|---|
| 43 | `sectionFailed` bar: "That didn’t save — your classes are as Meyy has them." + "Dismiss" (`.tp-savefail`, role=alert); set by `setSectionMismatchHandler` after `pullSectionState([key])` | page.jsx:267–279, 1263–1268 | **DONE 2026-09-16** | `(app)/_layout.jsx` installs the handler; `components/Notices.jsx` `SectionFailedBar` | shared sectionState.js `setSectionMismatchHandler`; `tp_savefail` styles | Built as recommended — the layout-level slot, PINNED between the bar and the Stack rather than inside a scroller, because on the phone every screen owns its own and a notice inside all four would be four notices. ★ Until this, the phone had installed NO handler: a verified server disagreement about the chapter she just attached was **silent**. Re-pull first, then speak — a caption for a corrected screen. |
| 44 | Privacy-notice bar `.pn-note` (role=status): "Meyy’s Privacy Notice has been updated (version {n})." + "Read it" (pine) + "Dismiss"; GET `/legal/privacy/status` once per sign-in; hidden inside Settings › Legal; both actions POST `/legal/privacy/seen` with context `updated_note_read` / `updated_note_dismissed` | page.jsx:909–935, 1269–1281; globals.css:5296–5306 | **DONE 2026-09-16** | `(app)/_layout.jsx` (state + both stamps); `components/Notices.jsx` `PrivacyNoteBar`; new `web.js` keys `pn_note/_t/_acts/_btn/_bt` measured off globals.css | `/legal/privacy/status`, `/legal/privacy/seen`; `/privacy` | ⚠️ **"Read it" goes to `/privacy` for now**, the standalone screen, which loads with or without an identity; at 6b it becomes Settings › Legal on the notice, the web's own destination. The stamp and its two contexts are unchanged. ⚠️ **No "hide inside Legal" case, and none is owed**: the web needs one because its notice is a VIEW in the same shell, where the phone's `/privacy` is a route outside `(app)` — this layout is not mounted while she reads it, and "Read it" stamps the version on the way out anyway. |
| 45 | Preparing card state `preparingCard` held ABOVE the tab; `onPreparing(desc)` returns true only when taken (attach path keeps in-place wait); `onPrepareError` marks `failed:true, message`; `onDismissPrepareError`; `onPrepared` clears it | page.jsx:567–601, 554–565 | DONE | mobile/lib/preparing.js (whole file); prepare.jsx:300–336; lessons.jsx:143–145, 619–624 | — | Named divergence: module store instead of shell state. The attach-path exception (`prepareReturn`) has no phone counterpart because the phone's picker has no "prepare a new one" footer yet (plan §2 5a). |
| 46 | `onPrepared` routing: from a section's attach popup → `pendingAttach` + land on My Classes (MyPlans binds it); from My Lessons → `editFlow="lessonplans"` | page.jsx:548–565 | **DONE** (2026-09-18) | prepare.jsx:301 always `router.navigate("/lessons")` | picker "prepare a new one" footer (deferred) | Deposit-only path DONE; auto-attach return path MISSING (depends on that footer). ★ **2026-09-18 reconcile:** the phone binds DIRECTLY — `prepare.jsx` carries `section`/`tag` from the card's picker, calls `bindSectionChapter` when the serve lands and returns to My Classes (`navigate(fromSection ? "/" : "/lessons")`). No `pendingAttach` hand-off is needed. |
| 47 | Paywall modal `.paywall-bg/.paywall-card`: kicker read off the message ("Free trial ends" / "Separate subscription" / "Subscription ended"), `paywall-msg` = server sentence, "Subscribe" (pine, opens SubscribeFlow), "Not now" (italic); tap outside closes; `onPaywall` pulls the proposed card | page.jsx:603–613, 1468–1489; globals.css:3976–3989 | **DONE 2026-09-16 (Q6)** | lessons.jsx — centred card, derived kicker, Subscribe + Not now | preparing.js `paywall`; `format.paywallKicker`; SubscribeFlow still NOT ported | The phone reads the kicker off the sentence through the SHARED `paywallKicker`, and **the web delegates to it too** — it had been spelled in page.jsx's JSX alone, which is exactly why the phone shipped without it. Both buttons are drawn: Subscribe raises "The subscription page is still in development. It will open from Settings." as a second BODY of the same Sheet (never a second Modal, the `dac26eb0` lesson), and "Not now" closes. ★ The SHAPE is what is preserved, not the wiring — a teacher who learns this window today must not meet a differently-shaped one the week Subscribe starts working. New `web.js` keys `paywall_body/msg/sub/sub_t/later/soon`, measured off globals.css 3987-4000; the subject·class kicker is gone, because the web has never had one and the sentence names the subject where it matters. |
| 48 | SubscribeFlow overlay `.subflow-overlay` (z 70, full paper) with `onDone` (close, bump `entSyncTick`, re-GET `/readiness`) / `onCancel` | page.jsx:614–619, 1449–1467; globals.css:4156 | **DONE** (2026-09-18) | — | — | Beta on manual grants. ★ **2026-09-18 reconcile:** Q11 un-deferred it: `(app)/subscribe.jsx` (in-app door) and `app/front-subscribe.jsx` over `components/SubscribeWizard.jsx`. |
| 49 | Toasts in My Lessons (`.mlp2-toast`) positioned above `--bnav-h` | (MyLessonPlans family) | DONE | lessons.jsx:664–673 | `mlp2_toast` | Listed here only because of the bar-height dependency (row 10). |

### G. Entitlement, lapsed, trial, academic year (page.jsx 136–142, 837–895, 761–835)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes / native answer |
|---|---|---|---|---|---|---|
| 50 | Entitlement sync: `fetchEntitlement()` on ready/user, on `visibilitychange` visible, on `focus`, every 20 s while visible; re-run on `entSyncTick` | page.jsx:845–886 | **DONE 2026-09-16** | `(app)/_layout.jsx` (mount · AppState active · 20 s while active); `@aruvi/shared/entitlement` | shared `entitlement.js` store; AppState | Built as recommended. ⚠️ Two divergences, both deliberate and both written into the store's header: it keeps a **device copy** (the web needs none — its tab stays open; a phone cold-starts and would flash My Classes at a lapsed teacher), and `entSyncTick` has no phone equivalent, so a **status-or-scope change invalidates `/account`** instead — the poll is the phone's notice that a name was captured. The timer stops in the background. |
| 51 | `entLapsed` = `e.lapsed` (fallback `enforced && status==="expired"`) | page.jsx:854–859 | **DONE** | `format.js` `entLapsed` (shared since 2026-09-15); read by the store | row 50 | — |
| 52 | `entTrial` = `status==="trial" || plan_id==="trial"` (feeds Settings card visibility) | page.jsx:860–866 | **DONE** | `entitlement.js` `entTrial` (lifted; the web still spells it inline — one to collect when page.jsx adopts the stores) | row 50, Settings family | Status, not `enforced` — the 2026-09-11 web fix, carried across rather than re-learnt. |
| 53 | `paidScopes` = `live_scopes` (or `scopes`) when enforced & active/grace, else null → TeachingProfile choosers | page.jsx:867–868 | **DONE** | `entitlementState().paidScopes` | row 50, TeachingProfile family | ⚠️ **first-run.jsx held a THIRD hand-rolled spelling of this that read `scopes`, not `live_scopes`** — found and deleted 2026-09-16. It calls `paidScopesOf` like everyone else now. |
| 54 | Lapsed → forced onto My Lessons (`goLessons()` when standing on My Classes) | page.jsx:888–895 | **DONE** | `(app)/_layout.jsx` — effect on `ent.lapsed`/`pathname` | row 51 | Built as recommended, `navigate` not `push` — she is being moved, not taken somewhere she can come back from. It also `clearPortal()`s, so a window opened a second before a revoke does not stay up over a shell that has just decided she may not edit. |
| 55 | Lapsed hides "My Classes" and "Add" in the bar; ProfilePortal never renders; profile pen locked | page.jsx:1375, 1395, 1438 | **DONE 2026-09-16** | `(app)/_layout.jsx` (both props + the Sheet's render gate); `lessons.jsx` (`onEditBudget` passed null) | row 51 | The two props had existed unpassed since the nav was ported. The pen goes dark by receiving NO handler — the gear's idiom; `YearPlan` already drew it only when it had somewhere to send her. |
| 56 | `yearInfo` GET `/academic-year` on ready; re-read on focus / visibilitychange; passed to MyPlans (cutover offer) and MyLessonPlans (prior-year folders) | page.jsx:768–805 | **DONE 2026-09-16** (the offer half) | `@aruvi/shared/year`; `(app)/_layout.jsx` rides the entitlement's cadence; My Classes re-reads on focus | `GET /academic-year`; AppState | The prior-year FOLDERS in My Lessons are still lessons.jsx's deferred item 3 — a different row of a different family, unaffected by this. |
| 57 | `runCutover`: POST `/academic-year/cutover {confirm:true}` → `clearLocalSectionCache()`, `clearLocalHistoryCache()`, latch first-gen, `pullSectionState()`, re-GET year; `cutoverBusy`, `cutoverResult`, `cutoverDismissed` (per-user reset) | page.jsx:807–835, 773–788 | **DONE 2026-09-16** | `@aruvi/shared/year` `runCutover` (10 tests); `components/YearNudge.jsx` | row 56; My Classes | `invalidatePlans()` + `notePlansYear` are in, as the note asked; the first-gen latch is `markGenerated()`. The per-user reset is `forUser()` at the top of every read and write — sign-out is not a remount on a phone either. ⚠️ **Q17: the dismissal is NOT persisted** (once per launch, the web's rule). ⚠️ A DEFECT its own test caught: `busy` cleared in a `.finally()` meant every branch RETURNED a snapshot still claiming busy, while subscribers saw the right value — which is what would have hidden it. |
| 58 | Per-user reset of session-only flags (`cutoverDismissed`, `cutoverResult`, `tourOfferedThisSession`) whenever `user` changes | page.jsx:783–788 | WEB-ONLY-BY-NATURE | — | — | Phone routes remount on sign-in; module stores must still be cleared in `endSession` (plans/readiness already are). |

### H. Tour state and offer (page.jsx 76–208, 261–265, 690–724, 1424–1428)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes / native answer |
|---|---|---|---|---|---|---|
| 59 | `tourEligible`: GET `/section-state` → ≤1 bound AND no `done`/`unit_index` anywhere; null on failure | page.jsx:116–131 | **DONE** (2026-09-18) | — | GuidedTour deferred (BRIEF) | — ★ **2026-09-18 reconcile:** `lib/tour.js` `fetchTourEligible`. |
| 60 | `tourSpent` from `GET /account` `tour_offered_at` (not while offered this session); `tourOnOffer = eligible && !spent && !dismissed && !lapsed`; POST `/account/tour-offered` the moment it is offered | page.jsx:146–167, 752–756 | **DONE** (2026-09-18) | — | row 29 (`/account` store) | Even with the tour deferred, the phone should NOT show the "Show me how" nudge; it does not, so nothing leaks. ★ **2026-09-18 reconcile:** `lib/tour.js` `spendTourOffer` / `offeredHere` / `tourOfferOpen`; `tour_offered_at` read through the account store. |
| 61 | Tour offer nudge `.dash-nudge.dash-nudge-click` "Let me show you around first" / "A short walk through tracking sections and handling lesson plans. Your lesson stays safe in My Lessons — you can add it to a class whenever you’re ready." / "Show me how →" on BOTH My Classes and My Lessons (`onStartTour` passed only while on offer) | MyPlans.jsx:1005–1030; MyLessonPlans.jsx:1082–1090; page.jsx:1290, 1336 | **DONE** (2026-09-18) | — | GuidedTour | Copy recorded for the eventual port. ★ **2026-09-18 reconcile:** `components/TourOffer.jsx`, rendered on BOTH My Classes (`index.jsx`:612) and My Lessons (`lessons.jsx`:864). |
| 62 | `startTour` (goClasses then step 1); `finishTour` (close Ask, clear, session-dismiss, goClasses, open check window); `tourNext`/`tourBack` shell moves: 2→3 lessons · 7→8 classes · 15→16 classes · 16→17 profile · 17→18 classes · 18→19 open Ask · 19→20 close Ask · 20 Done → finish; Back mirrors (1→null, 3→classes, 8→lessons, 17→classes, 18→profile, 19→close Ask, 20→open Ask) | page.jsx:204–208, 265, 700–724 | **DONE** (2026-09-18) | — | GuidedTour | — ★ **2026-09-18 reconcile:** `lib/tour.js` `startTour` / `tourNext` / `tourBack` / `tourSkip` / `endTour` with the `MOVES` / `BACK_MOVES` route maps (the orchestration moved OUT of the screens on 2026-09-17 — lesson 3 of the hand-off). ⚠️ `endTour` does NOT open the check window, by decision — see row 74. |
| 63 | `sectionCheck` prop → MyPlans re-binds the tour's target card on ANY exit (idempotent) | page.jsx:1342; MyPlans.jsx:170–188 | **DONE** (2026-09-18) | — | GuidedTour | — ★ **2026-09-18 reconcile:** `endTour` → `restoreBinding()` gives back what the demo borrowed (`lib/tour.js`:355). |
| 64 | `tourInfo` `{tag, chapter}` reported up by MyPlans for step copy; `tourActive`/`tourStep` props into MyPlans, MyLessonPlans; `autoFocus={tour == null}` on AskAruvi | page.jsx:92, 1289–1290, 1337–1338, 1448 | **DONE** (2026-09-18) | — | GuidedTour | — ★ **2026-09-18 reconcile:** `noteTourInfo` / `useTour()`; there is no autofocus on Ask Meyy on ANY phone (AskMeyy.jsx:29), so the tour-case guard is moot. |

### I. ProfilePortal — the ONE window, its queue, and its routing (ProfilePortal.jsx; page.jsx 197–260, 986–1062, 1084–1177, 1430–1443)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes / native answer |
|---|---|---|---|---|---|---|
| 65 | Window chrome: `.ap-overlay` (tap outside closes) › `.ap-modal.ap-grow` › `.ap-close` "✕" (aria "Close") › `.ap-head` (`.ap-kicker` "Your teaching", `.ap-title`, `.ap-sub`) › `.ap-list` › `.ap-foot` | ProfilePortal.jsx:80–116; globals.css:2681–2801 | **DONE** (verified against the phone 2026-09-17) | `AttachSheet.jsx` `Sheet`:56–133; `theme/web.js` ap_*:230–341 | theme/web.js: add `ap_grow`, `ap_row_line`, `ap_row_label`, `ap_row_val`, `ap_foot`, `ap_foot_go`, `ch_go` | ★ **ONE Sheet, whose children swap** — the LAYOUT owns it and `ProfilePortal`/`ProfileEditor` are body-only, which is what removed the two-fade flash between the window and an edit (`dac26eb0`). `ap_grow` became `ap_modal_tall` + `ap_grow_list`. |
| 66 | Title by mood: check → "Would you like to check your set-up?"; change → "What would you like to change?" | ProfilePortal.jsx:85–87 | **DONE** (verified against the phone 2026-09-17) | `ProfilePortal.jsx:52–56` (`portalChrome`) | — | Both mood titles verbatim; the layout passes them to the Sheet (`_layout.jsx:367`). |
| 67 | Sub-line by mood: check → caller `sub` or "Meyy started you off with its own suggested set-up. You can change any of it — or leave it and carry on teaching."; change → "Each item changes only itself — pick another for the next. Your lessons always stay in the library." | ProfilePortal.jsx:88–94 | **DONE** (verified against the phone 2026-09-17) | `ProfilePortal.jsx:57–59` | — | Both sub-lines word for word; in check mood the caller's `sub` overrides — `SetupCheckSub` (`_layout.jsx:274`), whose PARTS come from shared (row 75). |
| 68 | Rows (`ROWS`, `.ap-row.ap-row-line`): "Class" · "Section" · "Periods a week" · "Annual period budget", each label + optional right-aligned `.ap-row-val` (check mood only) + `›` chevron (`.ch-go`); `onPick(kind)` with kind ∈ class/section/ppw/budget | ProfilePortal.jsx:56–61, 96–104; globals.css:2763–2785 | **DONE** (verified against the phone 2026-09-17) | `ProfilePortal.jsx:42–47, 107–121` | TeachingProfile port's `portalIntent` screens | NO Subject row in either mood (founder). `ap_row_val` is gated to check mood, as on the web. |
| 69 | Footer `.ap-foot` "Want to see your full teaching profile?" + `›` → `onOpenProfile` (profile under Settings) | ProfilePortal.jsx:107–110; globals.css:2791–2801 | **DONE** (2026-09-18) | `ProfilePortal.jsx:127–131` | Settings + TeachingProfile families | ⚠️ **THE ROW DRAWS AND GOES NOWHERE.** `onOpenProfile` is still the no-op stub `_layout.jsx:416` left when the accordion did not exist — and **Settings › Teaching profile HAS landed since (6d)**, so this is a dead control on a live screen, and a one-line wiring to fix. Found in the 2026-09-17 audit. ★ **2026-09-18 reconcile:** wired — `onOpenProfile` closes the window then `router.navigate("/settings/profile")` (`(app)/_layout.jsx`). Closing first is a named divergence (a Modal floats above every route). |
| 70 | No closing/decline button in either mood (✕ and outside tap only) | ProfilePortal.jsx:111–115 | **DONE** (verified against the phone 2026-09-17) | `ProfilePortal.jsx:104–132` | — | Keep — do not add a "Not now". The exits are the Sheet's ✕ (`AttachSheet.jsx:123–126`) and the scrim (`:95`). |
| 71 | Queue API: `setupKey(subjectName, grade)` = `"{name}|{GRADE}"`; `queueSetupCheck(keys)` (dedupe, cap 24, key `setup_check_pending_{user}`); `takeSetupCheck(key)` (spend once → true); `pruneSetupCheck(validKeys)`; `SETUP_CHECK_DELAY_MS = 1000`; storage guarded by `getUser()` and try/catch | ProfilePortal.jsx:135–184 | **DONE** (F3, `65c1bf1d`) | `@aruvi/shared/setupCheck.js:23–80` (11 tests) | shared `storage`, `userKey`, `getUser` | The row's own recommendation is what happened: pure, zero-JSX, lifted to shared, and **the web re-exports it from ProfilePortal.jsx**. ⚠️ The Mobile-ref column is a SHARED path, not a mobile file — that is the point of it. |
| 72 | Diff effect: subject·class keys of `readiness` vs previous set (per user; first resolved profile = baseline only) → `queueSetupCheck(added)`; `pruneSetupCheck(keys)` | page.jsx:210–234 | **DONE** (2026-09-16) | `(app)/_layout.jsx` (the effect above the sign-in redirect) | `subscribeReadiness` (NEW, readiness.js) | Watching the PROFILE rather than each add path is what makes "whichever door it came through" true — a key that appears where none was IS an add, including one made on the web. ⚠️ The phone needed a SUBSCRIPTION where the web needs none: `readiness` is page state there and a module store here, so nothing re-renders on a write unless the store says so (2 tests, incl. a throwing listener). |
| 73 | `onLessonsScope(subject, grade)`: not during tour / with a window open → `takeSetupCheck` → 1 s timer (cancelled on unmount / superseded) → open `{mode:"check", reason:"added", subject, grade}` only if no window opened meanwhile | page.jsx:236–260 | **DONE** (2026-09-16) | `lib/portal.js` `noteLessonsScope`/`cancelLessonsScope`; called from `lessons.jsx` below its validation effect | row 72 | The decision and the timer live in the STORE, not the screen: My Lessons is a route with nothing above it, and the window lives in the layout. ⚠️ `takeSetupCheck` is called outside any updater (it spends the key). ⚠️ The tour guard is owed with 8b — there is no tour to be over yet. |
| 74 | `portalWin` moods: `{mode:"change"}` (Add), `{mode:"check", reason:"tour"}`, `{mode:"check", reason:"added", subject, grade}`; rendered only when `ready && !entLapsed`; never over the tour | page.jsx:190–197, 1438–1443 | **DONE** (2026-09-18) | `(app)/_layout.jsx` owns `win` via `lib/portal` | rows 65–73; F5 for lapsed | The TOUR mood waits on 8b (its trigger is `finishTour`); the lapsed guard on F5 (6a) — enforcement is off for every teacher today. ★ **2026-09-18 reconcile:** all three moods. ★ The TOUR mood is raised by first run's one-shot (`lib/firstRun.js` `queueFirstRunCheck` → `index.jsx`:279 `takeFirstRunCheck`), not by `finishTour` — founder, 2026-09-17: a trigger that only fires for teachers who ran the tour misses everyone who declined it. ⚠️ **WEB OWED**: the web still raises it only from `finishTour`. |
| 75 | `setupCheckSub`: added → "You’ve added **{subject}**. **{Stage} stage**. Amend any of these items below."; tour → "Meyy started you off with {Section **9A** … | **N sections** … | its own suggested set-up}. You can change any of it — or leave it and carry on teaching." | page.jsx:1084–1116 | **DONE** (2026-09-16) | `@aruvi/shared/setupCheck.setupCheckSub` + `SetupCheckSub` in `ProfilePortal.jsx` | shared `stageOfGrade`, `pretty` | ★ Lifted to shared and **the web now delegates** — CLAUDE.md §3, the `portalGradeIdxs` lesson. It returns PARTS, not a sentence: the web bolds with `<b>`, a phone by naming a semibold FACE (`ap_sub_b`; `fontWeight` does nothing to a bundled static cut). ⚠️ "with 0 sections" is still never said. |
| 76 | `setupCheckValues` (check mood only): class list (≤3 then "…"), section tags, ppw "N a week" only if one shared value, budget "N periods" via `annualBudgetPeriods` only if one shared value; scoped to `portalWin.subject` + stage for "added"; null renders NOTHING | page.jsx:1118–1177 | **DONE** (2026-09-16) | `@aruvi/shared/setupCheck.setupCheckValues`; the layout passes it to `ProfilePortal` | shared `annualBudgetPeriods`, `classNum`, `stageOfGrade` | Lifted to shared with row 75; the web delegates. ⚠️ Computed at RENDER, never frozen into the window: she amends periods a week, the window comes back, and it must say SEVEN. 6 tests, incl. the two-classes-disagreeing silence and the out-of-scope null. |
| 77 | `onProfilePortal(kind)`: record `portalOriginRef {home: editFlow, win: portalWin}`, scope `{subject, grade}` when reason "added", close window, set `profilePortal=kind`, open profile (`editFlow="profile"`) | page.jsx:986–1013 | **DONE** — named divergence (5d·3) | `_layout.jsx:401–413` | TeachingProfile family (`portalIntent`, `portalScope`, `onConsumePortal`) | ★ **NOT a `router.push` to a `/profile` route.** The editor is an overlay on the SAME Sheet (`lib/portal.js:100–121`), so there is no navigation to record and nothing to thread through params. `enterPortal({originRoute, win, scope})` then `resolvePortalPick` → `openEdit`/`openPick`. ⚠️ The scope stays the WINDOW's and is never an invented `exact: true` — see 5d·10's second entry. |
| 78 | `goPortalHome`: restore origin tab (lessons/classes) THEN re-open the saved window (save and cancel alike) | page.jsx:1045–1052 | **DONE** — named divergence (5d·3) | `lib/portal.js:136–139` (`closeEdit`) | row 77 | Restores the window on save AND cancel alike, the web's own rule. ⚠️ No `router.back()` and no tab restore: she never leaves the screen she opened it from, which is why `leavePortal`/`originRoute` (`portal.js:156–161`) are now DEAD CODE — delete them or a future reader will wire them. ★ **2026-09-18 reconcile:** `leavePortal` DELETED from `lib/portal.js` (no caller; its reset also dropped `edit`/`pick`). `originRoute` is still written by `enterPortal` and read by nothing — harmless, left for now. |
| 79 | `onEditYearBudget(subject, grade)`: `portalOriginRef {home, win:null}`, `lessonsPaneIntentRef="plan"`, scope `{subject, grade, exact:true}`, intent "budget", open profile | page.jsx:1014–1044 | **DONE** — named divergence (5d·21) | `lessons.jsx:743–745` | YearPlan pencil (My Lessons family; plan says budget pencil unlocked by 5b/5c) | `onEditBudget → openEdit({intent:"budget", subject, grade})`, nulled when `ent.lapsed` so the pencil goes dark. ⚠️ No `lessonsPaneIntentRef` and no `scope.exact`: the window opens OVER the Year Plan pane, so there is no round trip to return from — `lib/paneIntent` was deleted rather than ported. |
| 80 | `goProfile` (bare profile via tour: clears `profileViaSettings`, `profileAutoAdd`, `profilePortal`, scope, origin) | page.jsx:725 | **DONE** (2026-09-18) | — | GuidedTour | — ★ **2026-09-18 reconcile:** tour `MOVES[16] = "/settings/profile"` — the bare profile, under the Settings bar. |
| 81 | `profileAutoAdd` / `autoAddClassSubject` — permanently null plumbing | page.jsx:61, 981–985, 1307 | NOT-PORTED-BY-DECISION | — | — | Dead by CLAUDE.md §0; do not port. |
| 82 | MyPlans `onOpenPortal` prop (legacy greeting-row "+", now unused since the bar's Add) | page.jsx:1338 | NOT-PORTED-BY-DECISION | — | — | The bar's Add is the only door. |

### J. Ask Meyy mount (page.jsx 74, 1445–1448; AskAruvi.jsx 87–97, 193–197)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes / native answer |
|---|---|---|---|---|---|---|
| 83 | `askOpen` state at shell level; `<AskAruvi onClose autoFocus={tour==null} />` rendered above the body; scrim from `--hdr-h` to `--bnav-h` (bar stays live); `aria-modal="false"`; `data-tour="ask-aruvi-root"` | page.jsx:74, 1448; AskAruvi.jsx:87–97, 193–197 | **DONE** (2026-09-18) | (app)/_layout.jsx:38 `onAsk={() => {}}` | AskAruvi port (separate family); shared bank `loadBank` | Native: an absolutely-positioned panel in `(app)/_layout.jsx` between `Bar` height and the BottomNav (NOT a route — a route would cover the bar and lose the toggle semantics). `autoFocus` off only matters with the tour. ★ **2026-09-18 reconcile:** `components/AskMeyy.jsx` mounted in `(app)/_layout.jsx` between the measured brand bar (`barH`) and the BottomNav; open state in `lib/ask.js`. |
| 84 | Bar items' behaviour while Ask is open: destinations close it and go; Ask toggles; Ask carries the clay | page.jsx:1376–1420 | **DONE** (2026-09-18) | — | row 83 | — ★ **2026-09-18 reconcile:** destinations `closeAsk()` then navigate; Ask toggles; `active` is "ask" while open. |
| 85 | Settings › Ask row (`onAsk`) opens the same panel at full height (bar absent there on web — superseded: bar is now up in Settings too) | page.jsx:1323 | **DONE** (2026-09-18) | — | Settings family | — ★ **2026-09-18 reconcile:** Settings › Help card calls `openAsk` (`settings/index.jsx`:242). |

### K. Generate / My Lessons / My Classes routing owned by the shell (page.jsx 507–546, 621–630, 685–688, 1285–1342)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes / native answer |
|---|---|---|---|---|---|---|
| 86 | `onEnterGenerate(opts)`: scoped when `opts.subject/grade` or a single subject·grade profile, else `{mode:"pick"}` (G1.9 picker); `single` flag; `prepareReturn` from `returnSection` | page.jsx:519–546 | **DONE** — scoped only, by decision | prepare.jsx via route params (always scoped) | Generate/Allocate NOT ported (dead per BRIEF); PrepareLesson family | The multi-choice picker path belongs to the dead GenerateTab; only the scoped prepare survives on the phone. ★ **2026-09-18 reconcile:** the multi-choice picker belongs to the dead Generate tab; every phone door into Prepare carries its scope. |
| 87 | `onAllocateScoped(subjectSlug, gradeSlug)` (My Lessons "Need a chapter…") | page.jsx:507–517 | DONE (equivalent) | lessons.jsx:640–641 → `/prepare` | — | — |
| 88 | `onOpenSection(subject, grade, sectionTag, plan)` → `pendingOpen` consumed by MyPlans (deep-link from My Lessons to a section's tracking view) | page.jsx:621–630, 1334 | NOT-PORTED-BY-DECISION | My Lessons on the phone opens the read-only lesson directly (`/lesson` without `section`) | My Lessons family | If the web's lesson card still offers "open for section X", the phone needs `router.push("/lesson", {…section})`. ★ **2026-09-18 reconcile:** **dead on the web**: `MyLessonPlans` receives `onOpenSection` and never calls it. Nothing to port. |
| 89 | `pendingAttach` consumed by MyPlans (auto-bind after prepare-from-card) | page.jsx:68, 1335 | **DONE** (2026-09-18) | — | row 46 | — ★ **2026-09-18 reconcile:** closed with row 46 — the phone binds in `prepare.jsx` instead of handing off a `pendingAttach`. |
| 90 | View branches: MyLessonPlans props (`readiness, onAllocate, onOpenSection, tourStep, preparing, lapsed, yearInfo, onStartTour, tourActive, onScope, onEditYearBudget, paneIntent, onDismissPrepareError, heldScopes`) | page.jsx:1285–1294 | **DONE** (2026-09-18) | lessons.jsx | rows 56, 73, 79 | `lapsed`, `yearInfo`, `onScope`, `onEditYearBudget`, `paneIntent` have no phone source yet. ★ **2026-09-18 reconcile:** `lessons.jsx` has every source now: `lapsed` via the entitlement store, prior-year folders (`:878`), `noteLessonsScope` (`:325`), `onEditBudget` (`:814`), `TourOffer` (`:864`); `paneIntent` is not needed (row 79). |
| 91 | TeachingProfile branch props (`readiness, onChange, onBack, lapsed, paidScopes, autoAddClassSubject, portalIntent, onConsumePortal, portalScope, heldScopes, onSubscribe`); `data-tour="profile-root"` | page.jsx:1295–1314 | **DONE** — reshaped (5d) | `components/ProfileEditor.jsx:121` (`{intent, subject, grade, scope, onChrome, hasBack}`) | TeachingProfile family; rows 53, 77 | The contract is RESHAPED, not ported: the phone's editor is a window the layout renders, so `portalIntent`/`portalScope`/`onBack` became `intent`/`scope`/the Sheet's own corners, `readiness` is self-seeded from the store, and `onChange` is `commit` → `saveReadiness`. `heldScopes` (2026-09-17) is the one prop BOTH surfaces now take, and it decides one thing: whether a subject survives losing its last class. |
| 92 | Settings branch props (`view, setView, legalDoc, setLegalDoc, onAccountSaved→entSyncTick++, onOpenProfile, syncTick, trial, onSubscribe, onAsk, onSignOut, onErased`) | page.jsx:1315–1324 | **DONE** (2026-09-18) | — | Settings family; rows 23, 29, 50, 52 | — ★ **2026-09-18 reconcile:** the Settings routes exist — `settings/index · personal · subscription · data · support · about · legal · profile` — each reading its own stores; there is no props bundle because there is no shell component to thread it through. |
| 93 | MyPlans branch props (`subject, grade, ready, readiness, onReady, onNavigate, onEnterGenerate, user=displayName||user, onSignOut, lapsed, pendingOpen/onConsumePending, pendingAttach/onConsumeAttach, onStartTour, tourActive, tourStep, onTourInfo, onOpenPortal, yearInfo, onCutover, cutoverBusy, cutoverResult, onDismissCutoverResult, cutoverDismissed, onDismissCutover, sectionCheck`) | page.jsx:1331–1342 | **DONE** (2026-09-18) | index.jsx | rows 29, 56–57 | `user` prop is the DISPLAY NAME on web; phone passes the raw id. ★ **2026-09-18 reconcile:** the greeting reads the account's first name (`index.jsx`:709-720), not the raw id. |
| 94 | `lessonsPaneIntentRef` one-shot ("plan") consumed by MyLessonPlans remount; cleared by `goClasses` | page.jsx:685–688 | NOT-PORTED-BY-DECISION | lessons.jsx:139 comment ("pencil is step 5 here, so nothing to except") | row 79 | — ★ **2026-09-18 reconcile:** superseded — the budget editor opens as a window OVER the Year Plan pane, so there is no round trip; `lib/paneIntent` was deleted (row 79). |
| 95 | Foot card on phone My Classes: entitlement status line + Auto/Light/Dark segmented control | — (web has these in Settings › App) | **DONE** (2026-09-18) | index.jsx:269–286 | Settings family | Named in header as interim ("moves to Settings in step 6"); remove when Settings lands. ★ **2026-09-18 reconcile:** the foot card is DELETED (`index.jsx`:654 comment) — theme to Settings › Appearance, the trial counter to Subscription & billing. |

### L. GuidedTour steps — full record for the deferred port (GuidedTour.jsx 58–146)

Deferred as a whole (BRIEF). Each row = one step; anchors are `data-tour` names; mobile status DEFERRED for all.

| # | Step | Anchor / place / hand | Title | Body | Web ref |
|---|---|---|---|---|---|
| 96 | 1 | `nav-classes` · above | "This is where your classes sit." | "The bar at the foot of the screen is how you move around Meyy. ‘My Classes’ is where your sections always are — each one points to where you have reached in its lesson plan. Soon we will see how." | 59–61 |
| 97 | 2 | `nav-lessons` · above | "This is where your generated lesson plans sit." | "‘My Lessons’, beside it at the foot of the screen, holds every lesson plan you have generated — and is where you generate new ones." | 62–64 |
| 98 | 3 | `lesson-first` · below · hand centred | "See the lesson plan you just now generated." | "You can filter your lesson plans by subject and class to see them all in one place." | 65–67 |
| 99 | 4 | `lesson-report` · below | "Generate PDF reports" | "You can generate PDF reports of Lesson plan and assessment by clicking this button." | 69–71 |
| 100 | 5 | `lesson-archive` · below | "You can archive a Lesson plan" | "Push those lesson plans you no more want to see into the archive box. Restore it back anytime you need it." | 73–75 |
| 101 | 6 | `lesson-first` · below | "Let us open the lesson" | "click on the lesson card to open a particular lesson" | 78–80 |
| 102 | 7 | `preview-root` · over · lift 130 · scrollTop | "Let us open the plan to have a quick view." | "You may review a lesson plan in its entirety here anytime. We will now attach this lesson to section {tag} from ‘My Classes’ at the foot of the screen." | 83–86 |
| 103 | 8 | `section-add` · below · hand | "Let us attach a lesson plan to a section." | "You want to attach “{chapter}” to section {tag}. Click the + sign of that section card." | 87–89 |
| 104 | 9 | `attach-pop` (hand on `attach-pop-row`, centred) · over | "Select a lesson plan to track for Section {tag}." | "Here is where you select the different lessons to attach to your sections. You can also generate new lessons here." | 90–92 |
| 105 | 10 | `section-card-target` · below · hand centred | "You are now ready to track." | "You have successfully attached “{chapter}” for section {tag}. Let us click it to see how tracking works." | 93–95 |
| 106 | 11 | `lesson-root` · below · tip on `unit-tabs`/`lesson-phase-1` · scrollTop | "You are now ready to use the plan to teach and track progress." | "Everything for a unit sits under four tabs — Overview, Material, Lesson and Assess — with clear timed steps and teacher guidance." | 99–101 |
| 107 | 12 | `phase-bookmark` · above | "Bookmark where you left a particular section" | "Move this bookmark to any particular phase to indicate where you stopped or wish to begin next for a section. Each section will have independent bookmarks." | 106–108 |
| 108 | 13 | `mark-complete` · above · hand | "Track progress." | "Track chapter progress of “{chapter}” with section {tag} unit by unit. Upon completion of a unit, click this button to mark it complete." | 109–111 |
| 109 | 14 | `section-add` · over · lift 10% vh · hand | "You have completed the chapter and are now ready for the next." | "Once all units of the chapter are marked complete by you, you are ready to teach another chapter. All you need is to click [+]." (`.gt-plus` glyph) | 114–116 |
| 110 | 15 | `attach-pop` · over | "Select a plan." | "You can use the same window shown in step 8 to select an existing chapter or generate a new plan." | 117–119 |
| 111 | 16 | `grow-add` · above | "Add/amend sections, classes and/or subjects." | "Use this button to quickly add sections, classes or subjects to your teaching profile." | 120–122 |
| 112 | 17 | `settings-gear` · below | "Your teaching profile." | "Your profile is built from what you do — read it whole here, at any time. Changes are made with ‘Add’ at the foot of the screen; removing a subject is the one thing done here." | 129–131 |
| 113 | 18 | `ask-aruvi` · above | "Use Ask Meyy to answer your queries" | "Get answers for up to 100 questions across 5 categories and use intelligent search to narrow your query." | 133–135 |
| 114 | 19 | `ask-aruvi-root` · over | "Use Ask Meyy to answer your queries" | "Use either the categories or the intelligent search to look for answers to your queries." | 138–140 |
| 115 | 20 | none · center · welcome | "Welcome to Meyy" | (no body) | 143–145 |
| 116 | Chrome | `.gt-root.gt-block` fixed z70; `.gt-scrim` (hit-blocking; dim only when no ring); `.gt-ring` (2px ochre, r12, 9999px box-shadow cutout, 180 ms transitions); `.gt-hand` (bobbing outline SVG, drop-shadow); `.gt-tip` (tint-pine, 1.5px pine border, r14; title display 16.5 pine-d; body 13 ink-soft); footer "{step} of 20" (mono 11) · "← Back" · "Skip" · "Next →"/"Done ✓" (pine pill); welcome variant 27px centred, stacked footer | GuidedTour.jsx:264–294; globals.css:3457–3507 | — | — | Native answer when un-deferred: `onLayout`/`measureInWindow` per anchor via a registry of refs instead of `document.querySelector`; 200 ms poll → measure on layout/scroll events; `scrollIntoView` → `ScrollView.scrollTo`. |
| 117 | Placement maths: tip width `min(88vw, 330)`; above/below clamped 12px from edges; below clamped to `max(80, vh-260)`; over pinned bottom with `lift` (fraction or px, default 18); hand corner (−16/−24) or centre (−18) clamped `vh-52`/`vw-46`; `PAD = 8` ring padding | GuidedTour.jsx:219–260 | — | — | `Dimensions`/`useWindowDimensions` | — |
| 118 | Scroll behaviour: `scrollTop` steps pin `.bodycontent` to 0 at most twice per step (400 ms apart); others `scrollIntoView({block:"center"})` up to 5 tries when off-screen | GuidedTour.jsx:179–201 | — | — | — | — |

## Dependency notes

What must land BEFORE this family is complete on the phone:
- **A real signed-in shell layer** in `mobile/app/(app)/_layout.jsx`. Today it holds only the `Stack` + an inert-half BottomNav. Everything in sections F–J that page.jsx keeps "above the tab" needs a home that survives route changes: `askOpen`, `portalWin` (+ `portalOriginRef`), `sectionFailed`, `privacyNote`, `saveFailed`, entitlement (`lapsed/trial/paidScopes`), `yearInfo`, `displayName`. Two viable shapes: React state in the layout (screens reach it via context) or module stores with `subscribe*` in the `lib/preparing.js` idiom (already proven for the proposed card). Recommend the store idiom for entitlement/account/year (they are data), and layout state for `askOpen`/`portalWin` (they are UI).
- **`packages/shared` additions (pure, no JSX):** `setupCheck.js` (ProfilePortal.jsx:135–184 verbatim), `account.js` (GET `/account` → `display_name`, `tour_offered_at`, cached like readiness.js), `entitlement.js` (polling + derivations from page.jsx:845–868), `year.js` (GET `/academic-year` + cutover from page.jsx:789–835). A `subscribe` hook on readiness.js so the setup-check diff (row 72) can run off profile changes.
- **Settings family** (step 6) unlocks rows 27, 30–33, 44, 85, 92, 95 and the erase flow (23).
- **TeachingProfile family** unlocks the portal rows' destinations (68, 77–79, 91) and `paidScopes` consumption (53).
- **AskAruvi family** unlocks 39, 83–85 (and the bank refresh in row 24 can land now, independently).
- **FirstRun family** unlocks the activation gate (14–18). Until then the phone silently assumes every signed-in teacher is set up.
- **BottomNav constant**: export the bar height from BottomNav.jsx and use it in lessons.jsx's toast and the future Ask panel (row 10).
- **theme/web.js keys to measure**: `set_bar_title`, `set_bar_gear`, `set_bar_x`, `tp_savefail` (+ button), `pn_note` (+ `pn_note_acts`, `pn_note_read`), `paywall_card/msg/subscribe/later`, `ap_grow`, `ap_row_line`, `ap_row_label`, `ap_row_val`, `ap_foot`, `ap_foot_go`, `ch_go`; and confirm `brand-mark` 19px at ≤600px vs the phone's 22.

What this family unlocks once done: lapsed/trial behaviour across every screen; the "+" (Add) and Ask Meyy bar items; the check window after tour/added-subject; the Year Plan budget pencil round trip; cutover offer and prior-year folders in My Lessons; the privacy-notice bar; verified-mismatch captions; name on the bar and greeting.

## Open questions for the founder

*(Q6 = item 1, Q7 = item 2 and Q8 = item 5 were ANSWERED on 2026-09-16 — see §4 of the map for the answers and their reasoning. Kept below as written, because the question is what the answer is an answer to.)*

1. **Paywall on the phone without a Subscribe flow.** The web's paywall offers "Subscribe" (SubscribeFlow) + "Not now"; the phone's sheet offers only "Close" under a fixed title "Your free chapters are used up" that misdescribes the "different subject" and "subscription ended" 402s. Should the phone (a) mirror the web's kicker rule with the server sentence and a single "Not now", and (b) say anything about HOW to subscribe (support@meyy.in? nothing?) while purchases are manual grants?
2. **Lapsed on the phone.** Web hides My Classes and Add and forces My Lessons for a lapsed subscription. The phone has no entitlement derivation. Confirm the same rules apply on the phone during the beta (enforcement is OFF on the deployed API, so today no teacher is ever lapsed) — i.e. port now and let the server flag drive it, or leave until enforcement is turned on.
3. **First-run gate location.** page.jsx decides FirstRun vs shell after ONE `/readiness` read and the `firstGenNeeded` triple read. On the phone, should the gate live in `app/index.jsx` (before the shell mounts, like the login redirect) or inside `(app)/_layout.jsx` (so a mid-session profile wipe re-routes)? The web's behaviour is the latter (it re-evaluates whenever `ready` flips).
4. **Display name on the bar.** Web shows the captured first name; phone shows the raw mobile number. Is the number-on-the-bar acceptable until Settings › Personal profile exists on the phone, or should the account fetch (row 29) land first so both surfaces greet her by name?
5. **`html.app-shell` equivalents already differ by nature; nothing to ask.** But note the phone renders `<Bar>` per route rather than one bar in the layout — the web has "one bar, one shell". Moving `<Bar>` into `(app)/_layout.jsx` would guarantee it never disappears (the 2026-09-14 "login and wheel disappear" bug class) and give Ask Meyy/portal a fixed anchor. Product-neutral, but it changes every route file — worth a yes/no.
