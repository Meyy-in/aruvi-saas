# Teaching Profile family — TeachingProfile.jsx · wheels.jsx · shared budget/readiness/format (the bar's "Add" destination; Track D step 5c in flight)

> ★ **RECONCILED 2026-09-18.** 23 rows moved, each with a dated `★ 2026-09-18 reconcile:` clause. Of the 98 rows: **75 DONE · 10 NOT-PORTED-BY-DECISION · 2 WEB-ONLY · 2 retired on both (62, 66) · 2 DEFERRED (69 subject-removal confirms, waiting on the removal redesign; 75 the wheel's gesture demo) · 7 closed as not-ported by founder Q8 the same day.** (Row 16 closed the same day — its premise was wrong.)
> **One real bug found and FIXED while reconciling:** the save-failed banner rendered on the BUDGET step only, so a verified mismatch on a class, section, periods-a-week or duration save was silent (row 13). Plus one missing clause restored on the empty class list (row 34), and row 33's ⚠️ struck — it described a web rule that does not exist.
> **The one thing this family genuinely owes is a DECISION, not code — founder Q8 (rows 31, 40–45):** on the phone a new subject's classes arrive SET UP (Section A, default length, calibrated periods) and the set-up check window discloses it afterwards; on the web the same act walks her through sections → periods a week → durations → budget for every class. That is a product divergence, not a technical one, so CLAUDE.md §0 says it needs a yes.
> Still owed in code: the web should write budgets through `setGradeBudget` (row 92).

Scope note. On the web this family is reached by three doors, all rendering ONE component
(`TeachingProfile`, page.jsx:1305-1313): (1) the bottom bar's **Add** → `ProfilePortal` window
(change mood) → `onProfilePortal(kind)` (page.jsx:1007-1013) → TeachingProfile in **manage mode**
with `portalIntent`; (2) the check-mood window after the tour / after a subject-stage is added
(same `onProfilePortal`, with `portalScope`); (3) Year Plan's budget pencil → `onEditYearBudget`
(page.jsx:1034-1044) → `portalIntent="budget"` with an `exact` scope; (4) Settings › Teaching
profile → the bare accordion (`profileViaSettings`, `onBack=null`) — whose "+ add a subject" row
raises a window of its OWN since 2026-09-17 (row 68a), the one door that does not go through
page.jsx. Every exit of a portal visit
goes back through `goPortalHome` (page.jsx:1045-1051), which restores the tab AND the window.
On the phone, none of these doors exists yet: `BottomNav` renders **Add** with `onAdd={() => {}}`
(mobile/app/(app)/_layout.jsx:44), YearPlan accepts `onEditBudget` but lessons.jsx does not pass
it (lessons.jsx:602), and Settings is step 6. What HAS landed for 5c: `packages/shared/src/budget.js`
(reader + normaliser + writer), `web/app/lib/budget.js` (the web re-export), and the
`tp_val_*` / `tp_weeks` / `tp_estimate_sub` / `fr_q` / `fr_foot` / `fr_cta` / `fr_link` measures in
`mobile/theme/web.js:622-651` — with NO mobile consumer of any of them yet.

## Web source map

- **web/app/components/TeachingProfile.jsx** (1797 lines)
  - 1-10 imports: `getJSON, pretty, ROMAN, stageOfGrade, projectReadiness, API, withUser, ESTIMATE_WEEKS, weeksFromAnnual, ppwFromAnnual` (lib/format) · `DAYS_IN_WEEK, budgetPeriods, normalizeBudget` (lib/budget ← @aruvi/shared/budget) · `verifiedWrite, readinessFingerprint` (lib/verify) · `pushSectionState` (lib/sectionState) · `RollWheel, PickWheel, PpwTotalWheel, PpwSplitCell, normPpw, ppwMapSum, ppwAnchor, setPpwSplit, setPpwTotal, lowestDuration, DEFAULT_DURATION, DEFAULT_PPW, DURATION_CHOICES` (wheels).
  - 12-47 header comment (accordion · master edit · structure-vs-values · no whole-profile actions · cascades).
  - 49 `SECTION_LETTERS` A…Z · 48-52 `PER_CLASS_GOALS` = section|ppw|budget, `GOAL_WORD` map ("classes"/"sections"/"periods a week"/"annual period budget").
  - 65-68 local `classNum` (roman → 3..10; NOTE: falls back to the raw grade, where shared `format.classNum` upper-cases — a tiny divergence to resolve by using the shared one).
  - 77-79 `subjectSlugOf`, `deepCopy`, `secLetter`, `byRoman`.
  - 80-95 section naming: `SEC_NAME_MAX=8`, `secName`, `cleanSecName`, `secObj` (`{tag:"9A", sec:"A", name?}` — `name` ABSENT when blank), `namesFromSections`, `secSummary` ("9A (Rose)").
  - 96-105 `SecNameCell` — the "customize" text box in a PickWheel trailing column (not autofocused, stopPropagation).
  - 112-126 `Bin` and `Pencil` inline SVG icons.
  - 128-130 `clearSectionState(subj, grade, tag)` — removes `lu_pointer_{key}`, `current_chapter_{key}`, `lu_done_{key}` from localStorage, then `pushSectionState(key)` so the server drops the row. key = `${slug}_${grade}_${tag}`.
  - 5-5 `rekeyBudget` — budget map is keyed by GRADE INDEX; re-key when grades change shape.
  - 6-9 `gradeDraftFrom(rec)` — grade record → editable draft (sections as letters, `section_names`, normalised `ppw_by_duration`, anchor).
  - 134 `stageOfRoman = stageOfGrade`.
  - 136 component signature: `{ readiness, onChange, onBack, lapsed, paidScopes, autoAddClassSubject, onConsumeAutoAdd, portalIntent, onConsumePortal, portalScope, onSubscribe }`.
  - 146 `canon = readiness.subjects` (derived, never mirrored).
  - 149-230 state: `openSubject`, `confirm` (DEAD — never set), **`addWin`** (189-203 — the add-a-subject chooser is a WINDOW, not a screen; see the bullet below), `screen` (view|pickSubjects|classes|class|subjectDone|addSection|editNums|editSections|portalSubject|portalClass), `pickMode`/`classMode` add|manage, `portalGoal`, `portalSi`, `subConfirm`, `removeSubject` {si,name,step}, `classConfirm`, `fromPortal`, `backLabel`, `catalogue`, `queue`/`qi`, `picked`, `secNames`, `gradeOptions`, `draft`, `pendingIdxs`/`pi`, `classStep` (sections|ppw|durations|budget), `numCtx` {si,gi,g,step}, `ncfTotal`, `recTotal`, `secConfirm`.
  - 234-251 sticky-offset effect: measures `.hdr`/app-shell and sets `--tp-sticky-top` / `--tp-sub-top` CSS vars (WEB-ONLY-BY-NATURE).
  - 253-255 `GET /subjects` → catalogue of pretty names.
  - 257-260 keep `openSubject` pointing at a real subject (defaults to first).
  - 265-274 auto-add effect (`autoAddClassSubject` → `startAddClass`) — plumbing permanently null since 2026-08-21.
  - 279-312 PORTAL INTENT effect: sets `fromPortal`; `subject`→`startManageSubjects`; `class`→`startManageClasses(si)` (scoped / single-subject shortcut, else `portalSubject` screen); section|ppw|budget→`portalPickClass` (same shortcuts); `onConsumePortal()`.
  - 321-323 bounce: `fromPortal && screen==="view"` → `onBack()` (every exit of a portal visit leaves the component).
  - 360-395 `saveFailed` + `persist(subjectsOut)`: optimistic `onChange(projectReadiness({subjects}))`, then `verifiedWrite({ write: POST /readiness {subjects, cascade:true}, read: GET /readiness, expect: readinessFingerprint(y.subjects) === want })`; on "mismatch" re-syncs the view to the server's copy and sets `saveFailed`.
  - 398-441 `doRemove` (section/grade/subject cascade removal) + 444-475 `confirmCopy` — reachable only via `confirm`, which nothing sets → DEAD PATH (the per-class/section dustbins were retired; kept as the vocabulary of the manage-mode confirms).
  - 478 `startAddSubject` · 490-500 `startManageSubjects` / `startManageClasses(si)` (pre-tick enrolled).
  - ★ **478-487 `startAddSubject` / `closeAddWin` — the add chooser opens as a WINDOW over the accordion**
    (founder, 2026-09-17: "clicking + add a subject must pop up a window (not change the screen) similar to
    expo now"). `startAddSubject` sets `addWin` and the screen together; `closeAddWin` is the ONE exit (✕,
    scrim, and `onSubjectsPicked` 602-610, which lowers it before the class run takes the page). ⚠️ `setAddWin(!fromPortal)`:
    a portal visit is ALREADY drawn inside page.jsx's `.tp-window-card`, and a second overlay in there would
    nest one window inside another.
  - 981-1089 `pickSubjectsScreen()` — the chooser as a RENDERER, drawn two ways (full page for the portal's
    manage visit, overlay for the add row). 1094 `if (screen === "pickSubjects" && !addWin) return
    pickSubjectsScreen()` is the whole switch: with the window up the render falls through to the accordion
    and the chooser is drawn INTO it. One renderer, because the scope filter, the empty-chooser fork and its
    subscribe door are the screen's real content and two copies of them drift.
  - 507-510 `portalOpen(goal, si, gi)` → `startEditSections` | `startEditNums(step)`.
  - 532-536 `portalGradeIdxs(si)` — exact-scope filter, then stage filter, fallback all. 538-542 `portalPickClass`.
  - 544-552 `startSubjectAdds`, `onManageSubjectsContinue` (adds/removes diff → `subConfirm` or straight to adds).
  - 559-569 `applyRemoveSubject` (accordion dustbin, after 2 confirms; `setEditing(false)`). 570-582 `applySubjectChanges` (manage-mode removals then queued adds).
  - 585-602 `beginSubjectRun(name)` — builds the draft, `GET /subjects/{slug}/grades`, screen `classes`.
  - 604-611 `onSubjectsPicked` · 613-617 `startAddClass` (dead caller path only).
  - 622-637 `continueWithGrades` / `onClassesContinue` — ADD mode: new grades get the per-class run (sections→ppw→durations→budget).
  - 662-684 `applyManageClasses(keep, adds)` — MANAGE mode: per added class `GET /subjects/{slug}/{grade}/ncf-periods` → seeds `sections:["A"]`, `DEFAULT_DURATION`, `ppwFromAnnual(annual)||DEFAULT_PPW`, `budget:{method:"periods", value:annual}`; `finalizeSubject`; screen view.
  - 685-692 `onManageClassesContinue` · 693-718 `applyClassChanges` (clears section state of removed classes; last class removed + no adds → subject removed).
  - 720-723 `gIdx`, `updGrade`.
  - 727-746 budget-screen effect: `GET /subjects/{slug}/{grade}/ncf-periods` → `ncfTotal` (`ncf_total_periods`), `recTotal` (`recommended_total_periods`).
  - 751-761 `recommendSubLine()` — "Meyy recommends {rec} periods a year based on general norms for this class." + " (NCF norm: {ncf})" when different; else "As per NCF, this class requires {ncf} periods."
  - 780-816 `renderBudgetStep({kicker, heading, ppw, b, setValue, stepValue, onPpwPencil, footer})` — ONE renderer for both budget callers.
  - 825-829 `finalizeSubject(d)` — draft → canonical record (`secObj`, normalised ppw map, `grids` all −1 shape-compat, `budget[i] = g.budget || {method:"auto", value:0}`), upsert by name, `persist`.
  - 831-836 `onClassDone` — next pending class, else finalize; `subjectDone` checkpoint if more subjects queued.
  - 839-845 `startAddSection` — DEAD (no caller; `addSection` screen unreachable). 877-889 `saveAddSection` likewise dead.
  - 848-876 `startEditSections` / `requestEditSections` / `applyEditSections` (the portal "Section" row's screen; drops → `secConfirm`).
  - 891-916 `startEditNums(si, gi, step)` / `updNum` / `saveEditNums(finalBudget)`.
  - 921-941 screen `portalSubject` · 943-979 `portalClass`.
  - 981-1089 screen `pickSubjects` (add + manage; paid-scope filter; empty states; `subConfirm` modal 1063-1086).
  - 1096-1196 screen `classes` (add + manage; stage scope; portal stage filter; `classConfirm` modal 1167-1192).
  - 1198-1319 screen `class` (the per-class run): `sections` 1202-1229 · `ppw` 1231-1252 · `durations` 1254-1294 · `budget` 1296-1278.
  - 1321-1340 screen `subjectDone`.
  - 1342-1366 screen `addSection` (dead) · 1368-1413 `editSections` (+ `secConfirm` modal 1401-1410).
  - 1415-1514 screen `editNums`: `duration` 1420-1466 · `ppw` 1468-1493 · `budget` 1495-1461.
  - 1536-1544 view helpers `secCount`, `gradePpw` (× sections), `stats`.
  - 1538-1797 VIEW (accordion): savefail 1544-1550 · sticky block 1551-1585 (back 1552-1556 · header 1557-1565 · empty 1567-1575 · stats 1577-1584) · subject rows 1587-1697 (header 1593-1624, class cards 1632-1675) · "+ add a subject" row 1699-1709 · **the add-a-subject WINDOW 1711-1728 (new 2026-09-17)** · remove-subject 2-step modal 1730-1780 · generic `confirm` modal 1782-1794 (dead).

- **web/app/components/wheels.jsx** (610 lines)
  - 5-14 `_maxLabelWidth` canvas text measure (auto-fit). 26-27 `WHEEL_ROW=64`, `PICK_ROW=52`.
  - 36-55 `stepScroll(el, dir, rowPx, rowCount)` — absolute-row step, reduced-motion, 240 ms snap fallback.
  - 59 `wheelDemoDone` — one 22 px rock-and-settle demo per page load.
  - 69-233 `RollWheel({items, value, onChange, ariaLabel, large, rowPx=64, fit=false, peek=false})` — base and peek modes, loop ×3 in peek, settle timer 120 ms, `step`, `stepCycle`, ArrowUp/Down, default-to-first, reposition on items, demo, auto-fit, `role=listbox` + `aria-activedescendant`, cue: ▲▼ (base) or single ▼ (peek).
  - 282-291 `clusterOrder(options, selected)` → `{ordered, start}`.
  - 293-406 `PickWheel({options, selected, onToggle, labelFor, initialScrollTo, ariaLabel, children, summaryLabel=true, trailing, trailingHeader, leadingHeader, summaryFor, cluster=true})` — 4-row window, `restOn`, `pick`, running "Chosen (n)" summary, ▲▼ side arrows when >4 rows, ArrowUp/Down.
  - 434-437 `DEFAULT_DURATION=40`, `DEFAULT_PPW=6`, `DURATION_CHOICES` 20…120 step 5, `PPW_CHOICES` 1…14.
  - 439-513 pure ppw arithmetic: `ppwMapSum`, `_durs`, `_get`, `lowestDuration`, `ppwAnchor`, `normPpw`, `setPpwSplit`, `setPpwTotal`.
  - 518-524 `PpwTotalWheel({value, onChange})` — a `large` RollWheel over PPW_CHOICES with chip=number, label "period(s) a week".
  - 544-610 `PpwSplitCell({duration, selected, map, total, isAnchor, onSet, show})` — anchor shows bare number; others a button + position:fixed listbox 0…total, closes on select/Escape/outside/scroll/resize, flips above when tight.

- **packages/shared/src/budget.js** (111 lines) — `DAYS_IN_WEEK=6`, `ESTIMATE_WEEKS=30`; `budgetPeriods(ppw, b)` 36-42 (reads weeks|periods|days|auto); `normalizeBudget(stored, ppw, rec)` 62-69 (`{method:"auto", value:0}` = unset → `rec` → `ppw×30`); `clampPeriods` 72; `findScope` 77-83; `setGradeBudget` 95-103 (the WRITER — only `periods`); `gradeBudgetRecord` 106-111.
- **web/app/lib/budget.js** (5 lines) — `import "./shared-setup"; export * from "@aruvi/shared/budget"`.
- **packages/shared/src/readiness.js** (167 lines) — `READINESS_CACHE_PREFIX`, `cachedReadiness()` (sync), `cachedReady()`, `fetchReadiness({force})` (401 rethrown), `invalidateReadiness()` (call after ANY profile write), `clearReadiness()`. NOTE: read-only; there is no `saveReadiness`/POST helper in shared yet.
- **packages/shared/src/format.js** (parts this family calls) — `ROMAN` 12, `pretty` 14, `gradeUp` 15, `classNum` 18-21, `stageOfGrade` 28-33, `withUser` 88, `getJSON` 97, `largestRemainder` 292-305, `ESTIMATE_WEEKS`/`ppwFromAnnual`/`weeksFromAnnual` 327-331, `annualBudgetPeriods` 333-357 (the SECOND budget reader — grid-cells ÷ sections, falls back to `periods_per_week`), `subjectSlug` 361, `fetchSupportedGrades` 365-378 (cached `GET /subjects/{slug}/grades` — TeachingProfile does NOT use it, it calls getJSON directly at 576), `projectReadiness` 386-401.
- **packages/shared/src/verify.js** — `verifiedWrite` 35, `readinessFingerprint` 102-125 (name · grades{grade, sections tag[:name] sorted, durations sorted, ppw}).
- **packages/shared/src/sectionState.js** — `unbindSection` 42 (the shared answer to `clearSectionState`'s three key removals), `pushSectionState` 157, `clearLocalSectionCache` 217.

## Mobile counterpart map

- **mobile/components/RollWheel.jsx** (221 lines) — `RollWheel({items, value, onChange, ariaLabel, rowPx=64, align, padLeft, peek, clamp})`: ScrollView + `snapToInterval`, base (▲▼ clamped, chip) and peek (×3 loop, single ▼) modes, 120 ms settle on `onScroll`/`onMomentumScrollEnd`/`onScrollEndDrag`, default-to-first, repark on `[items, rowPx]`, `adjustsFontSizeToFit` when `clamp===1`. No keyboard (named divergence), no gesture demo (named divergence), no `large`/`fit` props (replaced by `adjustsFontSizeToFit` + `rw_*` measures). Measures `ws.rw_shell/rw_row/rw_label/rw_cue/rw_cue_btn/rw_cue_glyph` (peek) and `rw_shell_base/rw_row_base/rw_label_base/rw_chip*` (base) at web.js:509-529.
- **mobile/components/AttachSheet.jsx** (139 lines) — `Sheet({visible, onClose, kicker, title, sub, confirm, children})` 38-63: Modal + scrim-tap-to-close + ✕ + kicker/title/sub head (`ws.ap_overlay/ap_modal/ap_confirm/ap_close/ap_head/ap_kicker/ap_title/ap_sub`); `ws.ap_actions/ap_btn/ap_btn_label` for a two-button footer (clay danger = `#fff` on `t.clay`). This is the primitive every `.fr-modal-bg/.fr-modal` confirm in this family maps to.
- **mobile/components/YearPlan.jsx** (216 lines) — reads the budget through `annualBudgetPeriods(readiness, sSlug, gSlug)` 95 (falls back to `recSum`); accepts `onEditBudget` 47 but the totals row renders no pencil (24-35 names it as deferred to step 5); lessons.jsx:602 does not pass it.
- **mobile/app/(app)/prepare.jsx** (605 lines) — reads `classProfile` (durations + `ppw_by_duration`) 135-143, `annualBudget` 167-168 via `annualBudgetPeriods`; uses base `RollWheel` 400-402 (rowPx 92, clamp 2) and a −/+/TextInput stepper 407-425 (`ws.prep_step_*`) — the closest existing analogue of `.tp-val-row`.
- **mobile/theme/web.js:622-651** — budget-editor measures already transcribed (`fr_q`, `tp_val_row`, `tp_val_btn`, `tp_val_btn_t`, `tp_val_input`, `tp_val_unit`, `tp_weeks`, `tp_estimate_sub`, `fr_foot` (paddingTop 108 = 20 + 88), `fr_cta`, `fr_cta_t`, `fr_link`, `fr_link_t`). NOT present: `kicker_ochre` (no CSS rule exists on the web either — `.kicker-ochre` is an unstyled marker class, so the phone uses `ws.kicker`), `fr_hint`, `fr_modal*`, `tp_remove_confirm`, every `fr_sec_*` (PickWheel), `fr_ppw_*`, `tp_secname`, `tp_portal_*`, every accordion `tp_*`, `tp_savefail`, `trial_note` (present at 558), `fr_ready_*`, `fr_loading`, `tp_nomore`, `tp_implies_edit`, `tp_rm_keep`.
- **mobile/components/BottomNav.jsx:98-102** — the **Add** item with `showAdd` (lapsed gate) and `onAdd`; `_layout.jsx:44` wires `onAdd={() => {}}`.
- **ProfilePortal window** (web/app/components/ProfilePortal.jsx: ROWS 56-61 = Class · Section · Periods a week · Annual period budget; check/change moods) — none on mobile. It is a separate family in this inventory series but is the ONLY entry to the manage-mode screens below, so it is listed under dependencies.

## Inventory table

| # | Feature / UI element | Web ref (file:lines) | Mobile status | Mobile ref | Depends on | Notes / native answer |
|---|---|---|---|---|---|---|
| **A. Entry, props, plumbing** |||||||
| 1 | Component contract: `readiness`, `onChange(projection)`, `onBack`, `lapsed`, `paidScopes`, `portalIntent`, `onConsumePortal`, `portalScope {subject, grade, exact?}`, `onSubscribe`, `autoAddClassSubject`/`onConsumeAutoAdd` | TeachingProfile.jsx:136 | **DONE** | `components/ProfileEditor.jsx:121` | @aruvi/shared/readiness (`cachedReadiness`, `fetchReadiness`, `invalidateReadiness`), entitlement (`fetchEntitlement` → `paid_scopes`, lapsed) | On the phone this becomes a ROUTE (`/profile`) taking `intent`, `scope` params; `onChange` = write to the readiness store + `invalidateReadiness()`; `onBack` = `router.navigate` to origin. Auto-add plumbing (292-301, 589-593) is permanently null on the web — DO NOT port. ★ **RESHAPED, NOT PORTED**: `{intent, subject, grade, scope, onChrome, hasBack}` — the editor is a WINDOW the layout renders, so `portalIntent`/`portalScope`/`onBack` became `intent`/`scope`/the Sheet's own corners; `readiness` is self-seeded from the store (:133), `onChange` is `commit` → `saveReadiness` (:292), `paidScopes` and `lapsed` are self-fetched (:186), `onSubscribe` is `router.push("/subscribe")` (:655). `heldScopes` joined BOTH surfaces 2026-09-17 (rows 97–99). |
| 2 | Door 1: bottom-bar **Add** → ProfilePortal window (change mood) → `onProfilePortal(kind)` | page.jsx:1007-1013, 1389-1409 | **DONE** | `_layout.jsx:459`; `BottomNav.jsx:98–102` | ProfilePortal port (separate inventory), `portalOriginRef` equivalent | Native: the window is a `Sheet`; `onPick` navigates to `/profile?intent=…`; the origin (tab + window) must be remembered in a module store the way `lib/preparing.js` holds the proposed card, so the return re-opens the window. `onAdd` is no longer a no-op — it sets `{mode:"change"}` and the layout renders the window in the shared Sheet (:352, :393). |
| 3 | Door 2: check-mood window (tour end / added subject-stage) with `portalScope` (stage marker only) | page.jsx:1008-1011; TeachingProfile.jsx:284-310, 532-536 | **DONE** (2026-09-18) | `lib/portal.js:184–200`; `_layout.jsx:393` | GuidedTour (NOT-PORTED-BY-DECISION) for the tour-end trigger; the added-subject queue (`queueSetupCheck` etc., ProfilePortal.jsx) | Tour half is moot on the phone until the tour is ported; the added-subject-stage half depends on SubscribeFlow (NOT-PORTED-BY-DECISION) — on a manual-grant beta a scope can still arrive if a subject is added in the web and first opened on the phone. Founder question (Q1). The ADDED-subject half is live (`noteLessonsScope` raises `{mode:"check", reason:"added"}` with a `{subject, grade}` scope); the TOUR-end trigger waits on 8b, which is the only half the founder's Q1 ever really asked about. ★ Since 2026-09-17 the scope names the SUBJECT as well as the stage — see 5d·10. ★ **2026-09-18 reconcile:** both halves. The TOUR-end half is raised by first run's one-shot (`lib/firstRun.js` → `index.jsx`:279), which the founder chose on 2026-09-17 over `finishTour` because it also reaches teachers who skip the tour. |
| 4 | Door 3: Year Plan budget pencil → `portalIntent="budget"` with `{subject, grade, exact:true}`; returns to the PLAN pane via `lessonsPaneIntentRef` | page.jsx:1034-1044; TeachingProfile.jsx:517-536 | **DONE** — named divergence | `lessons.jsx:743–745`; `components/YearPlan.jsx:237–240` | budget editor screen (rows 68-80), `setGradeBudget` (shared budget.js:95), a "return to Year Plan pane" intent | Native: `router.push("/profile?intent=budget&subject=…&grade=…&exact=1")`; on save `router.back()` lands on My Lessons with its pane state intact (a route back preserves the pane, so `lessonsPaneIntentRef` may not be needed — verify). ⚠️ No `exact` scope and no `lessonsPaneIntentRef`: the editor opens as a window OVER the Year Plan pane, so both pick screens are skipped by construction and there is no round trip to return from. `lib/paneIntent` was DELETED rather than ported. |
| 5 | Door 4: Settings › Teaching profile → the accordion with `onBack=null` (Settings bar frames it) | page.jsx:1295-1313, 937, 954 | **DONE** | `(app)/settings/profile.jsx:148–243` | Settings screen | Accordion rows (§F) are needed for this door only; the three portal doors never show the accordion. ⚠️ Read-only by design: every edit leaves through `openEdit` (:113), and the screen has no `onBack` — the Settings bar frames it. |
| 6 | `fromPortal` bounce: every `setScreen("view")` on a portal visit calls `onBack()` — she never sees the accordion | TeachingProfile.jsx:189-210, 321-323 | **DONE** — native answer | `lib/portal.js:136` (`closeEdit`) | row 2 origin store | Native: a portal visit is its own stack of screens; "view" = `router.back()` to the origin, then re-raise the window from the store. Not the web's bounce: one Sheet whose children swap, so closing the edit simply falls back to the window underneath (`_layout.jsx:353, :367`). She never reaches an accordion from a portal visit at all. |
| 7 | `backLabel`: "← Back" (portal) vs "← Back to profile" (settings) | TeachingProfile.jsx:210 | NOT-PORTED-BY-DECISION | — | — | Exact strings. ★ **2026-09-18 reconcile:** native answer — every edit sits in the shell's one Sheet, whose ← corner (the step or the journey behind it) and ✕ replace both web labels. There is no footer back link to label. |
| 8 | Sticky measurement effect: `--tp-sticky-top` / `--tp-sub-top` from `.hdr` height / app-shell | TeachingProfile.jsx:234-251; globals.css:2884, 2903-2914 | WEB-ONLY-BY-NATURE | — | — | RN: `stickyHeaderIndices` on the accordion ScrollView, or let the open subject header travel (as YearPlan.jsx:150-153 chose). |
| 9 | `GET /subjects` → catalogue of pretty subject names (for the add-a-subject wheel) | TeachingProfile.jsx:253-255 | **DONE** | `components/ProfileEditor.jsx:112–120` | API `GET /subjects`; `pretty` (shared format.js:14) | Cache once per session like `fetchSupportedGrades` does. `primeSubjectCatalogue()` caches `GET /subjects` once per launch, primed on Settings focus (`settings/profile.jsx:143`). ⚠️ A failure caches nothing and retries. |
| 10 | `GET /subjects/{slug}/grades` per subject run, sorted by ROMAN, upper-cased | TeachingProfile.jsx:597-600 | **DONE** | `components/ProfileEditor.jsx:339` | — | Use the shared cached helper on the phone (the web bypasses it — harmless drift to note). |
| 11 | `GET /subjects/{slug}/{grade}/ncf-periods` while a budget screen shows → `ncf_total_periods`, `recommended_total_periods` | TeachingProfile.jsx:727-746 | **DONE** | `components/ProfileEditor.jsx:245, :430` | API route | Also fetched per added class in manage mode (row 46). |
| 12 | `persist()` — optimistic `onChange(projectReadiness({subjects}))` then `verifiedWrite` (POST /readiness `{subjects, cascade:true}`, GET /readiness, `readinessFingerprint` equality) | TeachingProfile.jsx:362-395 | **DONE** | `components/ProfileEditor.jsx:292–302` | shared verify.js:35 `verifiedWrite`, :102 `readinessFingerprint`; `postJSON`/`fetch` with `withUser`; a `saveReadiness()` helper in shared readiness.js (does not exist yet) | Add to shared readiness.js: `saveReadiness(subjects)` = POST + `invalidateReadiness()` + write-through of the new profile into `mem`/device copy, so My Classes/My Lessons repaint from the store. Both surfaces should then call the same function (step 5c's own rule). ⚠️ No optimistic `projectReadiness` pre-write: the store adopts the VERIFIED copy, which is `saveReadiness`'s own contract. |
| 13 | Mismatch banner: "That change didn’t save — this is your teaching profile as it stands." + **Dismiss**; only on `status==="mismatch"`, view re-synced to server copy | TeachingProfile.jsx:359-360, 387-394, 1545-1550; globals.css:4991-5004 `.tp-savefail` | **DONE** (2026-09-18) | `components/ProfileEditor.jsx:865–874` | row 12 | `role="alert"` → `accessibilityLiveRegion="assertive"`. ⚠️ **THE MISMATCH BANNER ONLY EXISTS ON THE BUDGET STEP.** The block sits inside the branch opened at :851, so a mismatch on a section, class, ppw or duration save sets `err` (:299) and renders NOTHING — the one outcome the read-after-write doctrine says she must be told about. Found in the 2026-09-17 audit. ★ **2026-09-18 reconcile:** **FIXED today.** The banner was written inline in the budget branch only, so a verified mismatch on a class, section, ppw or duration save set `err` and showed nothing. It is now one `saveFail` element placed under every step's heading (`ProfileEditor.jsx`). |
| 14 | `clearSectionState(subj, grade, tag)`: remove `lu_pointer_`, `current_chapter_`, `lu_done_` for key `${slug}_${grade}_${tag}`, then `pushSectionState(key)` | TeachingProfile.jsx:128-130 | **DONE** | `components/ProfileEditor.jsx:504, :530` | shared storage shim | `unbindSection(key)` already removes chapter/pointer/done and pushes; use it on the phone rather than re-listing the keys. Also `invalidatePlans` may be needed if My Lessons caches attachment state. |
| 15 | `rekeyBudget` when a grade is removed (budget keyed by grade INDEX) | TeachingProfile.jsx:5-5, 409, 420 | **DONE** | `@aruvi/shared/budget.js:112`, called at `ProfileEditor.jsx:461` | — | Pure; candidate to move into shared budget.js beside `setGradeBudget` (its header already warns about the index key). |
| 16 | `gradeDraftFrom(rec)` / `finalizeSubject(d)` — draft ⇄ canonical record; `grids` emitted as all −1 (shape-compat); unanswered budget written as `{method:"auto", value:0}` | TeachingProfile.jsx:6-9, 825-829 | **DONE** — the premise was wrong | `@aruvi/shared/profile.js:57` (`gradeDraftFrom`) | wheels arithmetic (row 96-101) in shared | Pure; move to shared (`profile.js`) so both surfaces write byte-identical records — the fingerprint test depends on it. ⚠️ `finalizeSubject` (shared, `profile.js:80`) has NO mobile consumer — the phone builds the class record inline in `applyClasses` (:435–466), including its own all-−1 `grids` fill. Two spellings of one normalization, which is the drift §3 exists to stop. ★ **2026-09-18 reconcile:** still PARTIAL, and it is a real drift risk: `finalizeSubject` (shared) is the web's writer, while the phone's `applyClasses` builds the class record inline with its own all-−1 `grids`. Fingerprints agree today; owed: have `applyClasses` build through the shared helpers so there is one spelling. ★ **2026-09-18 reconcile:** `finalizeSubject` is the writer of the web's per-class ADD RUN (`onClassDone`), which the phone does not have (founder Q8). The phone's `applyClasses` is the port of the web's MANAGE path, `applyManageClasses`, which also builds its records inline — so the two surfaces already use one spelling per path. Revisit only if Q8 brings the add run to the phone. |
| 17 | Section record + name: `secObj`, `cleanSecName` (collapse whitespace, no leading space, ≤8 chars), `namesFromSections`, `secSummary` ("9A (Rose)") | TeachingProfile.jsx:80-95 | **DONE** | `@aruvi/shared/profile.js:23–55`, used at `ProfileEditor.jsx:538, :760–762` | — | Pure; shared. |
| 18 | `stageOfRoman = stageOfGrade` | TeachingProfile.jsx:134 | DONE (helper) | shared format.js:28-33 | — | — |
| **B. Portal pick screens** |||||||
| 19 | Screen `portalSubject`: kicker "Your teaching · {what}", h1 "In which subject?", hint "Pick the subject whose classes you want to change." (class goal) / "Pick the subject, then the class whose {what} you want to change.", one `.tp-portal-row` per subject with "›", back link | TeachingProfile.jsx:921-941; globals.css:3002-3013 | **DONE** | `components/ProfilePick.jsx:97–107` | GOAL_WORD map (47-51) | Skipped when scoped or single-subject (327-336). Rows → `Pressable` list; measure `.tp-portal-row/.tp-portal-go` into web.js. ⚠️ The bottom "← Back" is gone (the window's ✕ is the exit), and the subject screen is SKIPPED on a scoped visit (`shared/profile.js:243`, 2026-09-17). |
| 20 | Screen `portalClass`: kicker "{subject} · {what}", h1 "Which class?", hint "Pick the class whose {what} you want to change.", rows "Class {n}" from `portalGradeIdxs` | TeachingProfile.jsx:943-979 | **DONE** | `components/ProfilePick.jsx:100–121` | `portalGradeIdxs` (499-514) | Skipped when exactly one class is in play (516-520). ⚠️ Back is a ← corner, drawn only when she has more than one subject (:82). The empty-list case now names the way out (2026-09-17). |
| 21 | `portalGradeIdxs`: exact-scope → that class; stage-scope → classes of that stage; fallback all (never blank) | TeachingProfile.jsx:532-536 | **DONE** | `@aruvi/shared/profile.js:184` + `resolvePortalPick` (:222) | `stageOfGrade` | Pure; shared. Both consumed by `ProfilePick.jsx:90` and `_layout.jsx:386, :410`. ★ `resolvePortalPick` is what decides which screens are skipped — one rule, two callers. |
| **C. Subjects wheel (add / manage)** |||||||
| 22 | Screen `pickSubjects` (ADD): kicker "Teaching profile · add a subject", h1 "What else do you teach?", hint "Pick the subject — or several — to add.", PickWheel `cluster=true`, CTA **Continue** disabled until a pick; ★ **raised as a WINDOW over the accordion since 2026-09-17** (`.ap-overlay.tp-window` › `.ap-modal.tp-window-card`, ✕ + scrim, no ←) | TeachingProfile.jsx:981-1089 (renderer), 1094 (full page), 1711-1728 (window) | **CHANGED** (2026-09-18) | ProfileEditor.jsx `step === "subject"` | PickWheel (DONE), `GET /subjects` | Same kicker and heading; hint reads "Pick a subject to add. You'll choose its classes next." because the phone takes **one subject per journey** — the web queues several and runs `subjectDone` checkpoints between them, which is a screen the phone does not have. ⚠️ `cluster={false}`, NOT the web's `true`: this is the list where clustering swallowed Mathematics for a teacher of four subjects, and the 2026-08-29 reversal was about the CLASS wheel only. Continue → `openEdit({intent:"class", subject})`, so the existing class wheel runs unchanged. ★ **AND THE WEB HAS COME TO THE PHONE, not the other way round** (founder, 2026-09-17): this chooser used to REPLACE the Settings page, so "+ add a subject" read as navigation and the list she was adding to vanished under it. It now floats over that list, which is what the phone has always done — the same Sheet every other profile edit uses. ⚠️ **The window holds the CHOOSER ONLY** on the web: Continue lowers it and the classes → sections → duration → periods → budget run takes the page, because that run is a conversation rather than a question. The phone keeps the whole journey in its Sheet, which it can afford — it takes one subject per journey and has no `subjectDone` checkpoint. ★ **2026-09-18 reconcile:** "+ add a subject" no longer opens a chooser on EITHER surface — it opens subscribe (founder Q8). The phone's subject step in `ProfileEditor` is now unreachable too. |
| 23 | Screen `pickSubjects` (MANAGE): kicker "Your teaching · subjects", h1 "What do you teach?", hint "Tick a subject to add it — untick one to remove it.", full catalogue with enrolled PRE-TICKED, `cluster={false}` (the swallowed-Mathematics rule), CTA **Continue** always enabled | TeachingProfile.jsx:490-494, 981-1050 | NOT-PORTED-BY-DECISION | — | PickWheel port | Only reachable via `portalIntent="subject"`, which no window sends any more (323-325) — effectively dead on the web; port only if the founder wants it (Q2). ★ **2026-09-18 reconcile:** unreachable on the web (no window sends `portalIntent="subject"`); the phone's only subject door is the one-at-a-time add (row 22). |
| 24 | Paid-scope filter: `paidScopes` array without "*" → only subjects in scope (enrolled always kept) | TeachingProfile.jsx:996-999 | DONE | ProfileEditor.jsx `subjScoped` / `subjectOptions` | `fetchEntitlement` → `paidScopesOf` | Enrolled subjects are excluded outright rather than "always kept": this is the ADD door, and offering her English again would be offering her nothing. (The web keeps them because its MANAGE mode shares the screen.) ⚠️ Unreachable server → `null` → NO limit, deliberately: a school network must never narrow her own subjects away. |
| 25 | Empty chooser, SCOPED: `.tp-nomore` "Your subscription covers the subject(s) you already teach. Another subject is a separate subscription." + CTA "Add a subject to my subscription" (`onSubscribe`) | TeachingProfile.jsx:1018-1029; globals.css:2997-2998 | DONE ✅ **WITH the CTA after all** | ProfileEditor.jsx subject step | `/subscribe` (6b) | ⚠️ **This row's own decision is reversed, and it should be.** It read NOT-PORTED-BY-DECISION because SubscribeFlow did not exist on the phone; it does since 6b, so the honest answer — name the limit and offer the way past it — is available and is what ships. This is the screen the founder's own account lands on (its four scopes are exactly the three subjects it already teaches), so without the CTA the one door built today would have opened onto a dead end. ⚠️ `closeEdit()` BEFORE `router.push`: pushing under an open Sheet leaves the scrim over the screen she was sent to. |
| 26 | Empty chooser, UNSCOPED: "Every subject Meyy offers is already in your profile." | TeachingProfile.jsx:1030-1032 | DONE | ProfileEditor.jsx subject step | — | Exact string. Unscoped = trial or a "*" grant, where it really is a fact about the catalogue and there is nothing to offer past it. |
| 27 | `.trial-note` under the wheel when scoped: "Your subscription covers what's shown here. Another subject or stage is a separate subscription." | TeachingProfile.jsx:1053-1058; globals.css:3992 | DONE | ProfileEditor.jsx subject step | web.js:558 `trial_note` | — | Word for word, and only when there IS a wheel — an empty chooser has already said it at more length with the way out attached (row 26). |
| 28 | Manage-subjects removal confirm modal: h2 "Remove {names}?", body "{Class n, …} — all cards and bookmarks — will be removed. Your lessons stay in the library." (fallback "Its classes"), danger CTA "Yes, remove {names}", link "Keep it"/"Keep them" which RE-TICKS the removed subjects | TeachingProfile.jsx:1063-1086 | NOT-PORTED-BY-DECISION | — | `Sheet` (AttachSheet.jsx:38) with `confirm`; `ws.ap_actions/ap_btn` | Map `.fr-modal` → `Sheet confirm`, `.tp-remove-confirm` (globals.css:3015-3020, solid red/clay) → `ap_btn` on `t.clay`. ★ **2026-09-18 reconcile:** belongs to the manage-subjects screen (row 23), which neither surface can reach. Subject REMOVAL is the redesign in row 62. |
| 29 | `onManageSubjectsContinue` / `applySubjectChanges`: removals clear every section's state, then queued adds run `beginSubjectRun` | TeachingProfile.jsx:545-552, 570-582 | NOT-PORTED-BY-DECISION | — | rows 12, 14 | — ★ **2026-09-18 reconcile:** as row 28. |
| 30 | Multi-subject queue: `queue`/`qi`, kicker suffix " · subject {i} of {n}" | TeachingProfile.jsx:212, 604-611, 1123 | NOT-PORTED-BY-DECISION | — | — | — ★ **2026-09-18 reconcile:** the phone takes one subject per journey (row 22), so there is no queue and no "subject i of n". |
| **D. Classes wheel (add / manage)** |||||||
| 31 | Screen `classes` (ADD, new subject): kicker "{subject}", h1 "Which classes do you teach {subject} to?", CTA **Continue** (disabled until a pick), `cluster` default true, `labelFor` "Class {n}" | TeachingProfile.jsx:1096-1157 | NOT-PORTED-BY-DECISION | `components/ProfileEditor.jsx:691–736` | PickWheel, row 10 | ⚠️ The ADD-a-subject classes screen is the SAME screen as manage: the CTA reads "Save" and is never disabled where the web's add run reads "Continue" disabled until she picks, and the kicker is "{subject} · classes". Reached by `goToClasses` (:407). ★ **2026-09-18 reconcile:** the phone's add-a-subject journey is subject → class wheel → **Save**, and each added class arrives SET UP (Section A, default length, calibrated ppw/budget — `ProfileEditor.jsx` `applyClasses`), exactly as a manage-mode add does. The web instead runs this per-class conversation. Nothing on the phone asks it. The set-up check window still fires for the new subject·class (the layout's readiness diff queues it), so the assumptions ARE disclosed — but this is a product divergence, not a technical limitation, and needs the founder's say-so (open question 8). Also the CTA reads **Save** where the web's add run reads Continue — consistent with the phone's shape, since nothing follows. ★ **2026-09-18 reconcile:** **ANSWERED — founder Q8, 2026-09-18: there is no such journey.** "+ add a subject" is SUBSCRIBING to one, on both surfaces (web → SubscribeFlow, phone → `/subscribe`). Checkout lands the subject with defaults; the set-up check asks about them on her first My Lessons visit. The web's per-class run is unreachable now (entry point cut in `TeachingProfile.jsx`; the code is left for deletion with a live check). |
| 32 | Screen `classes` (ADD, existing subject): h1 "Which classes are you adding for {subject}?", hint "Your current classes stay as they are — pick only the new ones." | TeachingProfile.jsx:1120, 1125, 1130 | NOT-PORTED-BY-DECISION | — | — | Reachable only via dead `startAddClass` → effectively unreachable on the web (the "+ add a class" button was retired, 1656-1657). Do not port. ★ **2026-09-18 reconcile:** dead on the web (`startAddClass` has no caller); do not port. |
| 33 | Screen `classes` (MANAGE): kicker "{subject} · classes", h1 "Which classes do you teach {subject} to?", hint "Tick a class to add it — untick one to remove it. A new class starts with Section A; change that under Section.", enrolled PRE-TICKED, CTA **Save** (always enabled) | TeachingProfile.jsx:495-500, 1123-1129, 1152-1155 | **DONE** | `components/ProfileEditor.jsx:691–736` | PickWheel | The portal "Class" row's screen. ⚠️ "Save" is never disabled at zero classes, where the web's `manageC` rule disables it. ★ **2026-09-18 reconcile:** the ⚠️ above is WRONG and is struck: the web's manage mode also never disables Save (`disabled={manageC ? false : …}`, TeachingProfile.jsx:1152). The phone matches. |
| 34 | Loading state "Loading classes…" (`.fr-loading`) and empty "Every class Meyy offers for {subject}{ at this stage} is already in your profile." | TeachingProfile.jsx:1131-1135; globals.css:2341 | **DONE** | `components/ProfileEditor.jsx:703–706` | — | ⚠️ The empty sentence omits the web's "{ at this stage}" clause. ★ **2026-09-18 reconcile:** the "{ at this stage}" clause is now carried (`ProfileEditor.jsx`, driven by `portalStage`). |
| 35 | Stage-scope filter (`allowedStages` from `paidScopes` for this subject; enrolled always kept) and portal stage filter (`portalStage` from `portalScope`) | TeachingProfile.jsx:1104-1118 | **DONE** (2026-09-17) | `components/ProfileEditor.jsx:383–390` | `stageOfGrade` | ★ **THE `portalStage` HALF LANDED 2026-09-17, AND IT WAS THE FOUNDER'S REPORT**: the phone had only `allowedStagesFor` (the entitlement half), so a scoped check window offered the settled stage's classes too. Now both filters, in the web's own order, with the `haveGrades` escape on the entitlement one and deliberately none on the stage one. |
| 36 | `.trial-note` on the classes wheel when scoped (same sentence as row 27) | TeachingProfile.jsx:1158-1163 | **DONE** | `components/ProfileEditor.jsx:737–741` | — | — |
| 37 | Manage-classes confirm modal: h2 "Remove {Class n, …} from {subject}?", body "{9A, 9B} — their cards and bookmarks — will be removed.{ No class is left — {subject} goes with it.} Your lessons stay in the library.", CTA "Yes, remove {names}", "Keep it/them" re-ticks | TeachingProfile.jsx:1167-1192 | **DONE** | `components/ProfileEditor.jsx:937–975` | `Sheet confirm` | ★ The last-class clause now reads "{subject} itself stays, with no classes" when she OWNS the subject (`subjectSurvivesEmpty`, :962) — 2026-09-17. "Keep it/them" re-ticks (:967). |
| 38 | `applyClassChanges`: clears removed classes' section state; adds applied with defaults; last class + no adds → subject removed | TeachingProfile.jsx:693-718 | **DONE** | `components/ProfileEditor.jsx:426–495, :497–512` | rows 12, 14, 46 | ★ **THE LAST-CLASS RULE CHANGED 2026-09-17**: the subject goes only if she does NOT hold a live paid scope for it. See rows 97–99. |
| 39 | Manage-mode adds applied WITHOUT the per-class run: `sections:["A"]`, `DEFAULT_DURATION`, ppw from `/ncf-periods` (`ppwFromAnnual`) or `DEFAULT_PPW`, budget `{periods, annual}` or null, sorted by ROMAN | TeachingProfile.jsx:662-684 | **DONE** | `components/ProfileEditor.jsx:435–450` | row 11, shared `ppwFromAnnual` (format.js:328) | Async: awaits all `/ncf-periods` fetches before saving; failure is silent (null → flat defaults). All `/ncf-periods` awaited first (:428–432); failure is silent (null → flat defaults). |
| **E. Per-class conversational run (ADD path only)** |||||||
| 40 | Kicker "{subject} · Class {n} · {i} of {pending}" | TeachingProfile.jsx:1200 | NOT-PORTED-BY-DECISION | — | — | Reached from row 31 (a NEW subject) only. ★ **2026-09-18 reconcile:** the phone's add-a-subject journey is subject → class wheel → **Save**, and each added class arrives SET UP (Section A, default length, calibrated ppw/budget — `ProfileEditor.jsx` `applyClasses`), exactly as a manage-mode add does. The web instead runs this per-class conversation. Nothing on the phone asks it. The set-up check window still fires for the new subject·class (the layout's readiness diff queues it), so the assumptions ARE disclosed — but this is a product divergence, not a technical limitation, and needs the founder's say-so (open question 8). ★ **2026-09-18 reconcile:** **ANSWERED — founder Q8, 2026-09-18: there is no such journey.** "+ add a subject" is SUBSCRIBING to one, on both surfaces (web → SubscribeFlow, phone → `/subscribe`). Checkout lands the subject with defaults; the set-up check asks about them on her first My Lessons visit. The web's per-class run is unreachable now (entry point cut in `TeachingProfile.jsx`; the code is left for deletion with a live check). |
| 41 | Step `sections`: h1 "Which sections of Class {n}?", hint "Every ticked section gets its own class card and its own bookmark.", PickWheel over A…Z with `leadingHeader="Section"`, `trailingHeader="customize"`, `labelFor` "{n}{L}", `summaryFor` "{n}{L} (Name)", trailing `SecNameCell`; CTA **Continue** (disabled until ≥1); back link | TeachingProfile.jsx:1202-1229 | NOT-PORTED-BY-DECISION | — | PickWheel two-column mode (§H), row 17 | The customize box: `TextInput maxLength={8}`, not autofocused (114-117). ★ **2026-09-18 reconcile:** see row 40. The SECTIONS screen itself exists (row 47, with the customize column); what is absent is reaching it as step 1 of an add. ★ **2026-09-18 reconcile:** **ANSWERED — founder Q8, 2026-09-18: there is no such journey.** "+ add a subject" is SUBSCRIBING to one, on both surfaces (web → SubscribeFlow, phone → `/subscribe`). Checkout lands the subject with defaults; the set-up check asks about them on her first My Lessons visit. The web's per-class run is unreachable now (entry point cut in `TeachingProfile.jsx`; the code is left for deletion with a live check). |
| 42 | Step `ppw`: h1 "How many periods a week does Class {n} get for {subject}?", hint "A number, not a timetable — Meyy never asks which days.", `PpwTotalWheel`, foot: **Continue** → durations, "← Back" → sections | TeachingProfile.jsx:1231-1252 | NOT-PORTED-BY-DECISION | `components/ProfileEditor.jsx:780–800` | `setPpwTotal`, `normPpw`, `ppwMapSum` | `PpwTotalWheel` = base RollWheel `large` with chip = the number and label "period a week"/"periods a week"; the phone's base wheel already draws a chip (rw_chip) — pass `rowPx` 64 and measure `.fr-wheel-lg .fr-wheel-label` (globals.css:2399) into `rw_label_lg`. The wheel and its arithmetic exist — but as the `editNums` step (row 52). The per-class ADD run this row belongs to does not exist, and the heading is the short "How many periods a week?". ★ **2026-09-18 reconcile:** see row 40. The ppw wheel exists as the editNums step (row 52). ★ **2026-09-18 reconcile:** **ANSWERED — founder Q8, 2026-09-18: there is no such journey.** "+ add a subject" is SUBSCRIBING to one, on both surfaces (web → SubscribeFlow, phone → `/subscribe`). Checkout lands the subject with defaults; the set-up check asks about them on her first My Lessons visit. The web's per-class run is unreachable now (entry point cut in `TeachingProfile.jsx`; the code is left for deletion with a live check). |
| 43 | Step `durations`: h1 "How long are your {subject} periods for Class {n}?", hint single "If more than one duration, select multiple." / multi "Split your {total} periods between the lengths — the shortest one takes whatever is left over.", PickWheel over 20…120 min, `initialScrollTo=40`, headers "Duration"/"Periods / week" only when multi, `summaryFor` "{d} min × {k}", trailing `PpwSplitCell`; last duration cannot be unticked; CTA **Continue** commits map/anchor/total; "← Back" → ppw | TeachingProfile.jsx:1254-1294 | NOT-PORTED-BY-DECISION | `components/ProfileEditor.jsx:802–849` | PickWheel two-column, `PpwSplitCell` port (§H), `lowestDuration`, `setPpwSplit` | The duration step, both hints, `DURATION_CHOICES`, `initialScrollTo`, the two column headers and the trailing split cell all exist; what is missing is the per-class ADD run this step belongs to. ★ **2026-09-18 reconcile:** see row 40. The duration step exists (row 53). ★ **2026-09-18 reconcile:** **ANSWERED — founder Q8, 2026-09-18: there is no such journey.** "+ add a subject" is SUBSCRIBING to one, on both surfaces (web → SubscribeFlow, phone → `/subscribe`). Checkout lands the subject with defaults; the set-up check asks about them on her first My Lessons visit. The web's per-class run is unreachable now (entry point cut in `TeachingProfile.jsx`; the code is left for deletion with a live check). |
| 44 | Step `budget` (per-class run): heading "How many periods for Class {n} this year?", value opens on `normalizeBudget(g.budget, ppw, recTotal)`, ±1 stepping floored at 1, pencil → ppw step, foot **"Save ✓"** (last class) / **"Next class →"**, "← Back" → durations | TeachingProfile.jsx:1296-1318 | NOT-PORTED-BY-DECISION | — | rows 68-80, shared `normalizeBudget` | Same renderer as the editNums budget. ★ **2026-09-18 reconcile:** see row 40. The budget renderer exists (row 54). ★ **2026-09-18 reconcile:** **ANSWERED — founder Q8, 2026-09-18: there is no such journey.** "+ add a subject" is SUBSCRIBING to one, on both surfaces (web → SubscribeFlow, phone → `/subscribe`). Checkout lands the subject with defaults; the set-up check asks about them on her first My Lessons visit. The web's per-class run is unreachable now (entry point cut in `TeachingProfile.jsx`; the code is left for deletion with a live check). |
| 45 | `onClassDone`: advance `pi`, else `finalizeSubject(draft)`; more subjects queued → `subjectDone`, else view | TeachingProfile.jsx:831-836 | NOT-PORTED-BY-DECISION | — | row 16 | — ★ **2026-09-18 reconcile:** see row 40. ★ **2026-09-18 reconcile:** **ANSWERED — founder Q8, 2026-09-18: there is no such journey.** "+ add a subject" is SUBSCRIBING to one, on both surfaces (web → SubscribeFlow, phone → `/subscribe`). Checkout lands the subject with defaults; the set-up check asks about them on her first My Lessons visit. The web's per-class run is unreachable now (entry point cut in `TeachingProfile.jsx`; the code is left for deletion with a live check). |
| 46 | Screen `subjectDone`: kicker "Teaching profile", `.fr-ready-note` "✓ {subject} saved." + "You can continue now, or come back for the rest later.", CTA "Continue to {next} →", link "Finish for now" | TeachingProfile.jsx:1321-1340; globals.css:2487 | NOT-PORTED-BY-DECISION | — | — | Only with ≥2 subjects added at once. ★ **2026-09-18 reconcile:** only reached when two or more subjects are added at once; the phone adds one per journey (row 22). |
| **F. Sections editor (portal "Section" row)** |||||||
| 47 | Screen `editSections`: kicker "{subject} · Class {n} · sections", h1 "Edit sections of Class {n}", hint "Tick to keep or add a section, untick to remove one. A removed section loses its bookmark — your lessons stay in the library. To remove the whole class, use the basket on the class.", PickWheel A…Z pre-ticked, cluster ON, customize column with existing names seeded, CTA **Save** (disabled at 0 picks), back link | TeachingProfile.jsx:848-853, 1368-1399 | **DONE** | `components/ProfileEditor.jsx:743–777` | PickWheel two-column, row 17 | Note the hint still says "use the basket on the class" though the class basket is gone (dead `confirm`); web copy defect to flag, port verbatim or ask (Q4). ⚠️ One-sentence hint ("Tick to keep or add a section, untick to remove one.") — the removal consequences moved into the confirm. |
| 48 | `requestEditSections` → `secConfirm` when any enrolled letter is unticked; `applyEditSections` clears removed sections' state, writes `secObj` with names, regenerates `grids`, persists | TeachingProfile.jsx:856-876 | **DONE** | `components/ProfileEditor.jsx:513–548` | rows 12, 14 | — |
| 49 | Sections removal confirm: h2 "Remove {9A, 9C}?", body "Its card and bookmark"/"Their cards and bookmarks" + " will be removed. Your lessons stay in the library.", CTA "Yes, remove {tags}", link "Keep it/them" (does NOT re-tick — closes only) | TeachingProfile.jsx:1401-1410 | **DONE** | `components/ProfileEditor.jsx:976–996` | `Sheet confirm` | Different "Keep" behaviour from rows 28/37 — port as is. "Keep it/them" closes WITHOUT re-ticking here — the row-28/37 difference is preserved. |
| 50 | Screen `addSection` ("Add sections to Class {n}", "You already have {tags}. Tick the new ones.", CTA Save) | TeachingProfile.jsx:839-845, 877-889, 1342-1366 | NOT-PORTED-BY-DECISION (dead: `startAddSection` has no caller) | — | — | Do not port. |
| **G. Numbers editor (portal "Periods a week" / "Annual period budget" rows, Year Plan pencil)** |||||||
| 51 | `startEditNums(si, gi, step)` loads a draft + the stored budget; `saveEditNums(finalBudget)` writes durations/ppw map/anchor/total and `budget[gi]` (fallback chain → `{auto,0}`), persists, view | TeachingProfile.jsx:891-916 | **DONE** | `components/ProfileEditor.jsx:303–321` | row 12 | `setGradeBudget` covers the budget-only save (Year Plan pencil, portal budget row when ppw untouched); the ppw+duration save needs a sibling `setGradePpw(subjects, subject, grade, {durations, ppw_by_duration, ppw_anchor})` in shared. ⚠️ The two save separately (no combined `saveEditNums`), and the exit is `closeEdit()` rather than a "view" screen. |
| 52 | Step `ppw` (editNums): kicker "{subject} · Class {n} · periods / week", h1 "How many periods a week?", hint "A number, not a timetable — you’ll set the period lengths next.", `PpwTotalWheel`, foot **Continue** → duration step, link **Cancel** → view | TeachingProfile.jsx:1468-1493 | **DONE** | `components/ProfileEditor.jsx:780–800` | row 42 | The ONLY way into the duration screen (1377-1380). ⚠️ No "Cancel" link — the ✕ corner is the only exit (:599). |
| 53 | Step `duration` (editNums): kicker "… · duration", h1 "How long are the periods?", hint single/multi ("… — {anchor} min takes whatever is left over."), PickWheel `initialScrollTo=g.durations[0]`, trailing split cells, CTA **Save** (commits + `saveEditNums()`), "← Back" → ppw step | TeachingProfile.jsx:1420-1466 | **DONE** | `components/ProfileEditor.jsx:802–849` | row 43 | ⚠️ "← Back" is the window's ← CORNER, not a footer link. |
| 54 | Step `budget` (editNums): kicker "… · annual budget", heading "How many periods for the year?", pencil → ppw step (stays inside the editor), foot **Save** (`saveEditNums(b)`), link **Cancel** | TeachingProfile.jsx:1495-1513 | **DONE** | `components/ProfileEditor.jsx:851–926` | rows 55-60 | The Year Plan pencil's destination — the first screen 5c should light. This step is the Year Plan pencil's destination (row 4); its own sense-check pencil goes to `ppw` and returns (:909–914). |
| 55 | Budget renderer: kicker (`.kicker.kicker-ochre`), `h1.fr-q` heading, NO sub-hint | TeachingProfile.jsx:780-792 | **DONE** | `components/ProfileEditor.jsx:626, :855` | — | `.kicker-ochre` has no CSS rule anywhere (grep) → the kicker renders as plain `.kicker` pine; use `ws.kicker`. |
| 56 | Value row `.tp-val-row.tp-val-solo`: round **−** button (aria "Fewer periods"), `input[type=number] min=1` (aria "Annual period budget"), **+** ("More periods"), unit "periods / year" | TeachingProfile.jsx:793-800; globals.css:3028-3041 | **DONE** | `components/ProfileEditor.jsx` budget step; web.js `tp_val_row/tp_val_btn/tp_val_btn_t/tp_val_input/tp_val_unit` | shared `clampPeriods` (budget.js:72) | ⚠️ `keyboardType="number-pad"` + `clampPeriods` on change and blur stand in for `input[type=number] min=1`. ★ **2026-09-18 reconcile:** the row had been mangled by an earlier edit (two status cells, a truncated note); rewritten, status unchanged. |
| 57 | Sense-check caption `.tp-weeks`: "{weeks} weeks (@ {ppw} periods/week)" + pencil button (title/aria "Change periods a week for this class") — rendered only when `annual>0 && ppw>0` | TeachingProfile.jsx:802-811; globals.css:3050-3064 | **DONE** | `components/ProfileEditor.jsx:906–916` | shared `weeksFromAnnual` (format.js:330), `budgetPeriods` | Pencil = 13px SVG (138-144); port as an inline `Pressable` with the Pencil path in react-native-svg. From the Year Plan door the pencil leads to the ppw step (row 52) — so 5c needs rows 52-53 too, or ships the budget screen without the pencil (Q5). ⚠️ The pencil reuses `ws.yp_budget_edit`; no `tp_implies_edit` measure was added. |
| 58 | Recommendation line `.tp-estimate-sub`: "Meyy recommends {rec} periods a year based on general norms for this class." + " (NCF norm: {ncf})" when ≠; fallback "As per NCF, this class requires {ncf} periods."; nothing when neither | TeachingProfile.jsx:751-761, 812; globals.css:3042 | **DONE** | `components/ProfileEditor.jsx:269–273, :917` | row 11 | Nothing renders when neither figure exists — the `metaErr` rule. |
| 59 | Foot `.fr-foot` (+88 px in `.tp-budget`): primary `.fr-cta` **Save** and `.fr-link` **Cancel** | TeachingProfile.jsx:1507-1512; globals.css:3072, 3340-3345 | **DONE** — named divergence | `components/ProfileEditor.jsx:918–925` | — | `.primary` colour (pine fill, `#fdfaf4` text) must be applied by the component. The foot and the pine Save (with a spinner while saving) are built; the `.fr-link` **Cancel** is absent BY DECISION — the ✕ corner replaced every footer Cancel (2026-09-15). |
| 60 | Budget value on open = `normalizeBudget(stored, ppw, recTotal)` — legacy weeks/days/auto read, `{auto,0}` = unset → calibrated → ppw×30; saved back ONLY as `{method:"periods", value}` | TeachingProfile.jsx:1299-1300, 1495-1496; shared budget.js:62-69 | DONE (arithmetic) | packages/shared/src/budget.js | — | The four read shapes: `weeks` → ppw×v · `periods` → v · `days` → round(ppw×v/6) · `auto`/other → v or ppw×30 (budget.js:36-42). Second reader `annualBudgetPeriods` (format.js:333-357) is what YearPlan/prepare use — on `periods` they agree; on legacy shapes they can differ (budget.js:23-29 records this). |
| **H. View — the accordion (Settings door only)** — ✅ **BUILT READ-ONLY 2026-09-16 (step 6d)**: `mobile/app/(app)/settings/profile.jsx`, reached from the Settings home card. Derived numbers live in `@aruvi/shared/profile` (`secCount`, `gradePpw`, `subjectPpw`, `profileStats`, `classCard`, `classCards`) and **the web reads them too** — 12 tests in `packages/shared/test/profileView.test.js`. Edit mode (62 pencil · 66 dustbin · 68 add · 69 confirms) is deferred as one piece. |||||||
| 61 | Back button `← back` (`.back.back-tr`, right-aligned) when `onBack` | TeachingProfile.jsx:1552-1556; globals.css:931-932 | NOT-PORTED-BY-DECISION | — | — | With the portal bounce (row 6) this never shows on a portal visit; under Settings `onBack` is null → never shows either. Effectively dead; skip. ★ **2026-09-18 reconcile:** dead on the web too (never shows on a portal visit or under Settings). |
| 62 | ~~Header pencil ⇄ **Done**~~ **RETIRED ON THE WEB TOO, 2026-09-16** (founder: "remove the pencil in web app and let us think of better way to delete subject"). It revealed two things and the phone could copy neither without hiding its only add door behind a control with nothing else to offer. `editing` state, the pencil, the Done toggle and the dustbin's trigger are gone; `applyRemoveSubject` and the two-step confirm STAY, unreachable, because the cascade is the expensive and risky part and re-deriving it from memory is how that gets got wrong. Removal is being redesigned. Header: `h1.lvl-title` "Your teaching profile" | TeachingProfile.jsx:1557-1565; globals.css:2859-2880 | N/A — GONE ON BOTH | — | entitlement lapsed flag | **No heading**: the frozen Settings bar reads "⚙ Teaching profile" and the web deleted its own `set-title` when that bar landed (app. 04 A3-A5). **No pencil**: it opens edit mode, and edit mode is rows 66 + 68 — the dustbin AND "+ add a subject". The phone has neither, and shipping only the dustbin is the exact shape the web tried on 2026-08-27 and retired the same week ("a control that promises editing and delivers only deletion is a trap"). So the phone offers the reading, not the half. |
| 63 | Empty state `.tp-empty` "No profile yet — add a subject to begin." | TeachingProfile.jsx:1567-1575; globals.css:2881 | DONE | settings/profile.jsx | — | The phone says **"No profile yet."** and stops. The web's second clause is true there because its next line is the row that does it (row 68); here there is no such row, so the sentence stops where the phone can actually deliver. Restore the full string the day row 68 lands. ★ **2026-09-18 reconcile:** the full string is restored now that row 68 has landed — "No profile yet — add a subject to begin." (`settings/profile.jsx`:173; the short form only when lapsed, as on the web). |
| 64 | Stats tiles `.tp-stats` ×4: `{n}` **Subjects** · **Classes** · **Sections** · **Periods / week** (ppw × sections per class) | TeachingProfile.jsx:1536-1544, 1577-1584; globals.css:2885-2889 | DONE | settings/profile.jsx `Stat`; `profileStats` in shared | web.js `tp_stats/tp_stat/tp_stat_n/tp_stat_l` | ★ The arithmetic moved to **`@aruvi/shared/profile`** and **the web was changed in the same commit to read it** — classes and sections are counted as SETS across subjects (one Class 6, however many subjects she takes it for). ⚠️ Measured in a **394px** iframe so 390 of content remained: the ≤400px rules are real here (gap 6 not 7, padding 10/7 not 11/9, `tp_stat_n` 19 not 21, `tp_stat_l` 8 not 8.5). Walked: 3 / 4 / 7 / 41 on both surfaces. |
| 65 | Subject row `.tp-sub` / `.tp-sub-hd`: name (display serif), `{n} periods / week` (per section), caret "▾"/"▸"; tap toggles; only ONE open; open row = tinted band + clay spine + sticky | TeachingProfile.jsx:1587-1624; globals.css:2892-2923 | DONE | settings/profile.jsx | web.js `tp_sub/tp_sub_hd/tp_sub_hd_open/tp_spine/tp_sub_name/tp_sub_ppw/tp_caret` | The spine is a `View`, not a `::before` — 3px, inset 13 top and bottom, rounded on its right edge only. `sticky` is unported: the phone's list is short and a sticky header inside a plain ScrollView would buy a `SectionList` for three rows. ⚠️ `▸` (U+25B8) is its FIRST use on this app — `▾` is proven (Dropdown), and they share a Unicode block, but worth an eye on the handset given the `⚙`/`⌄` findings. |
| 66 | Subject dustbin (`.tp-icon-btn.tp-icon-danger`, aria "Remove {s}") — was `editing && open` | TeachingProfile.jsx:1596-1614; globals.css:2975-2982, 3081-3082 | **REMOVED FROM THE WEB 2026-09-16**, never built on the phone | — | a new control, to be designed | ⚠️ **THERE IS STILL ONE DOOR, ON BOTH SURFACES, and it is not a gap**: unticking a subject's LAST class removes the subject, and the confirm says so in those words ("No class is left — English goes with it"). "+" → Class → untick all. What went is the DIRECT control, deliberately, until removal is given a design of its own; the two-step confirm copy and `applyRemoveSubject`'s cascade wait in the file for it. |
| 67 | Class card `.tp-classcard`: "Class {n}"; right "Sections" label + chips (`.tp-chip`, `.named` shows HER NAME ONLY, `title="Section {tag}"`); two columns "Periods / week per section" = "{k} × {d} min, …" (or "{ppw} a week", or "—") and "Annual budget" = "{total} periods" via `budgetPeriods` or "—" | TeachingProfile.jsx:1632-1675; globals.css:2929-2969 | DONE | settings/profile.jsx `ClassCard`; `classCard` in shared | shared `budgetPeriods` | Every value comes from **`classCard`** in `@aruvi/shared/profile`, which the web now reads too. ⚠️ At ≤400px `.tp-cc-cols` collapses to ONE column, so the phone STACKS the two lines — the split can run to two lines and squeezing it into 1.6fr is what the web's own media query exists to prevent. `title` becomes `accessibilityLabel` ("Section 3A"), which is what a screen reader announces either way. |
| 68 | "+ add a subject" row (`.tp-sub.tp-sub-add`, dashed edge, pine name, role=button, Enter/Space) — **shown whenever `!lapsed`** (the empty-profile / `editing` condition went with the pencil, 2026-09-16); opens the chooser as a window (row 68a) | TeachingProfile.jsx:1699-1709; globals.css:2991-2994 | DONE ✳️ ALWAYS SHOWN | settings/profile.jsx `AddSubjectRow` → `openEdit({intent:"subject"})` | rows 22, 27 | ★ **NOT BEHIND A PENCIL, and that is the divergence** (founder, 2026-09-16: "the edit button to add subject is missing in expo"). The web hides this row inside edit mode because its pencil reveals TWO things and removal is the other; the phone has no removal yet, so a toggle would hide the only door behind a control with nothing else to offer — and hiding the only way to add a subject is the dead end being fixed. Restore the toggle the day row 66 lands. ✅ **AND THE WEB FOLLOWED on 2026-09-16** — its pencil is gone and this row simply shows, so the two surfaces now agree on WHEN it appears; since 2026-09-17 they agree on WHAT it opens too (row 68a). `!lapsed` IS honoured (§2.5: the reading room keeps out what grows the account). Enter/Space is WEB-ONLY-BY-NATURE. |
| 68a | **The add-a-subject WINDOW** (new 2026-09-17): `{addWin && screen === "pickSubjects"}` → `.ap-overlay.tp-window` › `.ap-modal.tp-window-card` with `.ap-close` ✕ and a scrim-tap close, `pickSubjectsScreen()` inside | TeachingProfile.jsx:189-203, 478-487, 1711-1728; globals.css:5356-5405 (borrowed whole — nothing new was added) | DONE ✳️ NATIVE IDIOM | ProfileEditor.jsx via `openEdit({intent: "subject"})` → the shell's ONE `Sheet` | rows 22, 68 | ★ **NO NEW CSS, DELIBERATELY.** The card page.jsx already wraps a portal spot-edit in is sized for a two-column pick wheel, lifts the kicker clear of the ✕ (`.tp-window-card .tp { padding-top: 22px }`) and hides the flow's footer links because THE CORNERS DO THE NAVIGATING — so the chooser's own `.fr-link` back is hidden here and the ✕ is the exit, exactly as on the phone. ⚠️ The overlay is rendered by TeachingProfile itself, NOT by page.jsx: the component is already inside `.tp-window-card` on a portal visit, and `setAddWin(!fromPortal)` is what keeps the two from nesting. ⚠️ `.ap-overlay` is z 60 and `.tp-sticky` is z 5, so the pinned stats block haze correctly. On the phone none of this is a port — the Sheet IS the window, and it covers the whole journey rather than the chooser alone. |
| 69 | Remove-subject DOUBLE confirm (UNREACHABLE since 2026-09-16 — kept for the redesign): step 1 h2 "Remove {name}?", body "{Class 6, Class 7} — {k} section(s) will be removed from your teaching profile, along with every bookmark and chapter binding in them. This cannot be undone." + `.tp-rm-keep` "Your **lesson plans stay in the library** — removing a subject never deletes a plan.", CTA **Continue**; step 2 h2 "Are you sure?", body "This removes **{name}** and everything you have set up inside it. There is no way back.", CTA "Yes, remove {name}"; **Cancel** on both; scrim tap closes | TeachingProfile.jsx:1735-1780; globals.css:3077 | DEFERRED with row 62 | — | `Sheet confirm`, rows 62/66 | `applyRemoveSubject` (537-548) clears all section state, persists, exits edit mode. Ships with edit mode — see row 62. ★ **CLOSED 2026-09-18 — founder: no subject removal is needed.** "No need to remove a subject she has paid for — she can remove all its classes so it does not clog My Classes." The two-step confirm and `applyRemoveSubject` in `TeachingProfile.jsx` are now dead and go in the post-walk deletion pass. |
| 70 | Generic `confirm` modal (title/body/cta from `confirmCopy`, "Keep it") | TeachingProfile.jsx:444-475, 1782-1794 | NOT-PORTED-BY-DECISION (dead: `setConfirm({…})` is never called) | — | — | Strings retained: "Remove Section {tag}?" / "Its card and bookmark will be removed. It is the last section — Class {n} goes with it. Your lessons stay in the library." / "Remove Class {n} from {s}?" — vocabulary reused by rows 28/37/49. |
| **I. wheels.jsx — RollWheel** |||||||
| 71 | Base mode: one visible row (`rowPx` = snap step), `items [{id, chip?, label}]`, whatever settles is the pick (120 ms settle), clamped ▲▼ cue buttons (aria "Previous/Next {label}"), `large` label size | wheels.jsx:69-233; globals.css:2348-2410 | DONE | RollWheel.jsx:42-219 (base = `!peek`) | web.js:520-529 `rw_*_base`, `rw_chip*` | `large` not a prop on the phone — Prepare uses rowPx 92/clamp 2; PpwTotalWheel needs a `large` label measure (`.fr-wheel-lg .fr-wheel-label`, globals.css:2399). |
| 72 | Peek mode: white compact box, ×3 loop, recentre after settle, single cycling ▼ | wheels.jsx:74-86, 99-106, 122-141, 218-223 | DONE | RollWheel.jsx:47-56, 106-133, 139-148, 196-202 | web.js:509-515 | — |
| 73 | ArrowUp/ArrowDown keyboard step; `role=listbox` + `aria-activedescendant` | wheels.jsx:143-146, 202-204 | WEB-ONLY-BY-NATURE (named in RollWheel.jsx:28-30) | — | — | `accessibilityLabel` on the shell only; consider `accessibilityRole="adjustable"` + `accessibilityActions` increment/decrement for VoiceOver parity. |
| 74 | Default-to-first when `value` not in `items`; reposition on `[items]` | wheels.jsx:149-159 | DONE | RollWheel.jsx:62-89 | — | — |
| 75 | One-time gesture demo (22 px rock at 500/1000 ms) | wheels.jsx:57-59, 162-172 | DEFERRED (named RollWheel.jsx:192-195: "a thumb on a phone finds that out by touching it") | — | — | Also the `fr-cue-bob` animation is dropped. |
| 76 | Auto-fit (`fit`): canvas-measure longest label at design size, shrink to ≥12 px | wheels.jsx:5-14, 87-91, 174-199 | DONE (native answer) | RollWheel.jsx:184-185 `adjustsFontSizeToFit minimumFontScale 0.7` (single-line only) | — | — |
| 77 | `stepScroll` reduced-motion + 240 ms snap fallback | wheels.jsx:36-55 | DONE (native answer) | RollWheel.jsx:150-160 (commit-before-scroll) | — | — |
| **J. wheels.jsx — PickWheel (the multi-select every profile screen uses)** |||||||
| 78 | Fixed 4-row window (52 px rows, 208 px), scroll-snap, tap any row toggles; row = `button.fr-sec-opt` with `.fr-sec-check` "✓" and `.fr-sec-label` | wheels.jsx:293-406 (rows 363-372); globals.css:3110-3150 | **DONE** | `components/PickWheel.jsx:149–190` | none (pure RN ScrollView + Pressable) | New `mobile/components/PickWheel.jsx`: ScrollView `snapToInterval={52}`, rows as Pressable, `accessibilityState={{selected}}`. Measure `.fr-sec-wheel`, `.fr-sec-opt`, `.on` fills, `.fr-sec-check` box (3116-3117) into web.js `fs_*`. |
| 79 | `clusterOrder`: ticked options gather in one ascending run at the lowest pick's slot; unchosen between lowest and highest DROPPED; wheel rests on `start`; `cluster={false}` opt-out | wheels.jsx:257-291, 301-303 | **DONE** | `@aruvi/shared/pick.js:29`, used at `PickWheel.jsx:43, :85` | — | Pure; move to shared (`wheels.js`) — the founder's reversals (1098-1104, 1337-1339) make the per-screen `cluster` flag load-bearing. The `cluster={false}` opt-out is used by the subjects wheel (`ProfileEditor.jsx:668`) — the swallowed-Mathematics rule. |
| 80 | `pick(o, wasOn)` → `onToggle` then `restOn(nextStart)` (rAF + smooth scroll; reduced-motion) | wheels.jsx:311-336 | **DONE** | `components/PickWheel.jsx:101–105` | — | `scrollTo({y, animated:true})` after `setTimeout(0)`. Re-rests on the NEXT selection's cluster start, not the current one. |
| 81 | Running summary `.fr-pick-summary` (`role=status aria-live=polite`): "Chosen ({n}): **{summary}**" / "Nothing chosen yet — tap the rows above"; `summaryLabel={false}` suppresses | wheels.jsx:338-341, 388-394; globals.css:3131-3133 | **DONE** | `components/PickWheel.jsx:194–202` | — | `accessibilityLiveRegion="polite"`. |
| 82 | `children` = the CTA rendered INSIDE the wheel column (same width as rows) | wheels.jsx:387 | **DONE** | `components/PickWheel.jsx:192` | — | Every CTA in the editor is passed as `children` (`ProfileEditor.jsx:670, :716, :764, :842`). |
| 83 | ▲▼ side arrows (`.fr-sec-arrows-side`, bare glyphs, aria "Scroll up/down") only when `ordered.length > 4`; ArrowUp/Down keys | wheels.jsx:304-309, 396-403; globals.css:3247-3256 | **DONE** | `components/PickWheel.jsx:124, :205–216` | — | Keep the arrows: the web note says "phones aren't always obviously drag-scrollable" (236-238). Keyed off the CLUSTERED visible list, not `options`. |
| 84 | `initialScrollTo` (rest on a seeded option when nothing is selected; an existing selection wins) | wheels.jsx:343-351 | **DONE** | `components/PickWheel.jsx:107–118` | — | — |
| 85 | Two-column mode: `trailing(option, isSelected)` cell, `leadingHeader`/`trailingHeader` `.fr-sec-colhead`, row becomes `div.fr-sec-optrow` (button + `.fr-sec-trail`), `summaryFor` override, `.has-trail` heights | wheels.jsx:246-255, 356-360, 374-384; globals.css:3152-3183 | **DONE** | `components/PickWheel.jsx:59–61, 136–145, 162–190` | — | Used by sections (customize) and durations (ppw split). Used by both the sections and the durations steps. |
| **K. wheels.jsx — periods/week capture** |||||||
| 86 | `PpwTotalWheel` = RollWheel `large`, aria "Periods per week", items 1…14 with `chip=p` and label "period a week"/"periods a week" | wheels.jsx:518-524 | **DONE** | `components/ProfileEditor.jsx:785–789` | row 71 | — |
| 87 | `PpwSplitCell`: anchor row → bare `.fr-ppw-num`; other selected rows → `.fr-ppw-sel` chip (aria "Periods a week at {d} minutes: {v}", `aria-haspopup=listbox`) opening a position:FIXED `.fr-ppw-pop` listbox of 0…total, flips above when tight, closes on select/Escape/outside/scroll/resize; `.answered` styling once >0; nothing when `!show || !selected` | wheels.jsx:544-610; globals.css:3202-3245 | **DONE** | `components/PpwSplitCell.jsx:37–61` | — | Native answer: the chip opens a small `Sheet`-style bottom picker or an inline base `RollWheel` of 0…total (the web's reason for a custom listbox — macOS native select — does not apply; an RN `Modal` list is the honest port). Escape/scroll-close are WEB-ONLY-BY-NATURE. ⚠️ The fixed-position popup listbox is replaced by an INLINE 0…total strip owned by the editor (`ProfileEditor.jsx:818–840`) — the editor is itself a window now, and a window over a window was the 2026-09-15 defect. |
| 88 | `SecNameCell` customize input (`.tp-secname`, maxLength 8, aria "Your own name for section {tag} (optional, up to 8 characters)", stopPropagation) | TeachingProfile.jsx:96-105; globals.css:3192-3199 | **DONE** | `components/SecNameCell.jsx:27–36` | row 85 | `TextInput` inside the trailing cell; `keyboardShouldPersistTaps="handled"` on the wheel ScrollView so a tap on a row still toggles while the keyboard is up. |
| 89 | Arithmetic: `DEFAULT_DURATION=40`, `DEFAULT_PPW=6`, `DURATION_CHOICES`, `PPW_CHOICES`, `ppwMapSum`, `lowestDuration`, `ppwAnchor`, `normPpw` (total-preserving), `setPpwSplit` (clamped, anchor absorbs), `setPpwTotal` (shrinks split to fit) | wheels.jsx:434-513 | **DONE** | `@aruvi/shared/ppw.js:30–105` | — | Lift into `packages/shared/src/ppw.js` (no JSX in them) — the same move 5c made for the budget; `prepare.jsx:149-165 seedRows` already re-derives a split from `ppw_by_duration` and would read the same constants. Lifted whole; the WEB re-exports from it. |
| **L. Shared budget module (step 5c, landed)** |||||||
| 90 | `budgetPeriods(ppw, b)` — the reader of all four shapes | shared budget.js:36-42 | DONE | packages/shared/src/budget.js | — | Consumers: TeachingProfile class card (1592), renderBudgetStep (751). |
| 91 | `normalizeBudget(stored, ppw, rec)` | shared budget.js:62-69 | DONE | — | — | — |
| 92 | `clampPeriods`, `findScope(subjects, name, roman)`, `setGradeBudget` (writer, `periods` only, NUMBER key), `gradeBudgetRecord` (either key spelling) | shared budget.js:72-111 | **DONE** (2026-09-18) | — | — | TeachingProfile still writes the budget inline at 891-894 (`{...budget, [gi]: budget}`) — the web should adopt `setGradeBudget` too once the mobile editor lands, so both write through one function. ★ **2026-09-18 reconcile:** the phone writes through `setGradeBudget` (`ProfileEditor.jsx` `save`). The WEB still writes `budget[gi]` inline (TeachingProfile.jsx:891-894); moving it onto the shared writer is the remaining half of "one writer". ★ **2026-09-18 reconcile:** the web's `saveEditNums` now writes through the shared `setGradeNumbers` + `setGradeBudget`, the phone's own writers; a ppw-only save leaves the stored budget untouched. |
| 93 | `web/app/lib/budget.js` re-export via `shared-setup` | web/app/lib/budget.js:1-5 | DONE | — | `@aruvi/shared/budget` subpath must be in the shared package's `exports` map (package.json not in this snapshot — verify) | mobile imports subpaths the same way (e.g. `@aruvi/shared/plans`). |
| 94 | `DAYS_IN_WEEK=6` (also used for the `grids` shape-compat fill) | shared budget.js:32; TeachingProfile.jsx:410-412, 825, 872, 886 | DONE | — | — | — |
| **M. Shared readiness store (what the phone reads/writes)** |||||||
| 95 | `cachedReadiness()` sync device copy, `fetchReadiness()` one revalidation per session, `invalidateReadiness()` after any profile write | shared readiness.js:83-153 | **DONE** — read AND write | `index.jsx:18`, `lessons.jsx:61`, `prepare.jsx:61`, `settings/profile.jsx:41` | — | Header (35-40) explicitly reserves `invalidateReadiness` for step 5's profile writes. ★ 2026-09-17: the write side is live, and the store is now SUBSCRIBED to by My Classes, My Lessons and Prepare — each is a route that read the profile once on mount, so a write from any door (a purchase, the portal's save, a change made on the web) sat in the store while the screen drew what it had. The subscribe wizard forces a read after checkout (`subscribe.jsx`), which is what makes a bought subject appear at all. |
| 96 | A shared `saveReadiness(subjects)` (POST + verify + store write-through) | — (web does it inline, TeachingProfile.jsx:379-386) | **DONE** | `@aruvi/shared/readiness.js:228` (`saveReadiness`), `:220` (`adoptReadiness`) | rows 12, 95 | Must exist before any phone write; recommended as the first 5c task after the budget screen UI. The three-outcome contract (ok / unverified / mismatch), plus the optimistic `adoptReadiness` first run needs. |
| 97 | `projectReadiness({subjects})` — page.jsx keeps the projection; the phone stores the raw `{subjects}` profile | shared format.js:386-401 | DONE (helper) — n/a on phone | — | — | The phone's store holds `d.readiness` (readiness.js:113) which already carries the projection from the server; after a local write, store `projectReadiness({subjects})` for shape parity. |

## Dependency notes

**Must land before this family can be completed on the phone**

- **A `saveReadiness()` in `@aruvi/shared/readiness.js`** (row 96): POST `/readiness {subjects, cascade:true}` → `verifiedWrite` with `readinessFingerprint` → on ok/unverified write the new profile into `mem` + device copy (`persist`) so My Classes / My Lessons / Prepare repaint without a round trip; on mismatch adopt the server copy and surface the row-13 banner. Without it, every screen in §C–G has nothing to save to. The web should then call the same function (its inline block at TeachingProfile.jsx:379-386 becomes the second caller).
- **Pure logic lifted out of the two JSX files into shared** (rows 15-17, 21, 79, 89): `gradeDraftFrom`/`finalizeSubject`/`secObj`/`cleanSecName`/`rekeyBudget`/`portalGradeIdxs` and the whole `normPpw`/`setPpwSplit`/`setPpwTotal`/`clusterOrder` family. They are already dependency-free; leaving them in `wheels.jsx` means the phone re-implements the anchor arithmetic that produced the "additive second duration" bug the web fixed once (wheels.jsx:414-433).
- **`mobile/components/PickWheel.jsx`** (§J, rows 78-85) — the single largest missing primitive; five of the seven portal/manage screens are a PickWheel with different props. Its two-column mode carries both `SecNameCell` and `PpwSplitCell`.
- **A native `PpwSplitCell`** (row 87) and a `large` base-wheel measure for `PpwTotalWheel` (rows 42, 86).
- **`mobile/theme/web.js` measures** for: `.fr-hint` (2330), `.fr-sec-*` (3110-3183), `.fr-ppw-*` (3202-3245), `.fr-sec-arrows-side/.fr-sec-arrow-btn` (3247-3256), `.tp-secname` (3192-3199), `.tp-portal-list/-row/-go` (3002-3013), `.tp-remove-confirm` (3015-3020), `.tp-implies-edit` (3058-3064), `.tp-rm-keep` (3077), `.fr-ready-note` (2487), `.fr-loading` (2341), `.tp-nomore` (2997), and — for the Settings door only — the accordion set `.tp-hd/.tp-edit-*/.tp-stats/.tp-stat*/.tp-sub*/.tp-classcard/.tp-cc-*/.tp-chip*/.tp-icon-*/.tp-sub-add` (2856-2994) and `.tp-savefail` (4991-5004). Run `node mobile/theme/check-parity.mjs` after — the `.fr-q` 27-vs-32 trap (web.js:625-627) applies to every `.fr-*` rule here.
- **The ProfilePortal window + origin store** (rows 2, 6): the "Add" bar item is a no-op until the window exists, and every screen in §B–G exits by re-raising it. A module-level `lib/portal.js` in the shape of `lib/preparing.js` (origin route + window mood + scope) is the phone's `portalOriginRef`.
- **Entitlement scopes on the phone** (rows 24, 35, 62): `fetchEntitlement` is already called (index.jsx:120); `paid_scopes` and `lapsed` must be exposed to the profile route for the scope filters and the read-only-when-lapsed rule. ★ **LANDED 2026-09-17 (5d·11), and it brought a SECOND reader of the same field**: `ProfileEditor` now subscribes to the entitlement store for `paidScopesOf` (the display filter) AND `heldScopesOf` (the ownership question). ⚠️ **They are not interchangeable and the difference is not cosmetic** — `paidScopesOf` returns null ("no limit") whenever enforcement is off, so asking it "has she bought this?" answers "no limit", which is not an answer; `heldScopesOf` reads `live_scopes`, excludes `"*"`, and returns `[]` for a trial. Any new caller must pick deliberately. New in `@aruvi/shared/format`: `heldScopesOf` · `holdsSubject` · `heldStagesFor` · `heldClassesFor`; in `@aruvi/shared/profile`: `subjectSurvivesEmpty`.
- **Settings (step 6)** gates the accordion door (§H) only. The three portal doors can ship first — which is the order the web itself took (the profile became "a view" on 2026-08-27; all edits live in the portal).

**What this family unlocks**

- Year Plan's budget pencil (YearPlan.jsx:24-29 — "a one-line wiring" once the budget step exists); the bar's **Add** item; the tour-end / added-subject "check your set-up" window; My Classes gaining or losing section cards from the phone; section renaming (chips on My Classes); the "prepare a new one" footer's future return path is unrelated but shares the origin-store pattern.

**Recommended 5c order** (matches what is already on disk): (1) budget editor screen = rows 54-60 with `setGradeBudget` + `saveReadiness`, reached only from the Year Plan pencil (`exact` scope, no pick screens); (2) origin store + `Sheet`-based ProfilePortal + the two pick screens (rows 19-21); (3) PickWheel + sections editor (rows 47-49); (4) manage-classes (rows 33-39); (5) ppw/duration editor (rows 52-53, 86-89); (6) accordion under Settings with step 6.

## Open questions for the founder

1. ✅ *ANSWERED — yes, both moments ship (row 3).* **Check-mood window on the phone.** The "Would you like to check your set-up?" window fires at tour end (tour NOT ported) and on first opening a newly added subject·class (added via SubscribeFlow, NOT ported — but a subject added on the web IS visible on the phone). Should the phone raise the added-subject check window at all in the beta, or only the "+" change mood?
2. **`portalIntent="subject"` / manage-subjects screen (row 23).** No window sends it any more on the web; the only live subject add is the accordion's "+ add a subject" (Settings door). Port the manage-subjects wheel, or drop it and port only add mode?
3. ✅ *ANSWERED — the Subscribe CTA ships, now that the wizard exists (row 25).* **Scoped empty chooser (row 25).** The web offers "Add a subject to my subscription" → SubscribeFlow. On a manual-grant beta phone, what replaces the CTA — the sentence alone, or "write to support@meyy.in"?
4. ✅ *RESOLVED — both surfaces now say only "Tick to keep or add a section, untick to remove one."* **Stale copy on the web (row 47).** The edit-sections hint still ends "To remove the whole class, use the basket on the class." — that basket no longer exists (the class dustbin was retired and `confirm` is dead code). Port verbatim, or amend on both surfaces ("…use Class in the Add window")?
5. ✅ *ANSWERED — HOLD (Q1 of the map), and both pencils lit together in 5d.* **The sense-check pencil inside the budget step (row 57).** From the Year Plan door the pencil leads into the periods-a-week wheel and then the durations screen — two more screens the 5c budget step would have to carry on day one. Ship the budget screen first WITHOUT the pencil (named divergence until rows 52-53 land), or hold it until the ppw editor is ported?
6. ~~**Removal of a subject she has BOUGHT (rows 38, 66).**~~ ✅ **ANSWERED 2026-09-17 — OWNERSHIP RUNS FOR THE TERM.** The founder took the second option: an owned subject is rightly permanent until the subscription lapses, and Meyy does not offer to take away a thing she bought. ★ **The cost was named and accepted with the rule** — a teacher who buys the wrong subject-stage looks at it for the rest of the year, and nothing on either surface will remove it. If that shows up in support traffic, the answer is a *"stop teaching this, keep the subscription"* door in the removal redesign (row 62) — **not** a change to `subjectSurvivesEmpty`, which answers "has she bought it?" and must not be softened into "does she want it on screen?". The decision is recorded on the rule itself, in `packages/shared/src/profile.js`. *Original question below.* **Removal of a subject she has BOUGHT (rows 38, 66).** Since 2026-09-17 a held subject survives losing its last class, and the accordion dustbin was retired on 2026-09-16 — so a subscriber now has **no way to take a subject out of her profile on either surface** until the subscription ends. That is the intended reading of "the line is ownership, not use", but it was decided for the CASCADE, not as a policy on removal. Should the removal redesign (row 62) give her a door — "stop teaching this, keep the subscription" — or is an owned subject rightly permanent for its term?
7. **Two budget readers (row 60, budget.js:23-29).** `budgetPeriods` (profile card, editor) takes `periods_per_week` as given; `annualBudgetPeriods` (Year Plan, Prepare) derives weekly periods from grid cells ÷ sections first. On a legacy weeks/days/auto record with a stale grid they can disagree, so Year Plan and the class card could print two annual totals for one class. Resolve now (Year Plan reads `budgetPeriods` too) or leave until a real record shows it?
8. ✅ *ANSWERED 2026-09-18 — adding a subject IS subscribing (see the model below).* **(NEW 2026-09-18) Adding a subject on the phone — ask about each class, or set it up and disclose?** (rows 31, 40–45.) The web walks a teacher who adds a subject through a per-class conversation: sections, periods a week, period lengths, the year's budget — for every class she ticked. The phone ticks the classes, saves, and gives each class Section A, the default length and Meyy's calibrated periods; the "check your set-up?" window then names those assumptions on her first visit. The phone's shape follows the portal's own rule ("each row changes only itself") and §0's benefit-first rule, and it is how an added class already behaves on BOTH surfaces in manage mode — so the question is really whether the web's add-a-subject run should stay conversational at all. Options: (a) keep the phone's shape and name it as a divergence; (b) move the WEB to it too (one behaviour, fewer screens); (c) port the web's run to the phone.

**★ THE SET-UP MODEL, as the founder stated it (2026-09-18) — the rule every door follows:**
1. **First-time trial**, web and phone: first run seeds defaults; "Would you like to check your set-up?" is asked on her first My Classes visit **after the tour** — not while it runs and not while it is still on offer (a later session counts as passed on).
2. **First subscription to a subject·stage** (checkout OR `entitlement.py grant`, which now seeds exactly as checkout does): it appears in My Lessons, in My Classes with a default class, and in the profile. The first time she opens it in My Lessons **or visits My Classes** — whichever comes first — she is asked whether she wants to change it (founder, same day: after a purchase she may land on My Classes and see a Class 3 · 3A she never chose). Asked once: whichever screen reaches it spends the key (`takeNextSetupCheck`, +1 test, 212 green).
3. **A subject emptied by removing its classes, subscription live:** it stays in the profile; she adds classes through Add › Class (Section A and the rest are that class's starting values); **no question is asked** — it is an existing subject. Shared rule: `setupCheckAdds` (a key asks only when its subject is new to her, or it is a newly bought stage of a subject she teaches).
4. **"+ add a subject" is always subscribing to a new one.** There is no journey that walks her through sections · periods a week · lengths · budget for a new subject, on either surface.
