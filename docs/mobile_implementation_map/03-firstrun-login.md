# Front door + First run (Login · PrivacyNotice · Agreement · FirstRun · shared legalmd/auth/signout)

Snapshot read: `/mnt/user-data/uploads/aruvi-saas`. Note: the snapshot carries NO `web/app/lib/format.js` /
`auth.js` / `legalmd.js` / `sectionState.js` / `plans.js` (only `budget.js`, `shared-setup.js`,
`useSupportedGrades.js`); per `docs/mobile_migration_plan.md:213` they are thin re-exports of
`packages/shared/src/*`, so every web `../lib/x` import below is read as `@aruvi/shared/x`.

## Web source map

- `web/app/components/Login.jsx` (351)
  - 26–36 `Benefits` — headline "Plan engaging, NCF-aligned lessons in seconds." + four ✓ bullets.
  - 40–49 `Bar` — `.fr-brand.ob-bar` › `.hdr` › `.brand` (MeyyMark + "lesson studio"); NO user block.
  - 51 `SEEN_KEY = "aruvi_device_seen"` (raw localStorage, not the shim).
  - 53–99 `Login({ onEnter })` state: `screen` choose|signin|otp|subscribe|privacy · `mode` trial|subscribe · `flow` create|return · `live = authEnabled()` · `otpLen = live ? OTP_LEN(6) : 4` · mobile/otpSent/otp/otpErr/otpBusy/mobErr/mobBusy · `otpRefs` · id/signinErr/inputRef · `privacyFrom` + `openPrivacy()`; effects: first-device → choose (85–89); desktop-only autofocus ≥601px (92–96); `markSeen`, `enter(id)`.
  - 101 `mobileOk = /^\d{10}$/`.
  - 104–118 `setOtpDigit` (digits only; whole-code paste → spread + focus last; else 1 digit, advance) · `otpKeyDown` (Backspace on empty → previous box).
  - 121–126 `requestOtp(num)` — stub returns true; live `sendOtp` → err into `mobErr`.
  - 130–148 `verifyOtp` — live `verifyOtpRemote` else stub `otp !== "0000"` → "That code didn't match. (Preview build: use 0000.)"; then `POST /onboarding/verified` with `authHeaders(num)` → `uid = d.user_id`; route: subscribe+create → `subscribe` screen, else `enter(uid)`.
  - 151–160 `subscribe` screen → `<SubscribeFlow userId chrome={<Bar/>} onDone=enter onCancel→otp onTrial→enter(mobile)>`.
  - 163–175 `privacy` screen — `.ob-wrap.ob-wrap-lock` › `.ob-body.ob-body-lock` › `<PrivacyNotice frame onBack→privacyFrom||"signin">`.
  - 178–208 CHOOSE screen — Benefits, "Choose what works for you", two `.ob-plan` toggle buttons (Free to try / Subscribe), foot: "Create sign in →" (sets flow=create, otpSent=false, otp="", screen=otp) + "Already have an ID? Sign in".
  - 211–291 OTP screen — title/sub, mobile field (+91, tel, maxLength 10, readOnly when flow=return), quiet lines, privacy sentence w/ link, `mobErr`, then EITHER "Generate OTP →" (create + !otpSent: `idInUse` → `MOBILE_TAKEN`, then `requestOtp`) OR the OTP boxes (`autoComplete="one-time-code"` on box 0; maxLength otpLen on box 0 else 1) + Resend (live) / "Preview build: enter **0000**." (stub) + `otpErr` + "Verify & continue →" (disabled `otp.length !== otpLen || otpBusy`); foot "← Back" → signin (return) | choose (create).
  - 296–350 SIGN-IN screen — `signinOk` = 10 digits OR email regex; `submitSignin` → `GET /onboarding/known?id=` → known: stub `enter(uid)`; live: `setMobile(uid); flow=return; mode=trial; sendOtp(uid)` → err into `signinErr` else `otpSent=true; screen=otp` · `ambiguous_email` sentence · unknown sentence · catch sentence. JSX: Benefits, `.ob-rule`, kicker "Sign in", h1 "Who's planning today?", form (label "Mobile number or email", placeholder "98xxxxxxxx or you@example.com"), "Enter →", `signinErr`, "🛡 Your data is private and secure · Privacy Notice", foot "New to Meyy? Get started →".
- `web/app/components/PrivacyNotice.jsx` (90)
  - 36–51 props `{version, onBack, backLabel="← Back", frame}`; bare `fetch(${API}/legal/privacy[?version=])` (no identity); failure sentence.
  - 53–54 fail `.lgl-fail` / loading "Loading the privacy notice…".
  - 56–89 render: `.lgl.lgl-privacy.(lgl-frame|lgl-read)` › `.lgl-head` (framed: Back button `.fr-link.lgl-back` ABOVE the title; `h1.ob-title` = doc.title || "Privacy Notice") › `.lgl-scroll` (older-version hint; `renderMarkdown(doc.body,"pn")` in `.lgl-agreement.lgl-privacy-body`; `.lgl-version` "Version X · {dateWords(published)} · English · This notice is available at any time under Settings › Legal.") › unframed foot Back.
- `web/app/components/Agreement.jsx` (293)
  - 15–28 `withNoticeLink(text,onOpen)` — finds "Privacy Notice" in the final tick's text and makes it a `.lgl-link` button.
  - 64–91 props `{mode="read"|"sign", userId, onAccepted, onBack, backLabel, context="subscription_checkout"}`; state ticks/final/marketing/busy/err/showPrivacy; load `GET /legal/consent` (with `authHeaders(userId)` on the front door, `getJSON` when signed in).
  - 93–99 fail/loading ("Loading the agreement…"); `allTicked` = every ack ticked AND final.
  - 101–135 `accept()` → `POST /legal/consent {version, acknowledgements[], final:true, context, language, marketing_email}` (front-door path uses authHeaders + `errDetail`; signed-in uses postJSON) → `onAccepted()`.
  - 140–143 `jumpTo(id)` scrollIntoView (DOM).
  - 145–292 render: `.lgl.(lgl-sign|lgl-read)` › `.lgl-head` h1 "Legal Agreement with User" › `.lgl-scroll` (read-mode accepted line "✓ Accepted on {date} · version {v}" + stale suffix, or "You haven't accepted this agreement yet…"; sign-mode prior-version hint; intro; `ol.lgl-acks` of five `.lgl-ack` (n, title, body, checkbox "I understand and agree."); `.lgl-agreement` body; `.lgl-final` final tick with notice link; `.lgl-optional` marketing tick (v0.4+); `.lgl-version`) › sign-mode frozen foot `.ob-foot.lgl-foot` (tally "Five points" + five `.lgl-box` jump buttons; `.lgl-need` "Confirm each of the five points and accept the full agreement to continue."; err; "I accept — continue →"/"Recording…"; Back) › `.lgl-sheet` privacy sheet (bar "Privacy Notice" + `.ap-close` ✕, body `<PrivacyNotice/>`).
- `web/app/components/FirstRun.jsx` (815)
  - 4–9 imports: shared `getJSON, postJSON, markPrepared, pretty, gradeUp, ROMAN, stageOfGrade, fetchEntitlement, ppwFromAnnual`; `bindSectionChapter, pushSectionState`; `invalidatePlans`; wheels `RollWheel, normPpw, ppwMapSum, lowestDuration, DEFAULT_PPW`; `MeyyMark`.
  - 81–105 constants: `DEFAULT_DURATION=40`, `DEFAULT_PERIODS=12` (fallbacks only), `DURATION_CHOICES` 20..120 step 5, `PERIOD_CHOICES` 1..60, `DAYS` (6, only to shape the all−1 grid), `classNum(g)` (ROMAN index+3).
  - 115–202 `FirstRun({ user, onComplete, onPrepared, onPrepareError, onExit, onSignOut })` state: `step` welcome|subject|grade|chapter · `firstName` from `GET /account` (117–130) · subjects/subject · grades/grade · chapters/chapterNo · durationMin/periods · defaultPeriods/stdDuration · `editingField` null|duration|periods · refs `periodsTouched`/`durationTouched` · `sections=["A"]` · `activating` · `annualBudget` · durations/ppwByDur/weekTotal/budget (only read by buildActivationPayload as fallbacks) · genonChs/canonMinutes/trialInfo.
  - 205–210 mount: `GET /subjects` → `subjects`; `fetchEntitlement()` (= `GET /entitlement`) → `trialInfo`.
  - 215–217 arriving on chapter step closes any open edit box.
  - 220–226 `GET /subjects/{subject}/grades` sorted by ROMAN.
  - 229–260 `GET /subjects/{subject}/{grade}/chapters` → chapters (drop `placeholder`), `stdDuration = standard_duration_minutes||40`, seed `durationMin` iff `!durationTouched`, `annualBudget = annual_budget_periods>0 ? … : null`; `GET /genon/{subject}/{grade}/chapters` → `genonChs`, `canonMinutes`.
  - 266–279 `--fr-bar-h` measurement of `.fr-brand` (DOM).
  - 282 `genonAvailable`; 295–299 `estimateFor(no)` = round(`recommended_periods`) || 12.
  - 305–314 `pickChapter(no)` — sets chapterNo, defaultPeriods, `periodsTouched=false`, periods, closes periods box.
  - 322–330 chapters-arrived effect — re-derive defaultPeriods; seed periods only if `!periodsTouched`.
  - 332–336 `chosenChapter`; `tagFor(letter) = \`${classNum(grade)}${letter}\``.
  - 352–374 `buildActivationPayload(over)` → `{ subjects:[{ name: pretty(subject), grades:[{ grade: gradeUp(grade), sections:[{tag,sec}], durations, ppw_by_duration: normPpw(...), ppw_anchor: lowestDuration, periods_per_week: ppwMapSum }], grids:[[[-1×6]]], budget:{0: bdg || {method:"auto",value:0}} }] }`.
  - 386–406 `finishActivation(over)` — clears `current_chapter_/lu_pointer_/lu_done_` for `${subject}_${grade}_${tag}`, `pushSectionState(secKey)` (deletes server row), then `onComplete(payload, over.preparing)`.
  - 421–490 `prepareAndHandOff()` — rows `[{duration: durationMin, count: periods}]`; descriptor `{subject, grade, chapterNo:Number, chapterTitle, rows}`; seeds `seededPpw = annualBudget ? ppwFromAnnual(annualBudget) : DEFAULT_PPW(6)`, `over = { durations:[durationMin], ppwByDur:{[durationMin]:seededPpw}, weekTotal:seededPpw, budget: annualBudget ? {method:"periods", value:annualBudget} : {method:"weeks", value:30}, preparing }`; `finishActivation(over)`; then (NOT awaited) `POST /genon/{subject}/{grade}/{chapterNo}/plan {rows}` → hold to 5000 ms → `markPrepared(subject, grade, resp.filename)` (= `POST /plans-prepared`) → `invalidatePlans(\`${subject}/${grade}\`)` → `bindSectionChapter(secKey, resp.filename)` → `onPrepared({subject, grade, filename})`; catch → `onPrepareError(preparing, e.detail || "Couldn't build the lesson plan right now. Try again in a moment.")`.
  - 493–505 `Progress` rail — `ol.fr-prog` of Subject · Class · Chapter, `.done` shows ✓, `.current`.
  - 521–545 `Brand` — `.fr-brand` › `.hdr` › brand lockup + `.hdr-user` › `.hdr-user-id` (firstName||user, "Log out" → onSignOut). No gear, no nav.
  - 553–563 subscribed-entry scope filter: `frPaidScopes` (enforced && active|grace && no "*") → `visibleSubjects` / `visibleGrades` by `stageOfGrade`.
  - 566–606 WELCOME; 609–629 SUBJECT; 632–671 CLASS; 697–814 CHAPTER (see inventory).

- `packages/shared/src/legalmd.js` (137): `parseInline` (28–50), `parseMarkdown` (52–128 → blocks h2/h3/p/ul/hr/table with `headText`), `dateWords` (132–137, `en-IN` "27 August 2026"). The web's `renderMarkdown(md, keyBase)` React renderer is NOT in shared (it lives in the web's thin `lib/legalmd.js`, absent from the snapshot).
- `packages/shared/src/auth.js` (96): `OTP_LEN=6`, `configureAuth`, `authEnabled`, `accessToken` (sync; falls back to `sb-*-auth-token` via the shim), `sendOtp` (E.164 `+91…`; sentences "Too many attempts — please wait a minute and try again." / "Couldn't send the OTP right now. Please try again." / "Sign-in service is not configured."), `verifyOtp` (type "sms"; "That code has expired — tap Resend to get a new one." / "That code didn't match. Please check and try again."), `signOutAuth`, `authHeaders(userId)` (Bearer else `X-Aruvi-User`).
- `packages/shared/src/signout.js` (49): `TEACHER_CACHE_PREFIXES`, `clearTeacherCaches(extraPrefixes)` — section cache, history, bank, plans, readiness, user, prefix sweep, `aruvi_user`.

## Mobile counterpart map

- `mobile/app/index.jsx` (8): `<Redirect href={getUser() ? "/(app)" : "/login"}>` — the sign-in gate only; NO first-run gate.
- `mobile/app/login.jsx` (223): header names the one divergence (no Subscribe card / SubscribeFlow). `SEEN_KEY` via `storage` shim (58); `MOBILE_TAKEN` redefined locally (29) with DIFFERENT words from the web's SubscribeFlow export. `Benefits` (31–38), module-level `Wrap` (42–51, KeyboardAvoidingView + Bar + ScrollView + pinned foot), `Login()` 53–215: `enter` (73–78: markSeen, `setUser`, `primeBank`, `router.replace("/(app)")`), `requestOtp` (82–87), `verifyOtp` (89–106, always `enter`), `submitSignin` (108–132, + `signinBusy`), CHOOSE (134–149), OTP (151–197), SIGN-IN (199–214).
- `mobile/app/privacy.jsx` (45): bare `fetch(${API}/legal/privacy)`, title, spinner, `Markdown`, "Version {v}", foot "← Back" → `router.back()`.
- `mobile/components/OtpBoxes.jsx` (46): boxes, paste spread (whole AND partial), backspace, `oneTimeCode`/`sms-otp` autofill, `autoFocus` box 0, `selectTextOnFocus`.
- `mobile/components/Markdown.jsx` (55): `parseMarkdown` blocks → Text/View; tables always stacked as cards.
- `mobile/components/ui.jsx` (60): `Button` (primary/link, `busy` spinner), `Link`, `Field`, `Input`, `Quiet`, `ErrorLine`.
- `mobile/components/Bar.jsx`: the ONE bar; `user` defaults to `getUser()` so it renders the identity + Log out (via `lib/session.endSession`) wherever a user exists — first run would get the identity block for free, gear present-but-inert unless `onSettings` passed.
- FirstRun: **none yet**. Reusable pieces: `mobile/components/RollWheel.jsx` base mode (`peek=false`: tint-pine shell `rw_shell_base`, ▲▼ clamped stepping, `chip`, `clamp`, `rowPx`; no `large`, no `fit`), `mobile/app/(app)/prepare.jsx` (the chapter wheel + `/chapters` + `/genon/.../chapters` fetch pattern, `CoverageNote`, the un-awaited serve + `startPreparing` + `router.navigate("/lessons")` handoff, `PREPARING_MS`), `mobile/lib/preparing.js` (`startPreparing/clearPreparing/failPreparing/paywallPreparing`, module-level store any route can watch), `mobile/components/ProposedCard.jsx` (the proposed card + 96 % Animated bar; consumed by `(app)/lessons.jsx:144–268, 444–452, 618–624`), `mobile/components/PrepareCta.jsx` (the clay→ochre "✦ Prepare …" identity, `size="primary"`).
- `mobile/app/(app)/index.jsx:255` currently prints "No classes yet — set up your teaching profile (first run comes in a later step)." for an empty profile — the placeholder where the gate must go.

## Inventory table

### A. Login — CHOOSE screen (web 178–208 · mobile 134–149)

| # | Feature / UI element | Web ref (file:lines) | Mobile status | Mobile ref | Depends on | Notes / native answer |
|---|---|---|---|---|---|---|
| 1 | First-device routing: no `aruvi_device_seen` → choose, else signin | Login.jsx:51, 85–89, 98 | DONE | login.jsx:28, 58, 74 | `storage` shim | Web reads raw `window.localStorage`; phone uses the shim (same key). Web decides in an effect (first paint = signin, then flips); phone decides in the initial state — no flash. |
| 2 | Bar (brand lockup only, no user block) `.fr-brand.ob-bar` | Login.jsx:40–49 | DONE | Bar.jsx (user=null pre-sign-in) | `hdr_*` in web.js | Web bar is un-fixed on these scrolling screens; phone Bar is in-flow — same. |
| 3 | Benefits headline "Plan engaging, NCF-aligned lessons in seconds." | Login.jsx:28 | DONE | login.jsx:33 | `type.headline` | Phone uses `type.*` roles, NOT measured `ob_*` keys — `.ob-headline` / `.ob-benefits` / `.ob-tick` are not in web.js yet. |
| 4 | Four ✓ bullets "Lesson plan in seconds, not hours" · "NCF / NCERT aligned" · "Assessment built in" · "Every section's status at one glance" (`&ensp;`-separated, ticks in `.ob-tick`) | Login.jsx:29–34 | PARTIAL | login.jsx:34–36 | — | Same words; phone joins with three spaces and un-styled ✓ (web ticks are a span with their own class, pine). Measure `.ob-tick`. |
| 5 | h2 "Choose what works for you" | Login.jsx:184 | DONE | login.jsx:141 | `type.h2` | — |
| 6 | Plan card "Free to try" — hd bold · sub "Try Meyy with no cost. Perfect to explore and get started." · points "Any 3 chapters · unlimited lesson plans per chapter · all core features to plan & assess" | Login.jsx:186–191 | PARTIAL | login.jsx:142–146 | `.ob-plan`, `.ob-plan-hd/-sub/-points`, `.on` | Web: a toggle BUTTON with `.on` state (`mode==="trial"`). Phone: a static View pre-styled as selected (tint_pine + pine border) because there is only one plan. Strings identical. `.ob-plan` measures not in web.js. |
| 7 | Plan card "Subscribe" — "Unlimited access to plan across your entire syllabus." · "Unlimited chapters · your full subject & stage, every class in it" (`.ob-plan-sub2`) + `mode` state | Login.jsx:55, 193–198 | NOT-PORTED-BY-DECISION | login.jsx:10–13 header | SubscribeFlow | Beta on manual grants (plan §0). No `mode` state on the phone. |
| 8 | CTA "Create sign in →" → flow=create, otpSent=false, otp="", screen=otp | Login.jsx:201–203 | DONE | login.jsx:137 | `Button` | Phone clears `otp`/`otpErr` via the screen-change effect (71) instead of inline. |
| 9 | Link "Already have an ID? Sign in" → signin | Login.jsx:204 | DONE | login.jsx:138 | `Link` | — |
| 10 | `.ob-wrap` / `.ob-body` / `.ob-foot` frame (foot pinned, hairline top) | Login.jsx:180,182,200 | DONE | login.jsx:42–51, 217–220 | — | KeyboardAvoidingView is the phone-added answer for a keyboard over the foot. `ob_*` measures not in web.js (hand-set 20/22/12). |

### B. Login — OTP screen (web 211–291 · mobile 151–197)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes |
|---|---|---|---|---|---|---|
| 11 | Title "Let's verify your mobile" · sub "We'll send you a one-time password (OTP) to sign in securely." | Login.jsx:216–217 | DONE | login.jsx:154–155 | — | Web uses `&rsquo;`; phone straight apostrophe. |
| 12 | Mobile field: label "Enter your mobile number", `+91` prefix `.ob-cc`, `type=tel inputMode=numeric maxLength=10`, placeholder "Enter mobile number", digits-only onChange, clears `mobErr` | Login.jsx:218–227 | DONE | login.jsx:156–163 | `Field`, `Input` | Phone adds `textContentType="telephoneNumber"` (capability the phone adds: number autofill). |
| 13 | Mobile field `readOnly` when `flow==="return"` | Login.jsx:223 | PARTIAL | login.jsx:160 | — | Phone locks ALSO once `otpSent` on the create path (`editable={flow!=="return" && !otpSent}`) — a divergence not named in the header: on the web she can still edit the number under the boxes. Founder call: which is right? |
| 14 | Quiet "We'll never share your number." | Login.jsx:228 | DONE | login.jsx:164 | `Quiet` | — |
| 15 | Legal line "By continuing you confirm you are 18 or older and have read Meyy's Privacy Notice." with `.lgl-link` button → `openPrivacy()` (remembers `privacyFrom`) | Login.jsx:232–234, 81–82 | DONE | login.jsx:165–168 | `/privacy` route | Web: in-state screen swap, every typed field survives; phone: `router.push("/privacy")` + `router.back()` — the Login screen stays mounted under the pushed route, so state survives too. |
| 16 | `mobErr` line (`.ob-err`, role=alert) | Login.jsx:236 | DONE | login.jsx:169 | `ErrorLine` | — |
| 17 | "Generate OTP →" (create path, before send): `idInUse(mobile)` → `MOBILE_TAKEN` else `requestOtp`; label "Sending…" (live) / "Checking…" (stub) while busy; disabled `!mobileOk || mobBusy` | Login.jsx:237–253 | PARTIAL | login.jsx:170–179 | `idInUse` (`GET /onboarding/known`), `sendOtp` | Logic identical. STRING DIFFERS: web `MOBILE_TAKEN` (SubscribeFlow.jsx:20) = "This mobile number is already in use. Create using a different number."; phone (login.jsx:29) = "This mobile number already has a Meyy sign in. Tap Sign in below." — and the web's reasoning comment says the create door must NOT point at sign-in. Align to the web's words (or lift the constant to shared). Phone `busy` shows a spinner instead of the label text (spinner replaces "Sending…"). |
| 18 | OTP boxes: label "Enter the OTP", `otpLen` boxes, `type=tel`, box 0 `autoComplete="one-time-code"` + `maxLength=otpLen`, others 1; auto-advance; backspace steps back; whole-code paste spreads; `aria-label "OTP digit N"` | Login.jsx:258–270, 104–118 | DONE | OtpBoxes.jsx:1–46, login.jsx:182–184 | `OtpBoxes`, mono font | Phone adds partial-paste spread (161–163), `selectTextOnFocus`, `autoFocus` on box 0 (web does not autofocus the OTP boxes) and `textContentType="oneTimeCode"`/`autoComplete="sms-otp"` — SMS-code autofill is the named phone capability. Backspace via `onKeyPress` (native answer to `onKeyDown`). Box 44×52, gap 8 (`.ob-otp-box` measures hand-set, not in web.js). |
| 19 | Live: "Sent by SMS to +91 {mobile}. Resend" (Resend disabled while `otpBusy`; clears otp/err, re-requests) | Login.jsx:271–275 | DONE | login.jsx:185–189 | `sendOtp` | Phone uses an inline pressable Text; guard `if (otpBusy) return` instead of `disabled`. Web's `if (ok) setOtpErr("")` is a no-op both sides. |
| 20 | Stub: "Preview build: enter **0000**." | Login.jsx:278 | PARTIAL | login.jsx:190 | — | Phone drops the bold on 0000. |
| 21 | `otpErr` line | Login.jsx:280 | DONE | login.jsx:191 | `ErrorLine` | Phone additionally clears `otpErr` on every box change (183). |
| 22 | "Verify & continue →" / "Verifying…" — disabled `otp.length !== otpLen || otpBusy` | Login.jsx:281–282 | DONE | login.jsx:192 | — | Phone: `busy` → spinner; label "Verifying…" unreachable behind the spinner. |
| 23 | `verifyOtp`: live `verifyOtpRemote` → err; stub `!== "0000"` → "That code didn't match. (Preview build: use 0000.)"; then `POST /onboarding/verified` (`authHeaders(num)`) → `uid = d.user_id` (swallow failure) | Login.jsx:130–145 | DONE | login.jsx:89–104 | `verifyOtp`, `authHeaders`, `/onboarding/verified` | Identical. |
| 24 | After verify: `mode==="subscribe" && flow==="create"` → subscribe screen (`setMobile(uid)`), else `enter(uid)` | Login.jsx:146–147 | NOT-PORTED-BY-DECISION | login.jsx:105 (always `enter`) | SubscribeFlow | — |
| 25 | `enter(id)` = `markSeen()` + `onEnter(id)` → page.jsx:632–638 `setUser(id)` + `primeBank()` | Login.jsx:99; page.jsx:632–638 | DONE | login.jsx:73–78 | `setUser`, `primeBank`, expo-router | Phone also `router.replace("/(app)")`. |
| 26 | Foot "← Back" → resets otpSent/otp; → signin (return) or choose (create) | Login.jsx:287 | DONE | login.jsx:153 | — | — |

### C. Login — SIGN-IN screen (web 293–350 · mobile 199–214)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes |
|---|---|---|---|---|---|---|
| 27 | Benefits block repeated + `.ob-rule` hairline | Login.jsx:328–329 | PARTIAL | login.jsx:204 | — | Benefits present; the `.ob-rule` divider is absent on the phone. |
| 28 | Kicker "Sign in" (`.kicker.login-kicker`) + h1 "Who's planning today?" (`.login-q`, Fraunces 26/23px) | Login.jsx:330–331 | MISSING | — | `.login-q` (globals.css:409, 420) | Phone has no kicker and no question heading; it goes straight to the field. Product copy dropped — restore. |
| 29 | Field label "Mobile number or email", placeholder "98xxxxxxxx or you@example.com", `type=text autoComplete=off spellCheck=false`, ref for desktop autofocus | Login.jsx:333–337 | PARTIAL | login.jsx:205–208 | `Field`, `Input` | Phone label is "Sign in with your mobile or email" and placeholder "Mobile number or email" — both differ from the web. Phone adds `keyboardType="email-address"`, `textContentType="username"`, `returnKeyType="go"` + `onSubmitEditing` (the form-submit translation) — fine as native answers, but the strings should match. |
| 30 | Desktop-only autofocus (≥601px) | Login.jsx:92–96 | WEB-ONLY-BY-NATURE | — | — | Phone correctly does not autofocus (keyboard would cover the foot). |
| 31 | Submit "Enter →" (`.primary.login-btn`), disabled `!signinOk`; `<form onSubmit>` | Login.jsx:332, 338–340 | PARTIAL | login.jsx:212 | — | Phone label is "Sign in →" (web: "Enter →") and adds a `signinBusy` spinner the web lacks. Align label. |
| 32 | `submitSignin`: `GET /onboarding/known?id=` → known: stub `enter(uid)`; live: `setMobile(uid); flow="return"; mode="trial"; mobErr=""; otp=""; sendOtp(uid)` → err → `signinErr`; else `otpSent=true; screen="otp"` | Login.jsx:298–313 | DONE | login.jsx:108–124 | `getJSON`, `sendOtp` | Identical bar `mode` (n/a on phone). |
| 33 | Error "More than one Meyy account uses this email. Please sign in with your mobile number." (`reason==="ambiguous_email"`) | Login.jsx:314–317 | DONE | login.jsx:126 | — | — |
| 34 | Error "We don't recognise this mobile or email yet — tap “New to Meyy? Get started” below to create your sign in." | Login.jsx:319 | DONE | login.jsx:127 | — | — |
| 35 | Error "Couldn't reach Meyy right now. Try again in a moment." | Login.jsx:321 | DONE | login.jsx:130 | — | — |
| 36 | Phone-only quiet line (live): "We'll send a one-time password to the mobile on the account." | — | — | login.jsx:210 | — | Not on the web. Either add to web or drop — a product string added without a founder note. |
| 37 | Trust line "🛡 Your data is private and secure · Privacy Notice" (`.fr-secure`, link → `openPrivacy`) | Login.jsx:343–344 | MISSING | — | `.fr-secure` (globals.css:3346), `/privacy` | The returning sign-in has NO privacy link on the phone — the OTP screen has one, but the web's DPDP reasoning (Login.jsx:75–80) links it from BOTH screens. |
| 38 | Foot link "New to Meyy? Get started →" → choose | Login.jsx:347 | PARTIAL | login.jsx:203 | — | Phone drops the trailing "→". |
| 39 | `signinErr` cleared on submit only | Login.jsx:301 | DONE | login.jsx:206 (also on each keystroke) | — | Harmless difference. |

### D. Login — SUBSCRIBE path (web 151–160 → SubscribeFlow.jsx)

Coarse rows only, per assignment. All NOT-PORTED-BY-DECISION (plan §0: beta on manual grants; `BillingProvider` is the seam).

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes |
|---|---|---|---|---|---|---|
| 40 | `<SubscribeFlow userId chrome={<Bar/>} onDone=enter onCancel→otp onTrial→enter>` mount from Login | Login.jsx:151–160 | NOT-PORTED-BY-DECISION | — | SubscribeFlow, Agreement | The in-app door (page.jsx paywall/Settings) passes no `onTrial`. |
| 41 | Five-step rail `.ob-steps`: Verify · About you · Agreement · Subjects · Pay | SubscribeFlow.jsx:86–100 | NOT-PORTED-BY-DECISION | — | — | — |
| 42 | Trial-or-Subscribe OFFER modal on mount (front door only, once per session; `.ob-offer-back/.ob-offer-box`) | SubscribeFlow.jsx:228–236, 354–386 | NOT-PORTED-BY-DECISION | — | — | Trial → `onTrial()` = signed straight in. |
| 43 | About-you screen (name, email w/ `EMAIL_TAKEN` check via `idInUse`, `Dropdown` Role/State, school) → `GET /account` prefill, "Save & continue →" | SubscribeFlow.jsx:65–66, 175, 387–491 | NOT-PORTED-BY-DECISION | — | `Dropdown.jsx`, `/account` | — |
| 44 | Agreement step (`<Agreement mode="sign" userId …>`), skipped when `GET /legal/consent/status` says accepted | SubscribeFlow.jsx:214–225, 493–533 | NOT-PORTED-BY-DECISION | — | Agreement.jsx | Agreement's sign mode is SubscribeFlow-only; read mode belongs to Settings › Legal (other reader). |
| 45 | Subjects & stages cart (`GET /subjects`, `/subjects/{s}/grades`, `stageOfGrade`, `scopeLabel`, price rows) | SubscribeFlow.jsx:240–260, 535–634 | NOT-PORTED-BY-DECISION | — | — | — |
| 46 | Pay stub "Review & pay" · ₹ rows · "Total ₹N / year" · "Preview build: online payment opens soon — this activates your…" · `POST /onboarding/checkout` (409 on missing consent) → `onDone(uid)` | SubscribeFlow.jsx:270–300, 636–687 | NOT-PORTED-BY-DECISION | — | `/onboarding/checkout` | — |
| 47 | `MOBILE_TAKEN` / `EMAIL_TAKEN` exported constants (shared wording with the API's 409) | SubscribeFlow.jsx:15–20 | PARTIAL | login.jsx:29 | — | Phone re-declares `MOBILE_TAKEN` with different words (row 17). Lift to `@aruvi/shared`. |

### E. PrivacyNotice (web PrivacyNotice.jsx · mobile privacy.jsx)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes |
|---|---|---|---|---|---|---|
| 48 | Bare fetch `GET /legal/privacy` (no identity), optional `?version=` | PrivacyNotice.jsx:41–51 | PARTIAL | privacy.jsx:110–113 | `API` | Phone has no `version` prop (the "show the version her record names" path — needed later by Settings › Legal, other reader). |
| 49 | Loading "Loading the privacy notice…" (`.fr-loading`) | PrivacyNotice.jsx:54 | PARTIAL | privacy.jsx:120 | — | Phone shows a spinner, no words. |
| 50 | Failure "The privacy notice couldn't be loaded just now. Check your connection and try again." (`.lgl-fail`, role=alert) | PrivacyNotice.jsx:48–49, 53 | PARTIAL | privacy.jsx:112, 121 | — | Phone string is "Couldn't load the notice right now." — align. |
| 51 | Locked three-band frame (`frame` prop → `.lgl-frame`; caller adds `.ob-wrap-lock/.ob-body-lock`): head pinned, document scrolls, Back pinned | PrivacyNotice.jsx:29–35, 60; Login.jsx:163–175 | PARTIAL | privacy.jsx:115–128 | — | Phone: Bar + ScrollView + pinned foot — the document scrolls and Back is pinned, but the TITLE scrolls away with the body (web pins it in `.lgl-head`). |
| 52 | Framed: Back button ABOVE the title, inside the pinned head (`.fr-link.lgl-back`, label "← Back") | PrivacyNotice.jsx:65–67 | PARTIAL | privacy.jsx:125–127 | — | Phone puts Back at the FOOT (the web's UNframed placement). Founder's 2026-09-04 rule was "the way out is visible from the first line". |
| 53 | Title `h1.ob-title` = `doc.title || "Privacy Notice"` | PrivacyNotice.jsx:68 | DONE | privacy.jsx:119 | `type.title` | — |
| 54 | Older-version hint "This is version X, which you were shown. The current notice is version Y." | PrivacyNotice.jsx:71–74 | MISSING | — | `version` prop | Only matters with `version`; note for Settings reader. |
| 55 | Body `renderMarkdown(doc.body, "pn")` in `.lgl-agreement.lgl-privacy-body` | PrivacyNotice.jsx:75 | DONE | privacy.jsx:122 → Markdown.jsx | `parseMarkdown` | Phone Markdown = same block set; tables ALWAYS stacked (the ≤600px rule). Bold/italic runs mapped to `type.bodyStrong/bodyItalic`. |
| 56 | Version line "Version {v} · {dateWords(published)} · English · This notice is available at any time under Settings › Legal." (`.lgl-version`) | PrivacyNotice.jsx:76–79 | PARTIAL | privacy.jsx:123 | `dateWords` (shared) | Phone prints only "Version {v}" — drops date, language and the Settings pointer. |
| 57 | Unframed foot Back (`.lgl-privacy-foot`) — read mode in Settings / sheet | PrivacyNotice.jsx:83–87 | — | — | — | Settings reader's concern; the phone's one screen is effectively this shape. |
| 58 | Return path: `onBack` → `setScreen(privacyFrom || "signin")` | Login.jsx:171, 82 | DONE | privacy.jsx:126 `router.back()` | expo-router | Equivalent. |

### F. Agreement (web Agreement.jsx) — sign mode is SubscribeFlow-only; read mode is Settings › Legal

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes |
|---|---|---|---|---|---|---|
| 59 | Load `GET /legal/consent` (front door: `authHeaders(userId)`; signed-in: `getJSON`) | Agreement.jsx:80–91 | NOT-PORTED-BY-DECISION (sign) / MISSING (read, Settings step 6) | — | `authHeaders` | Read mode needs only `getJSON`. |
| 60 | Fail "The agreement couldn't be loaded just now. Check your connection and try again." · loading "Loading the agreement…" | Agreement.jsx:88–94 | MISSING (read) | — | — | — |
| 61 | Heading "Legal Agreement with User" | Agreement.jsx:152 | MISSING (read) | — | — | — |
| 62 | Read-mode accepted line "✓ Accepted on {date} · version {v}" (+ " — a newer version applies from your next subscription") / "You haven't accepted this agreement yet — you'll be asked to when you subscribe. It's here to read at any time." | Agreement.jsx:159–173 | MISSING (read) | — | `dateWords` | Settings reader. |
| 63 | Sign-mode prior-version hint "The agreement has been updated since you accepted version X. Please read it again and confirm each point." | Agreement.jsx:177–180 | NOT-PORTED-BY-DECISION | — | — | — |
| 64 | Intro (`doc.intro` markdown) + five `.lgl-ack` blocks (n · title · body) | Agreement.jsx:182–203 | MISSING (read) | — | `Markdown` | Read mode renders them without checkboxes. |
| 65 | Per-point checkbox "I understand and agree." (sign) | Agreement.jsx:194–200 | NOT-PORTED-BY-DECISION | — | — | Would need a native checkbox (no RN primitive; Pressable + glyph). |
| 66 | Agreement body `.lgl-agreement` | Agreement.jsx:206 | MISSING (read) | — | — | — |
| 67 | Final tick with "Privacy Notice" words as a link (`withNoticeLink`) → privacy SHEET (`.lgl-sheet`, `.ap-close` ✕) | Agreement.jsx:15–28, 208–216, 279–290 | NOT-PORTED-BY-DECISION | — | — | — |
| 68 | Optional marketing tick (`doc.optional`, v0.4+), `marketing_email` in POST | Agreement.jsx:71–75, 225–234 | NOT-PORTED-BY-DECISION | — | — | — |
| 69 | Version line "Version {v} · English · This agreement is available at any time under Settings › Legal." | Agreement.jsx:236–237 | MISSING (read) | — | — | — |
| 70 | Frozen foot: tally "Five points" + five jump boxes (`jumpTo` scrollIntoView), `.lgl-need` sentence, err, "I accept — continue →"/"Recording…", Back | Agreement.jsx:245–274 | NOT-PORTED-BY-DECISION | — | — | `scrollIntoView` → `ScrollView.scrollTo` + `measureLayout` if ever ported. |
| 71 | `POST /legal/consent` body + 409 `errDetail` handling | Agreement.jsx:101–135 | NOT-PORTED-BY-DECISION | — | — | — |

### G. FirstRun — shared plumbing, bar, rail (web 115–563)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes |
|---|---|---|---|---|---|---|
| 72 | Activation GATE: `!ready \|\| firstGenNeeded` → `<FirstRun>` instead of the shell; `ready` = GET /readiness has ≥1 subject; wait for `readinessLoaded` (no flash) | page.jsx:1073–1082, 39–47 | MISSING | (app)/index.jsx:255 prints a placeholder | `fetchReadiness`/`cachedReadiness` (shared), (app)/_layout.jsx | The phone's `(app)/_layout.jsx` redirects only on `!getUser()`. A first-run route (`app/first-run.jsx` or `(app)/first-run`) plus a gate in `_layout` (or `index.jsx`) on `readiness.subjects.length === 0` is needed; the loading state must hold until `/readiness` resolves. |
| 73 | `firstGenNeeded` heuristic for a subscriber whose checkout built a profile: `ready && nothing in /plans-prepared && nothing bound in /section-state && no prior_years in /academic-year` → first run; one-shot latch per user (`everGeneratedRef`, `latchUserRef`) | page.jsx:395–439 | MISSING | — | `GET /plans-prepared`, `GET /section-state`, `GET /academic-year` | Only reachable by a manually-granted subscriber whose profile was provisioned server-side — that IS the beta path, so it is needed on the phone. |
| 74 | Props contract `{ user, onComplete(payload, preparing), onPrepared({subject,grade,filename}), onPrepareError(desc, message), onExit, onSignOut }` | FirstRun.jsx:115; page.jsx:1081–1082 | MISSING | — | `lib/preparing` | On the phone the three callbacks collapse into the store: `startPreparing(desc)` (= onPreparing/setPreparingCard), `clearPreparing()` (= onPrepared), `failPreparing(msg)` (= onPrepareError), `paywallPreparing` for a 402 (web FirstRun has NO 402 branch — see row 112). `onComplete` becomes a local function (row 108). `onExit` is never passed by page.jsx — dead prop. |
| 75 | First name for the bar: `GET /account` → `display_name` first word, capitalised, ignored if numeric | FirstRun.jsx:117–130, 538 | MISSING | Bar.jsx:553 prints raw `user` | `/account` | Bar.jsx shows the id; the web's shell bar also shows the first name (page.jsx, other reader). Add the same `/account` read once in Bar or a shared hook. |
| 76 | Bar `.fr-brand` › `.hdr` with brand + `.hdr-user-id` (name, "Log out" → `onSignOut`); NO gear, NO nav; `position:fixed` + measured `--fr-bar-h` | FirstRun.jsx:521–545, 266–279 | PARTIAL | Bar.jsx:528–563 | `endSession` | Bar renders identity + Log out whenever `getUser()`; the gear renders inert when `onSettings` is null — on first run the web has NO gear at all. Pass a prop to hide it, or accept the inert glyph (a visible divergence). `--fr-bar-h` measurement = WEB-ONLY-BY-NATURE (native layout). Bottom nav must NOT render on first run (Phase 1 is shell-less) → the route must live OUTSIDE `(app)` or `_layout` must suppress `BottomNav`. |
| 77 | `GET /subjects` on mount → `subjects` | FirstRun.jsx:205–206 | MISSING | — | `getJSON` | — |
| 78 | `fetchEntitlement()` → `trialInfo` (welcome trial card + scope filter) | FirstRun.jsx:209, 553–563 | MISSING | (prepare.jsx:103 has the same call) | shared `fetchEntitlement` | — |
| 79 | Subscribed-entry scope filter: `frPaidScopes` = enforced && (active\|grace) && no "*" → `visibleSubjects` (scope subject) / `visibleGrades` (scope stage via `stageOfGrade`) | FirstRun.jsx:553–563 | MISSING | — | shared `stageOfGrade` | — |
| 80 | `GET /subjects/{s}/grades` sorted by `ROMAN` | FirstRun.jsx:220–226 | MISSING | — | shared `ROMAN` | — |
| 81 | `GET /subjects/{s}/{g}/chapters` → chapters minus `placeholder`; `standard_duration_minutes` → `stdDuration` + seed `durationMin` iff `!durationTouched`; `annual_budget_periods` → `annualBudget` | FirstRun.jsx:229–256 | PARTIAL | prepare.jsx:112–118 (chapters + placeholder filter only) | — | prepare.jsx does not read `standard_duration_minutes` / `annual_budget_periods`. |
| 82 | `GET /genon/{s}/{g}/chapters` → `genonChs`, `canonical_minutes` | FirstRun.jsx:257–259 | DONE (pattern) | prepare.jsx:120–127 | — | Reuse verbatim. |
| 83 | Progress rail `ol.fr-prog` — Subject · Class · Chapter; `.fr-prog-dot` (✓ when done, number otherwise), `.fr-prog-label` mono 10.5px uppercase, connector `::before` 2px (`--line`, pine when done/current) | FirstRun.jsx:493–505; globals.css:2311–2325 | MISSING | — | web.js `fr_prog*` (not present) | Measure `.fr-prog`, `.fr-prog-step`, `.fr-prog-dot`, `.fr-prog-label` + `.done/.current` colours. Connector line = absolute View between dots. |
| 84 | Step-arrival effect: arriving on chapter closes any open edit box | FirstRun.jsx:215–217 | MISSING | — | — | — |
| 85 | `classNum(g)` (ROMAN idx+3) | FirstRun.jsx:102–105 | DONE | shared format.js:18 | — | Already in shared; FirstRun's local copy is a duplicate. |
| 86 | `tagFor(letter)` = `${classNum(grade)}${letter}`; `sections=["A"]` | FirstRun.jsx:172, 336 | MISSING | — | — | Section key convention `${subject}_${grade}_${tag}` (matches (app)/index.jsx:57). |

### H. FirstRun — WELCOME (web 566–606)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes |
|---|---|---|---|---|---|---|
| 87 | `.fr-wrap.fr-welcome` › `.fr-welcome-body` › h1 "Welcome to Meyy!" (`.fr-welcome-title` 30px / 36px ≥ bp) | FirstRun.jsx:568–575; globals.css:3332, 3354 | MISSING | — | web.js `fr_welcome_title` | — |
| 88 | Trial card (only `trialInfo.enforced && status==="trial"`): `.fr-welcome-rule` + `.fr-trial-card` › `.fr-trial-tick` ✓ · h2 "Your free trial" · p "Your free trial covers any {trial_chapter_cap} chapters. For any single chapter, you can generate unlimited number of Lesson plans." · h2 "To get started" · p "Answer three quick questions and Meyy will create your first lesson plan." | FirstRun.jsx:576–591 | MISSING | — | `/entitlement` | Measure `.fr-trial-card/-tick/-h/-p`, `.fr-welcome-rule`. |
| 89 | Non-trial variant: h2 "To get started" (`.fr-welcome-h2`) + p (`.fr-welcome-sub`) same sentence | FirstRun.jsx:593–598 | MISSING | — | — | — |
| 90 | Foot `.fr-foot` › "Prepare my first lesson →" (`.primary.fr-cta.prepare-cta`) → step subject | FirstRun.jsx:601–603 | MISSING | PrepareCta.jsx (identity) | `PrepareCta size="primary"`, web.js `fr_foot/fr_cta` (present: 649–652) | The clay→ochre CTA identity is already ported. |

### I. FirstRun — STEP 1 SUBJECT (web 609–629)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes |
|---|---|---|---|---|---|---|
| 91 | Rail active "Subject"; `.fr-step-body` › h1 `.fr-q` "What do you teach?" · `.fr-hint` "Let's start with one subject. Roll the box or use the arrows — the subject shown is your pick." | FirstRun.jsx:613–616 | MISSING | — | web.js `fr_q` (628, present), `fr_hint` (absent) | — |
| 92 | "Loading subjects…" (`.fr-loading`) while empty | FirstRun.jsx:617 | MISSING | — | — | — |
| 93 | `RollWheel ariaLabel="Subject" large` items `{id: slug, chip: first letter, label: pretty}` | FirstRun.jsx:619–620 | PARTIAL | RollWheel.jsx base mode | web.js `rw_shell_base/rw_row_base/rw_label_base/rw_chip*` (present) | Base mode exists (▲▼, chip). `large` (`.fr-wheel-lg .fr-wheel-label` 17px, globals.css:2399) is NOT a RollWheel prop on the phone — add a `large` label style. Web arrow-key stepping = WEB-ONLY-BY-NATURE (named in RollWheel.jsx header). `items` must be memoised (prepare.jsx:208–216 lesson). |
| 94 | Foot: "Continue" (`.primary.fr-cta`, disabled `!subject`) + "← Back" (`.fr-link`) → welcome | FirstRun.jsx:624–625 | MISSING | — | `Button`, `Link` / web.js `fr_cta`, `fr_link` | Plain pine `.fr-cta`, not the prepare identity. |

### J. FirstRun — STEP 2 CLASS (web 632–671)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes |
|---|---|---|---|---|---|---|
| 95 | Rail active "Class"; h1 "Which class do you teach {pretty(subject)} to?" · hint "You can add more classes later. Roll the box or use the arrows — the class shown is your pick." | FirstRun.jsx:636–639 | MISSING | — | — | — |
| 96 | "Loading classes…" | FirstRun.jsx:640 | MISSING | — | — | — |
| 97 | `RollWheel ariaLabel="Class" large` items `{id: g, chip: classNum(g), label: "Class N"}`; onChange resets `durationTouched=false` then sets grade | FirstRun.jsx:644–646 | MISSING | RollWheel base | — | The `durationTouched` reset is a guard, keep it. |
| 98 | Section STATEMENT `.fr-sec-note`: "We'll start you with **Section {tagFor("A")}**. You can add or rename sections any time from your teaching profile." (only when `grades.length>0 && grade`) | FirstRun.jsx:657–662; globals.css:2334–2338 | MISSING | — | web.js `fr_sec_note` | — |
| 99 | Foot: "Continue" (disabled `!grade`) → chapter; "← Change subject" → subject | FirstRun.jsx:665–667 | MISSING | — | — | — |

### K. FirstRun — STEP 3 CHAPTER (web 697–814)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes |
|---|---|---|---|---|---|---|
| 100 | Rail active "Chapter"; h1 "Choose the chapter to teach" · hint "Roll the box or use arrows to pick one chapter." | FirstRun.jsx:700–703 | MISSING | — | — | — |
| 101 | "Loading chapters…" | FirstRun.jsx:705 | MISSING | — | — | — |
| 102 | `RollWheel ariaLabel="Chapter" rowPx={92}` items `{id: String(n), chip: n, label: title}`; onChange = `pickChapter` | FirstRun.jsx:707–708 | DONE (pattern) | prepare.jsx:400–402 (`rowPx=92 padLeft=14 clamp=2`) | RollWheel base | Reuse prepare's wheel call; onChange must be `pickChapter` (row 103), not a bare setter. |
| 103 | `pickChapter(no)`: set chapterNo, `defaultPeriods=estimateFor(no)`, `periodsTouched=false`, `periods=est`, close periods box; `estimateFor` = round(`recommended_periods`) else 12 | FirstRun.jsx:295–314 | MISSING | prepare.jsx:193–206 uses a DIFFERENT suggestion (largestRemainder over HER budget) | — | First run has no profile yet, so it reads the API's `recommended_periods` (master plan) — do NOT reuse prepare's `sugByChapter`. |
| 104 | Chapters-arrived effect (re-seed periods only if `!periodsTouched`, keep `defaultPeriods` tracking) | FirstRun.jsx:322–330 | MISSING | — | — | — |
| 105 | `.fr-defaults` › duration box `.fr-default` (+`.fr-default-editing`): kicker row "Class duration" + tag "NCF recommended" (`.fr-tag-recommended`, iff `durationMin === stdDuration`); view row "{n}-minute classes" (`.fr-default-val.fr-default-val-muted`) + "Change" (`.fr-change-btn`); edit: `RollWheel ariaLabel="Class duration"` over `DURATION_CHOICES` (chip = minutes, label "minute classes"; onChange sets `durationTouched=true`) + note `.fr-dur-note` "Some classes run longer than others. Let's keep to one duration for now — you can add more later." + "Done" (`.fr-done-btn`) | FirstRun.jsx:712–739; globals.css:2423–2453 | MISSING | — | web.js `fr_default*`, `fr_tag_recommended`, `fr_change_btn`, `fr_done_btn`, `fr_dur_note` | prepare.jsx deliberately has NO duration control (durations come from the profile) — first run is the one place a duration is asked. Only one edit box open at a time (`editingField`). |
| 106 | Periods box: kicker "Estimated periods" + tag "Meyy recommended" (iff `periods === defaultPeriods`); view "{n} periods" + "Change"; edit: `RollWheel ariaLabel="Estimated periods"` over 1..60 (label "period"/"periods"; onChange `periodsTouched=true`) + "Done" | FirstRun.jsx:740–764 | MISSING | prepare.jsx:406–425 uses a −/+ stepper + TextInput instead | — | Different control by design: first run uses a WHEEL, prepare a stepper. Do not substitute. The two tag strings differ on purpose (FirstRun.jsx:743–747). |
| 107 | Sanity band `.fr-bud-warn` (only when `periods !== defaultPeriods` AND `<5 \|\| >25`): "That's very few periods for a chapter — you can still go ahead." / "That's a lot of periods for one chapter — you can still go ahead." | FirstRun.jsx:771–777; globals.css:3084 | MISSING | — | web.js `fr_bud_warn` | — |
| 108 | Coverage floor `.prep-floor` (genonAvailable && canonMinutes): `floorP = round(0.6·cm/dur)`; if `periods < floorP`: "Below {floorP} periods the plan compresses; some sections may move to guided self-study — the plan still closes the chapter and names them." | FirstRun.jsx:778–803 | DONE (pattern) | prepare.jsx:581–605 `CoverageNote` | web.js `prep_floor` (present) | `CoverageNote` also emits the "Above {topP}…" surrender line, which first run does NOT show — pass `canonTop={undefined}`? No: it derives `topP` from minutes when `canonTop` is absent. Either add a `floorOnly` prop or inline the floor test as the web does. |
| 109 | Foot: "Prepare the lesson →" (`.primary.fr-cta.prepare-cta`, disabled `!chosenChapter \|\| activating`) → `prepareAndHandOff`; "← Change class" → grade | FirstRun.jsx:810–811 | MISSING | PrepareCta.jsx | `PrepareCta` | — |

### L. FirstRun — activation + handoff (web 352–490; page.jsx 441–480, 554–600)

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes |
|---|---|---|---|---|---|---|
| 110 | `buildActivationPayload(over)` — canonical `subjects[]` record: `name: pretty(subject)`, `grades:[{grade: gradeUp(grade), sections:[{tag,sec}], durations:[durationMin], ppw_by_duration: normPpw([d],{[d]:ppw},ppw,lowest), ppw_anchor: lowestDuration, periods_per_week: ppwMapSum}]`, `grids:[[[-1,-1,-1,-1,-1,-1]]]`, `budget:{0: …}` | FirstRun.jsx:352–374 | MISSING | — | `normPpw`, `ppwMapSum`, `lowestDuration`, `DEFAULT_PPW` — currently in `web/app/components/wheels.jsx:434–483` (JSX file, NOT in shared); `gradeUp`, `pretty` (shared) | Lift the four pure helpers (and `DEFAULT_DURATION/DEFAULT_PPW`) into `@aruvi/shared` (they are also needed by the TeachingProfile port). For a single duration `normPpw` reduces to `{[d]: ppw}`. |
| 111 | Seeded defaults in `prepareAndHandOff`: `seededPpw = annualBudget ? ppwFromAnnual(annualBudget) : DEFAULT_PPW(6)`; budget `{method:"periods", value: annualBudget}` else `{method:"weeks", value:30}`; durations `[durationMin]`; `rows=[{duration: durationMin, count: periods}]`; descriptor `{subject, grade, chapterNo: Number, chapterTitle, rows}` | FirstRun.jsx:421–456 | MISSING | prepare.jsx:295–300 (descriptor shape identical) | shared `ppwFromAnnual`, `ESTIMATE_WEEKS=30` | Descriptor shape matches `startPreparing`'s contract (preparing.js:281–287) — prepare.jsx passes `chapterNo` as the string id; ProposedCard `pad()`s it either way. |
| 112 | `finishActivation(over)`: clear `current_chapter_/lu_pointer_/lu_done_` for the section key + `pushSectionState(secKey)` (deletes stale server row); `onComplete(payload, preparing)` | FirstRun.jsx:386–406 | MISSING | — | shared `sectionState` (`unbindSection` / `pushSectionState`) | On the phone use `unbindSection(secKey)` (sectionState.js:42) rather than raw key removal. |
| 113 | page.jsx `onFirstRunComplete(payload, preparing)`: latch `everGeneratedRef`; MERGE with existing `readiness.subjects` by name (subscriber's other purchased subjects survive); `setReadiness(projectReadiness)`; `setReady(true)`; set subject/grade; `goLessons()`; `setPreparingCard(preparing)`; `verifyReadiness(merged)` = `POST /readiness {subjects}` then `GET /readiness` fingerprint check (`verifiedWrite`, `readinessFingerprint`) → on mismatch adopt server truth, `saveFailed` | page.jsx:357–393, 441–480 | MISSING | — | shared `projectReadiness`, `verifiedWrite`, `readinessFingerprint`, `invalidateReadiness`; `lib/preparing.startPreparing`; `router.replace("/(app)/lessons")` | Phone: POST /readiness (no shared writer exists — readiness.js has only read/invalidate; add one or fetch inline as page.jsx does), `invalidateReadiness()`, `startPreparing(preparing)`, navigate to My Lessons. `saveFailed` surface (shell banner, other reader) is owed too. |
| 114 | The serve, NOT awaited: `POST /genon/{s}/{g}/{ch}/plan {rows}` → hold `max(0, 5000 − elapsed)` → `markPrepared(s,g,filename)` (`POST /plans-prepared`, NO `periods` arg on first run) → `invalidatePlans("s/g")` → `bindSectionChapter(secKey, filename)` → `onPrepared` | FirstRun.jsx:458–483 | PARTIAL | prepare.jsx:302–323 (serve + hold + invalidate + `clearPreparing`) | shared `postJSON`, `markPrepared`, `invalidatePlans`, `bindSectionChapter`; `PREPARING_MS` | Differences from prepare.jsx to keep: (a) first run ALWAYS holds 5 s (never `already_yours`); (b) it calls `markPrepared` explicitly (prepare.jsx relies on the serve registering + a `verifiedWrite` read-after-write on `/plans-prepared` — first run has no read-after-write; consider adding prepare's); (c) it BINDS the plan to the default section (`bindSectionChapter`) — the card lands attached; (d) `chapterNo` is `Number`. Then `clearPreparing()`. Also `invalidatePlans` BEFORE `clearPreparing` (prepare.jsx:319–323 ordering rule). |
| 115 | Failure: `onPrepareError(preparing, e.detail \|\| "Couldn't build the lesson plan right now. Try again in a moment.")` → failed card stays with Dismiss | FirstRun.jsx:484–489; page.jsx:599–601 | DONE (pattern) | prepare.jsx:324–336 `failPreparing`; ProposedCard failed state | `lib/preparing` | Web FirstRun has NO 402 branch (a 402 would render as a failed card, not the paywall window). prepare.jsx does branch (`paywallPreparing`). On first run a 402 is only possible for a lapsed/exhausted account that somehow has no profile — follow prepare.jsx's branch anyway; it is strictly better. |
| 116 | Landing: shell opens on MY LESSONS with the proposed card at the head (`onPreparing` descriptor → `preparingCard`), replaced in place by `onPrepared` | page.jsx:471–474, 554–600; MyLessonPlans (other reader) | DONE (mechanism) | lessons.jsx:144–268, 444–452, 618–624 | `lib/preparing`, ProposedCard | Already the everyday prepare's mechanism; first run only needs to call `startPreparing` then `router.replace("/(app)/lessons")` (replace, not navigate — first run must not stay on the stack). lessons.jsx scopes the card to its wheels' subject·grade (444–446); it must open on the first-run subject·class — page.jsx sets `subject`/`grade` before `goLessons` (461–462, 473). The phone's lessons.jsx wheel default is profile order (subjects[0]) which, with a one-subject profile, matches. |
| 117 | Tour offer (`.dash-nudge`) beneath the first lesson on My Lessons and My Classes; `startTour` → `goClasses()` first; GuidedTour steps anchor `nav-classes`, `nav-lessons`, `grow-add`, `ask-aruvi`, section card, `unit-tabs`… | page.jsx:265, 1290, 1336, 1427 | DEFERRED | — | GuidedTour (named deferred in BRIEF) | Dependency only — owned by the page.jsx / MyPlans reader. First run's landing does not depend on it. |
| 118 | Post-tour one-shot "Are these your sections?" = `ProfilePortal` in `{mode:"check", reason:"tour"}` raised by `finishTour` (Done AND Skip) on My Classes; rows Class · Section · Periods a week · Annual period budget with current values | page.jsx:170–209 | DEFERRED | — | ProfilePortal, TeachingProfile section manage screen, GuidedTour | Dependency only — rides the tour; without the tour on the phone this prompt has no trigger. Founder question below. |

### M. Shared modules

| # | Feature / UI element | Web ref | Mobile status | Mobile ref | Depends on | Notes |
|---|---|---|---|---|---|---|
| 119 | `parseMarkdown` / `parseInline` / `dateWords` | legalmd.js:28–137 | DONE | Markdown.jsx, (dateWords unused on phone yet) | — | Web's `renderMarkdown` React renderer is web-only; phone's `Markdown.jsx` is its twin. Phone Markdown lacks an `h1`? No — parser maps `#`/`##` → h2; fine. |
| 120 | `sendOtp` / `verifyOtp` / `authHeaders` / `OTP_LEN` / `authEnabled` / `accessToken` / `signOutAuth` | auth.js:1–96 | DONE | login.jsx:19, boot.js:90, session.js:15 | supabase-js client with kv-store storage | Phone's boot.js hands the SAME storage so the cold-start token fallback works. |
| 121 | `clearTeacherCaches(extra)` with the web's four extra prefixes | signout.js; page.jsx:671 | DONE | session.js:19–25 | — | Phone mirrors the web's EXTRA list; keep in step. |
| 122 | `primeBank()` at sign-in | page.jsx:637 | DONE | login.jsx:76 | shared ask-aruvi/bank | — |

### N. CSS class families to measure into `mobile/theme/web.js` (not yet present)

`.ob-wrap/.ob-body/.ob-foot` (+ `-lock` variants) · `.ob-headline` · `.ob-benefits` · `.ob-tick` · `.ob-h2` · `.ob-plan` (+`.on`, `-hd`, `-sub`, `-points`, `-sub2`) · `.ob-title` · `.ob-sub` · `.ob-field` / `.login-field` · `.ob-mobile-row` · `.ob-cc` · `.ob-quiet` (+`.ob-legal`) · `.ob-err` · `.ob-cta` · `.ob-otp-label` · `.ob-otp-row` · `.ob-otp-box` · `.ob-rule` · `.kicker.login-kicker` · `.login-q` (409/420) · `.login-btn` · `.fr-secure` (3346) · `.lgl-link` · `.lgl` / `.lgl-frame` / `.lgl-read` / `.lgl-head` / `.lgl-back` / `.lgl-scroll` / `.lgl-agreement` / `.lgl-privacy-body` / `.lgl-hint` / `.lgl-version` / `.lgl-fail` / `.lgl-privacy-foot` · `.fr-wrap` / `.fr-welcome` / `.fr-welcome-body` / `.fr-welcome-title` (3332, 3354) / `.fr-welcome-rule` / `.fr-welcome-h2` / `.fr-welcome-sub` · `.fr-trial-card` / `-tick` / `-h` / `-p` · `.fr-prog` family (2311–2325) · `.fr-step-body` · `.fr-hint` · `.fr-loading` · `.fr-wheel-lg` (2399) · `.fr-sec-note` (2334–2338) · `.fr-defaults` / `.fr-default` / `.fr-default-editing` / `-kicker-row` / `-kicker` / `-row` / `-val` / `-val-muted` / `-wheel-wrap` (2423–2453) · `.fr-tag-recommended` (2436) · `.fr-change-btn` (2437) · `.fr-done-btn` (2447) · `.fr-dur-note` · `.fr-bud-warn` (3084) · `.prep-floor` (1183, already `prep_floor`). Already present: `fr_q`, `fr_foot`, `fr_cta`, `fr_cta_t`, `fr_link`, `fr_link_t`, `rw_*_base`, `rw_chip*`, `pcta_*`, `sc_prep*`, `hdr_*`.

## Dependency notes

Must land BEFORE FirstRun can be completed on the phone:
- **A first-run gate** in `mobile/app/(app)/_layout.jsx` (or `app/index.jsx`): after `getUser()`, hold on `fetchReadiness()`; `subjects.length === 0` → first-run route; plus the `firstGenNeeded` heuristic (`/plans-prepared`, `/section-state`, `/academic-year`) with a per-user latch. The route must render WITHOUT `BottomNav` and without the gear (Phase 1 is shell-less) — either outside `(app)` with its own `getUser()` redirect, or a `_layout` flag.
- **Lift `normPpw`, `ppwMapSum`, `lowestDuration`, `ppwAnchor`, `DEFAULT_DURATION`, `DEFAULT_PPW`, `DURATION_CHOICES`** out of `web/app/components/wheels.jsx:434–483` into `@aruvi/shared` (pure, no JSX). TeachingProfile's port needs the same.
- **A readiness WRITER in shared** (`POST /readiness {subjects}` + `invalidateReadiness`) with the `verifiedWrite`/`readinessFingerprint` read-after-write, so first run, TeachingProfile and ProfilePortal share one path. Today only page.jsx/TeachingProfile do it inline on the web.
- **RollWheel `large` mode** (17px label, `.fr-wheel-lg`) for the Subject/Class wheels.
- **Bar**: first-name from `GET /account` (shared with the shell bar) and a way to render WITHOUT the gear on first run.
- **`MOBILE_TAKEN` / `EMAIL_TAKEN`** lifted to shared so login.jsx stops re-declaring different words.
- **Login parity fixes** (cheap, no dependency): kicker + "Who's planning today?", field label/placeholder, "Enter →", "🛡 … Privacy Notice" on sign-in, `.ob-rule`, "→" on the foot link, drop the phone-only quiet line (or add it to the web), privacy screen strings/version line/Back-at-top.
- **Reusable as-is**: `RollWheel` base mode (Chapter wheel with `rowPx=92 clamp=2`), `PrepareCta`, `lib/preparing` + `ProposedCard` + lessons.jsx's consumption, prepare.jsx's `/chapters` + `/genon` fetch effect and un-awaited serve + `PREPARING_MS` hold + `failPreparing`/`paywallPreparing` branch, `CoverageNote` (floor half only), shared `fetchEntitlement`, `stageOfGrade`, `classNum`, `ROMAN`, `gradeUp`, `pretty`, `ppwFromAnnual`, `markPrepared`, `invalidatePlans`, `bindSectionChapter`, `unbindSection`/`pushSectionState`, `projectReadiness`.

What this family unlocks: a brand-new trial teacher (or a manually-granted subscriber with a server-provisioned profile) can reach My Classes/My Lessons on the phone at all — today an empty profile dead-ends at `(app)/index.jsx:255`. It also unlocks the tour offer and the post-tour "Are these your sections?" prompt (both owned by the page.jsx/MyPlans reader and deferred with GuidedTour), and the profile-portal "+" flows that assume a seeded `periods_per_week`/budget.

## Open questions for the founder

1. **OTP mobile field lock**: the phone locks the number once the OTP is sent on the create path (`editable={… && !otpSent}`); the web keeps it editable under the boxes. Which is intended? (Unnamed divergence.)
2. **`MOBILE_TAKEN` wording**: web (create door) says "This mobile number is already in use. Create using a different number."; phone says "…already has a Meyy sign in. Tap Sign in below." — the web's comment explicitly rejects pointing at sign-in from the create door. Confirm the web's words are the product's.
3. **Post-tour "Are these your sections?"** rides `finishTour`. With GuidedTour deferred on the phone, does the check window get a different trigger (e.g. first open of My Classes after first run), or wait for the tour?
4. **First run on the phone and the gear/nav**: the web's first run has no gear and no nav. The phone's `Bar` renders an inert gear whenever a user exists — acceptable, or hide it on first run for strict parity?
5. **402 on first run**: the web FirstRun has no paywall branch (a 402 becomes a failed card). Port prepare.jsx's `paywallPreparing` branch to first run (recommended) or keep strict parity?
6. **Read-after-write on the first-run serve**: prepare.jsx verifies `/plans-prepared` after the serve; the web FirstRun does not. Add it on both surfaces?
