/* Moved to packages/shared/src/ppw.js (Track D step 5d F2, 2026-09-15) — this is the web import
   path. Lifted out of components/wheels.jsx so the phone's numbers editor runs the same
   arithmetic rather than a second copy of it. */
import "./shared-setup";
export * from "@aruvi/shared/ppw";
