/* Moved to packages/shared/src/setupCheck.js (Track D step 5d F3, 2026-09-15) — this is the web
   import path. The queue is shared so the phone's check window asks, and stops asking, by exactly
   the same rules. */
import "./shared-setup";
export * from "@aruvi/shared/setupCheck";
