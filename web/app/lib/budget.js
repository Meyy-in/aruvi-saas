/* Moved to packages/shared/src/budget.js (Track D step 5c, 2026-09-15) — this is the web import
   path. Lifted out of TeachingProfile.jsx so the phone's budget editor reads the same record by
   the same arithmetic rather than deriving its own. */
import "./shared-setup";
export * from "@aruvi/shared/budget";
